# Ledger — AIS v3 (Inventory API v3)

Living document. Update at every session close. **Date opened:** 2026-09-03. Branch `feature/ais-v3` on the
deployed tree `db7c746248`.

## 0. Principles

| ID | Principle |
|---|---|
| P-1 | The authority for the AIS v3 protocol is the LL viewer source (`indra/newview/llaisapi.h`, `llaisapi.cpp` and the AIS call sites in `llinventorymodel.cpp` / `llviewerinventory.cpp`), captured in `AIS-V3-SPEC.md` with file:line. Firestorm is a test client only; no code, config, threshold or test may be keyed on Firestorm. Later sessions implement the spec document and do not open the viewer tree. |
| P-2 | Nothing in the AIS handler may take a `Scene` or `ScenePresence`: only an agent id, an `IAisInventoryBackend` and the request. Phase 2 hosts the same handler on Robust. |
| P-3 | **Every live checklist on this grid is a Firestorm result, and there is no stock-viewer control.** The stock LL viewer will not start against this grid: its Vivox voice component refuses to initialise outside SL and that stops the viewer coming up at all. This does not weaken P-1 — the LL viewer *source* stays the authority — but it fixes what a green run is worth: "Firestorm is satisfied", not "the protocol is right". Behaviour seen **only** in Firestorm must be confirmed in the source before it is relied on; behaviour the LL viewer has and Firestorm does not will never be exercised here, and that is a permanent coverage gap, not a to-do. Recorded A8, 2026-09-04. |

## 1. Decisions

| ID | Decision | Status | Ruled | Notes |
|---|---|---|---|---|
| A-D1 | Phase 1 is region-side: `AISv3Module` (`ISharedRegionModule`, `[AIS] Enabled = false`) registers `InventoryAPIv3` per agent from `OnRegisterCaps`; all inventory work goes through `IAisInventoryBackend`, so the handler is host-agnostic | **Ruled** | 2026-09-03 | Spec §Tree state T1/T6: registration + the viewer's own request of the cap name are the two conditions; module home `Source/OpenSim.Region.ClientStack.LindenCaps/AIS/` |
| A-D2 | Merge order: AIS first, before SSB's server-initiated bake work lands on `master` | **Ruled** | 2026-09-03 | SSB (`feature/ssb-appearance`) reads COF `Version` from the inventory service and does not depend on AIS (ADR-006 there); AIS changes the viewer's inventory traffic wholesale (§1g) and must be proven alone |
| A-D3 | `LibraryAPIv3` is not advertised until the library backend exists; A0 registers only `InventoryAPIv3` | Proposed | — | The viewer asks for both (`llaisapi.cpp:72-76`); `FetchItem`/`FetchCategory*` with `type == LIBRARY` and `CopyLibraryCategory` go to the library cap and fail closed without it |
| A-D4 | The `[AIS]` switch stays `false` in every shipped ini until every §1a operation returns something other than 501 | Proposed | — | Risk A-R1 |

## 2. Open questions

| ID | Question | Owner | Blocks | Status |
|---|---|---|---|---|
| A-Q1 | Item / link / category field set as `unpackMessage(const LLSD&)` reads it (`indra/llinventory/llinventory.cpp`, `llviewerinventory.cpp`) — not in A0's permitted files | A1 (needs permission to read those two files) | A1 | open |
| A-Q2 | Wire encoding the viewer sends and accepts for AIS bodies (LLSD XML vs binary) and the `Destination` header of COPY — both live in `llcorehttputil.cpp` | A1 | A1 (COPY), A2 | open |
| A-Q3 | Request bodies of `SlamFolder`, `UpdateItem` / `UpdateCategory`, `CreateInventory` `items` / `links` / `categories` arrays | A1 | A2 | **Closed A5.** Update bodies A2 (`llviewerinventory.cpp:435-454`, `:651-665`, `:1436-1457`). Slam body A3: a bare LLSD **array** (`llappearancemgr.cpp:2209-2245`, `:1795-1833`). Create A4/A5: `links` verified (`llviewerinventory.cpp:1352-1370`); `items` verified **and refused**, its builder being compiled out behind `USE_AIS_FOR_NC` (`:1120-1157`); `categories` verified A5 — `LLInventoryCategory::asAISCreateCatLLSD` (`llinventory.cpp:1256-1276`), a **base-class** method, which is why A4 could not find it: `category_id` (null on create), `parent_id`, `type_default` (integer), `name`, and optionally `thumbnail`/`favorite`. Nothing in A-Q3 is UNVERIFIED any more. |
| A-Q4 | Which viewer code paths call the remaining `Fetch*` operations and `CopyLibraryCategory` | A1 | A3 | **Partly resolved A2b/A4.** The fetch callers are `llinventorymodelbackgroundfetch.cpp:937` and `:994` (A2b). `PurgeDescendents` is `purge_descendents_of` (`llviewerinventory.cpp:1590-1643`) from Empty Trash / Empty Lost and Found (`llinventorymodel.cpp:4125-4131`, `:4136-4158`). `CopyLibraryCategory`'s own callers are still outside the permitted files and remain UNVERIFIED; the operation is implemented from the spec. |
| A-Q5 | The viewer's retry policy on 403 with `depth > 0` (caller re-requests at a lower depth?) and on transport errors | A1 | A3 | open |
| A-Q6 | Does OpenSim need to synthesise `_updated_category_versions` for every folder the data layer bumped (V6 shows the increment happens per store/delete/move) — i.e. the handler must re-read `GetFolder` after each mutation to report the new version | A2 | A2 | **Resolved A2: yes, and it is not optional.** `doUpdate` skips descendent accounting for any category absent from `_updated_category_versions` (`llaisapi.cpp:1625-1629`), so an unlisted folder keeps a stale count and version forever. Every A2 mutation re-reads the affected folder with `GetFolder` **after** the write and reports that value; a test drives a backend that changes underneath to prove the read is fresh rather than cached. |
| A-Q7 | Whether the sim should also serve `simulate` (named in the A0 brief; the viewer never sends it) | — | — | open, low |
| A-Q8 | **Depth semantics are our contract, not the viewer's.** A1 implemented `depth=N` as "N counts generations expanded below the requested folder" (`AisInventory.Walk`) and could not pin it. | **John** | A5 (advertising the cap) | **Resolved A2b, from the source rather than a capture — and A1's reading was right.** The viewer only ever sends `depth=50` (recursive) or `depth=0`; it parses the top-level category at that depth and each `_embedded` level one lower (`llaisapi.cpp:1205`, `:1461-1464`), and versions a category only while `depth >= 0` **and** its descendent count is known (`:1380-1407`). So N licenses exactly N generations below the requested folder: deeper is wasted, shallower is safe because `onAISContentCalback` re-queues every descendant anyway (`llinventorymodelbackgroundfetch.cpp:610`). Spec §1c-bis; no live capture needed. |
| A-Q9 | **`IInventoryService` cannot delete a folder that is not in Trash, and does not say so.** The only overload on the interface was `DeleteFolders(userID, folderIDs)`, the `onlyIfTrash = true` form: it `continue`s past any folder whose parent is not Trash or Lost And Found and still returns `true` (`XInventoryService.cs:459-478`), while the LL viewer calls `RemoveCategory` on any non-protected folder wherever it sits (`llviewerinventory.cpp:1545-1568`). | **John** | A5 | **Resolved A2b by John's ruling (a): `IInventoryService` gains `DeleteFolders(userID, folderIDs, onlyIfTrash)`**, add-only — the two-argument signature and every existing caller are untouched. Implemented across all eight implementors plus the Robust-side handler, which reads a new optional `ONLYIFTRASH` field and defaults it to true, so an older simulator or an older Robust keeps today's behaviour. `DELETE /category` now passes `onlyIfTrash: false`, still verifies by re-reading, and refuses protected folders with 403. A2's 409 is gone. |
| A-Q10 | **A slam has no transaction to sit in.** `IInventoryService` has no batch or transactional write, so replacing a folder's links is N `AddItem` calls plus one `DeleteItems`. A3's ordering makes the dangerous failure impossible, but a reader racing the slam still sees both sets of links for a moment, and a mid-slam process death leaves duplicates behind. | **John** | A5 | **Open.** The fix is a service-side operation that replaces a folder's links in one call — the Phase 2 `SlamLinks` this ledger already suggested under A-R2. Until then the duplicate window stands and is documented in `AisSlam.Run`. |
| A-Q13 | **`InventoryFolderBase.Version` is `ushort`** (`InventoryFolderBase.cs:67`) while the column is `int(11)`. A folder past 65535 wraps on the way through the service, which would make any version comparison — including A7's resolution rule — pick wrongly. Truly Bazar's COF is at 457, so this is not reachable today. | **John** | A7 | **Open.** Widen the property, or resolve on a value read straight from the row. Worth doing before a long-lived COF gets there. |
| A-Q14 | **Firestorm sends `UpdateCategory` twice for a single folder rename.** Two identical `PATCH /category` requests milliseconds apart, observed 11:52:32 and 11:52:41 on 2026-09-04 for one rename. | — | nothing | **Closed, no action.** The operation is idempotent: the second request sets the name that is already set and produces the same delta. Recorded only so a log full of doubled `PATCH /category` lines is not mistaken for the A-R3 re-fetch loop — A-R3 repeats without bound, this repeats exactly once. It is client behaviour and nothing server-side may be keyed to it (P-1). |
| A-Q15 | ~~Is step 13's legacy fallback in the shared LL path, or is it Firestorm's own?~~ **CLOSED A11, 2026-09-04 — and the question was mis-framed.** There is no fallback: in `create_inventory_item` the whole AIS arm sits inside `#ifdef USE_AIS_FOR_NC` (`llviewerinventory.cpp:1120`-`:1166`), the macro is **not defined**, so it never compiles and control falls unconditionally to the legacy `CreateInventoryItem` UDP send at `:1169`. The code is in the **LL tree**, so every viewer built from this source - stock included - creates items over UDP and never asks AIS. | **John** | ~~widening `AIS_Enabled`~~ | **Closed.** Step 13 no longer gates widening, and P-3's missing control run does not bite because the answer came from the source rather than a run. **Also corrected:** the A5 record's claim that "AIS answered 501" was an unverified inference - the log shows no `CreateInventory` at the notecard creation at all, only a `FetchItem` syncing the result. Our 501 route was never reached. |
| A-Q16 | **For a local user, AIS's system-folder resolution scans the suitcase too.** `AisInventory.GetSystemFolder` picks the highest-version folder of a type from `GetInventorySkeleton`, which for a local user returns every folder they own - including the suitcase's own type-46. Suitcase COFs sit at version 1 today so the root COF always wins, **but nothing enforces that**: if a suitcase COF ever overtook the root one, a local user's outfit would resolve into their suitcase. | **John** | nothing yet | **Open.** Options: prefer a candidate that is not inside a type-100 subtree, or exclude the suitcase subtree from the candidate set for a non-HG session. Needs care - for an actual HG visitor the suitcase COF *is* the right answer, and `HGSuitcaseInventoryService` warps the root deliberately (`GetRootFolder :149-165`). |
| A-Q17 | ~~Step 10 has no established cause.~~ **ANSWERED 2026-09-04 (A12/A13), and neither hypothesis was right.** H1 (a rejected response delta) is dead - A11's logging showed the deltas correct and complete. H2 (the appearance record never updated) was wrong as framed - the record was not simply never written; the wearables were correct. **The cause: the detach happened and the deferred appearance save was dropped**, because the agent left before the 5 s timer fired (`AvatarFactoryModule.SaveAppearance`, `sp == null -> continue`). Predates AIS, hits the legacy path equally, symmetric on wear. | **John** | ~~closing step 10~~ | **Closed.** Fixed in `dc4e417bb3` (T-2), deployed as `bfb50070d8`; step 10 passed on a clean run with a ~20 s gap and no slam (`A13-STEP10-CLOSED.md`). |
| A-Q18 | **`feature/ais-v3` is the integration branch and its name says otherwise.** It carries AIS v3, `fix/appearance-save-flush`, `integration/legiongrid-trusted-hg` and now `feature/ssb-appearance`, and both live servers are built from it — region `1.1.212-alpha+f4b18601d3`, Robust `1.1.208-alpha+a2c8fb63f3`. A branch named for one feature that is in fact the deployment source is exactly how the two lanes came to overwrite each other. | John | nothing technical; it is a naming and process decision | **Open, deliberately not acted on.** Candidates: rename to `integration` or fold into `develop`. Not renamed this session by explicit instruction — a rename touches every worktree, both deploy scripts and the nbgv height, and wants its own slice. Until then the rule is: **deploy only from `feature/ais-v3`, and merge feature branches into it rather than deploying them.** |

## 3. Risks

| ID | Risk | Mitigation |
|---|---|---|
| A-R1 | **Partial AIS is worse than none.** Once `InventoryAPIv3` is in the seed cap, the LL viewer routes delete, purge, slam and create through it with no fallback (spec §1g); a 501 on any of them breaks that operation for every LL-viewer user on the region | `[AIS] Enabled` defaults to `false` (A-D4); A0 registers nothing when disabled; the flag is flipped only on a test region once every operation is implemented and the harness is green |
| A-R2 | **SlamFolder atomicity.** PUT `/category/{id}/links` replaces a folder's links as one operation and the viewer expects one `_updated_category_versions` entry for it; OpenSim has no transactional multi-item write, so a crash or a concurrent change between the writes leaves a half-slammed COF. | **Addressed A3, not eliminated.** The slam creates the new links **first** and removes the old ones **second**, so the folder never holds fewer links than it started with and a stripped outfit is impossible. A failed creation rolls back the creations already made; a failed rollback leaves extra links, never fewer; a failed removal leaves both sets. All three are covered by fault-injection tests. **The remaining window:** between the last creation and the last removal the folder holds both sets, so a bake or fetch racing the slam sees duplicates. Closing it needs a transactional or batch write on `IInventoryService`, which does not exist — see A-Q10. |
| A-R3 | Descendent-count / version drift, **in both directions** (reworded A2 on A1's finding). Too few collections: a folder returned without all three never gets a descendent count, so the viewer never accepts its version and re-fetches it forever. Too many: a **partial** view padded with empty siblings gives the viewer a descendent count that is wrong but plausible, and it then banks a version for content it never received — the worse failure, because it is silent. | A **complete** view (`/children`) carries all three collections at every expanded level, even when empty. A **partial** view (`/categories`, `/links`, a subset) carries only the collection asked for, and no empty siblings. For a Current Outfit or Outfit folder `links` alone is a complete count by the viewer's own rule (`llaisapi.cpp:1466-1482`). |
| A-R4 | Links emitted as items (`AssetType.Link` inside `items`) would be parsed as items, not links, and the outfit machinery breaks | The backend splits links out of `InventoryCollection.Items` into `_embedded.links` (spec §1c, T5); golden fixtures pin the shape |
| A-R5 | Error bodies that carry `item_id`/`category_id` + `parent_id` are parsed as updates (spec §1f) | The 501 body is a flat map with `error_code`, `error_description`, `message` only; fixture `error.xml` pins it |
| A-R6 | **Listing a folder the viewer does not have in `_updated_category_versions` dereferences null in the viewer.** `doUpdate` does `LLViewerInventoryCategory *cat = gInventory.getCategory(id); ... if (cat->getVersion() != version)` with **no null check** (`llaisapi.cpp:1760-1762`). A server that reports a version for a folder the viewer has never fetched crashes it. | Only ever list folders the operation itself touched and the viewer necessarily knows — the parent of the object it just named, or the object itself. Never list a folder speculatively, and never list the whole path to the root. |
| A-R7 | **A cap can be advertised and still be unreachable.** AIS is the first capability in this tree whose URLs carry sub-paths (`<capurl>/category/{id}/children`). `BaseHttpServer` keeps exact-path and variable-path handlers in separate dictionaries and matches only the latter by prefix (`TryGetSimpleStreamHandler`, `BaseHttpServer.cs:1109-1123`), and `Caps.RegisterSimpleHandler` had no way to ask for the second — so every AIS request 404'd before the handler was entered while the seed response looked perfect. Cost: one live run, all inventory unusable for the test avatar. | Fixed A6: `varPath` threaded through `Caps`/`CapsHandlers` (add-only), `AISv3Module.VarPath = true`, and a wiring test that registers through the real `Caps` and fails if it is flipped back. The 114 handler tests all passed throughout — anything that binds a handler to a URL needs a test that exercises the **binding**. |
| A-R8 | ~~Nothing stops a second system folder existing.~~ **CORRECTED A9, 2026-09-04.** The seven accounts do not hold duplicate system folders: the version-1 folders are the **HG suitcase skeleton**, parented to `My Suitcase` (type 100) and created deliberately by `HGSuitcaseInventoryService.CreateSystemFolders` (`:172-186`). Sixteen system types show the pattern across the same seven accounts, which are exactly the accounts that have a suitcase; agents with more than one type-46 folder outside a suitcase number **zero** (`A9-SUITCASE-NOT-DUPLICATE.md`). | **A unique index on `(agentID, type)` must NOT be added** - it would reject the legitimate suitcase skeleton and break suitcase creation grid-wide. A candidate shape is `(agentID, parentFolderID, type)`; **not settled, not recommended yet** - it needs its own session, because the read-then-write race in `CreateUserInventory` is real but no observed duplicate is attributable to it. The `EnsureSystemFolder` narrowing (`68bfa60735`) stands as hardening, not as a fix for anything observed. |

## 4. Session log

| Date | Session | Commit | Result | Decisions/questions raised |
|---|---|---|---|---|
| 2026-09-03 | A0 | see DONE in the session report (spec commit, skeleton commit) | `AIS-V3-SPEC.md`: 16 operations, 7 meta keys + 5 `_embedded` collections extracted with viewer file:line; tree state T1–T6; `AISv3Module` skeleton (disabled by default, 501 on every route), `IAisInventoryBackend`, `AisRouter` with one NUnit test per URL shape, golden envelope fixtures, HTTP-level 501 test (27 tests, new project `Tests/OpenSim.Region.ClientStack.LindenCaps.AIS.Tests`; the existing caps test project is not in the solution and does not compile at HEAD); `Source/OpenSim.Services.AISv3` (webapi template, never in the solution) deleted | A-D1, A-D2 ruled; A-D3, A-D4 proposed; A-Q1..A-Q7; A-R1..A-R5 |
| 2026-09-04 | A1 | `03e428c124`, `9be9d2e58d`, `9d43eb2a9d` | The read surface: backend primitives + `AisInventory` composition, every fetch route with the envelopes the viewer parses, and `LibraryAPIv3` read-only alongside `InventoryAPIv3`. A-Q1 resolved (the readers are `LLInventoryItem::fromLLSD` / `LLInventoryCategory::fromLLSD`, tabulated with lines; the category id key is `category_id`, the folder type `type`/`type_default` as an integer), A-Q2 resolved (LLSD XML both ways, `application/llsd+xml` on both headers, COPY's destination as the `Destination` header). 52 tests. | A-R3 needed rewording; depth semantics unpinned; A0's fixtures corrected to the resolved field set |
| 2026-09-04 | A2 | `4d9a661f02`, `<mutations>`, `<harness>` | The delta contract extracted into spec §1d-bis with citations, then the four single-object mutations implemented against it: `PATCH /item`, `PATCH /category`, `DELETE /item`, `DELETE /category`. Confirmed A0's delta-key set against the source and that there is **no** updated-object delta key — an updated object is top-level content, and its folder must be listed in `_updated_category_versions` or the viewer discards the update outright. A-Q3 resolved for the update bodies. 68 tests, solution 0 errors. | **A-R6** opened (the viewer null-derefs on a version for a folder it lacks); **A-Q9** opened (the inventory service will not delete a folder outside Trash and returns true anyway); **A-Q8** opened for depth semantics; A-Q6 answered — yes, every mutation re-reads the folder after the write |
| 2026-09-04 | A2b | `d5afbce36d`, `<deletion>` | Two blockers cleared. **A-Q8 (depth)** settled from the source rather than a live capture: the viewer only ever sends `depth=50` or `depth=0`, parses each `_embedded` level one depth lower and versions a category only while that depth is >= 0 and its descendent count is known, and re-queues every descendant regardless — so `depth=N` licenses exactly N generations below the requested folder, which is what `AisInventory.Walk` already did. UNVERIFIED marker removed; a clamp to the viewer's own ceiling of 50 added. **A-Q9 (folder deletion)** resolved by adding the three-argument `DeleteFolders` overload to `IInventoryService` and implementing it everywhere, including a backward-compatible `ONLYIFTRASH` field on the Robust wire; `DELETE /category` now works outside Trash and refuses protected folders with 403. 72 tests, solution 0 errors. | Protected-folder membership is UNVERIFIED (`llfoldertype.cpp` is not a permitted read); our rule is the root or any system type except `FolderType.Outfit` |
| 2026-09-04 | A3 | `2bf8ed89f4`, `193d7e6fc7`, `<harness>` | Protected-folder set replaced by the viewer's own table (`llfoldertype.cpp:85-127`, allow-list with a protected default, since `lookupIsProtectedType` returns true for unknown types at `:154-162`) — strictly more permissive than A2b's guess. **SlamFolder** implemented: the body is a bare LLSD **array**, not the `{links:[...]}` A0 guessed, confirmed against both builders (`llappearancemgr.cpp:2209-2245`, `:1795-1833`); create-then-remove ordering so the folder never holds fewer links than it started with; rollback on a failed creation; all three failure modes fault-injected. **CreateInventory** implemented at the viewer's real route `POST /category/{parentId}` (`llaisapi.cpp:115`), not `/children`. 98 tests, solution 0 errors. | A-R2 addressed but not eliminated; **A-Q10** opened for the missing transactional write; `items`/`links` create arrays still UNVERIFIED (no caller in any permitted file) |
| 2026-09-04 | A4 | `20ab17ffb7`, `d18897eb63`, `ac5a43ed69`, `<harness>` | The last two operations, and the create shapes A3 guessed. **Create:** `links` verified as A3 had it; `items` verified and now **refused with 501** rather than creating an item with no asset (its builder is compiled out behind `USE_AIS_FOR_NC` and expects the server to mint the asset); `categories` still UNVERIFIED. **Purge:** the deltas must be **enumerated** — the opposite of a folder delete — because `onDescendentsPurgedFromServer` is reached only from `onObjectDeletedFromServer` (`llinventorymodel.cpp:2023`) and never from the AIS path; composed from `PurgeFolder` plus A2b's `DeleteFolders(onlyIfTrash:false)`, with no rollback possible and survivors named in the error. **Copy:** the tree's library permission rule reused field for field (`Scene.Inventory.cs:1053-1064`), the `,depth=0` tid quirk handled, the library handler now carrying the agent's inventory as its destination. Every spec operation is now implemented; the only 501 left is COPY arriving on the inventory cap. 112 tests, solution 0 errors. | A-Q3 and A-Q4 closed as far as the permitted files allow; the protected rule is deliberately not applied to slam, purge or a copy destination — three separate instances of the same reconciliation |
| 2026-09-04 | A5 | `6fc4ab3426`, `4d725a7b2d`, `3448d6e380` | A-Q3 closed: `asAISCreateCatLLSD` (`llinventory.cpp:1256-1276`) is a **base-class** method, which is why A4 could not find it; its key set matches what A4 already accepted, and the server now honours the body's `parent_id`. The advertisement flag was **grid-wide**, which A-R1 does not permit — now resolved per region via a `[<Region Name>] AIS_Enabled` override, with an INFO line per region naming the caps it advertises. `OpenSimDefaults.ini`'s `[AIS]` block rewritten. `A5-LIVE-CHECKLIST.md`: 15 steps in 5 phases. Region deployed; flag left false. | Robust redeploy needed for folder deletion outside Trash; the Robust deploy was later attempted and **stopped** — see A6 notes |
| 2026-09-04 | A6 | `fa4eb5f14a`, `ee6d2a3554` | **First live run failed.** Ebony advertised both caps, Firestorm switched all inventory traffic to AIS, and every request 404'd inside the HTTP server: sub-path caps must be registered as variable-path handlers and `Caps.RegisterSimpleHandler` could not express it. Diagnosis written before the fix (`A6-LIVE-FAILURE.md`), including the side-by-side against `FetchInventory2Module` that isolates the single differing row. Fixed by threading `varPath` through `Caps`/`CapsHandlers` and registering AIS with it, plus DEBUG logging at registration and at handler entry — their absence is why this needed a code read rather than a log grep. 7 wiring tests, 121 total; the new test was verified to fail with the bug reintroduced. | **A-R7** opened. **Verified live 2026-09-04**: with the fix deployed the caps served real traffic and the checklist reached step 15 — see `A5-RUN-2026-09-04.md`. |
| 2026-09-04 | A7 | `6cd13a3645`, `68bfa60735` | **Second live failure: checklist step 10 did not persist.** Truly Bazar has two type-46 folders (`71c3c184…` v457, the one the viewer uses; `52c327c4…` v1, the one we returned) and seven grid accounts have the same shape. `XInventoryService.GetSystemFolderForType` picks `folders[0]` (`:272-294`) from a query with no `ORDER BY` and no `LIMIT` (`MySQLGenericTableHandler.cs:154-185`), so the take-off was written to a folder no viewer reads. **Predates AIS**: every `GetFolderForType` caller inherits it, but the legacy path never asks the server which COF is real — it reads the skeleton, which returns both — so AIS made an existing data fault visible rather than causing it. Resolution now scans the skeleton by type and takes the highest `Version`, ties on lowest id, with a WARN naming every candidate. Prevention narrows the creation race. 131 tests; the three new resolution tests were verified to fail with the fix reverted. | **A-R8** and **A-Q13** opened. Deployed as `e800eb61b6`; **step 10 re-run passed** (`A5-RUN-2026-09-04.md`). Dedupe of the seven accounts proposed but **not built** — John's call, with a backup; until then the A7 WARN fires on every COF resolution for those accounts. |
| 2026-09-04 | A8 | `4165077a5f` | **First full checklist run recorded** (`A5-RUN-2026-09-04.md`), on Ebony at `1.1.191-alpha+e800eb61b6`. 12 pass, step 10 failed then passed after A7, steps 7/14/15 not run. Step 8 (Empty Trash) passed, confirming the purge enumeration against real nested content. Step 13 is the result that matters: AIS answers 501 for notecard creation and **the viewer falls back to the legacy UDP path**, so the item is created and the resident sees nothing wrong — the checklist's "single biggest argument against flipping this flag more widely" is withdrawn, confirmed server-side by the region log rather than inferred from the client — but **A-Q15** keeps open whether that fallback is Firestorm's own or in the shared LL path, and that is what gates widening. **New standing constraint: the stock LL viewer cannot run against this grid** (Vivox refuses to initialise outside SL and the viewer will not start), so every live checklist is a Firestorm result with no control run. P-1 is unchanged — the source stays the authority — but a green checklist now means "Firestorm is satisfied", not "the protocol is right". | **A-Q14** opened and closed; **A-Q15** opened. Dedupe and the Robust redeploy still outstanding |
| 2026-09-04 | A9 | `3fb35f63b5` | **The dedupe was not run: there was nothing to deduplicate.** Stopped at the plan step with the database untouched (`SELECT`s and one `mysqldump` only). The survey showed the two type-46 folders in each pair have **different parents** - keeper under `My Inventory`, version-1 folder under `My Suitcase` - refuting the parentage inference `A7-DUPLICATE-COF.md` rested on. Confirmed three ways: the 7 affected accounts are exactly the 7 with a suitcase; sixteen system types show the same pattern; genuine duplicates outside a suitcase number zero. Executing the plan would have permanently deleted part of the suitcase skeleton for seven accounts. Backup taken first: `legiongrid-predupe-20260904-1332.sql`, 2,685,971,589 bytes - **and the first attempt at that dump, piped through PowerShell `Set-Content`, was corrupt (BOM + U+FFFD replacement of binary data); assume any PowerShell-piped `.sql` in `_backup` is unrestorable.** | **A-R8** corrected, **A-Q16** opened. `A7-DUPLICATE-COF.md` corrected in place; step 10 re-diagnosed in A10 |
| 2026-09-04 | A10 | `dd90969f6e` | **A7 corrected on the record, and step 10 reopened.** Part 1 rewrote `A7-DUPLICATE-COF.md` around A9's finding, keeping the superseded reasoning visible. Part 2 re-diagnosed step 10 from the region log and the live database and **failed to establish a cause**, so no fix was written. Three findings: take-off is `DELETE /item` (RemoveItem), **not** `SlamFolder`, so the create-then-remove machinery was never on the failing path; COF resolution addressed the correct folder on both sides of the fix (the new WARN at 13:11:38 prints it); and the passing re-run is **not a clean repetition** - a full slam at 13:13:53 rewrote every link in COF before the relog, which the creation timestamps in the database confirm. Slam ordering, `IsLink` (types 24 and 25), `RemoveItem`'s deltas, the folder-version bump on item delete, and the `FetchCOF` envelope were all examined and are sound. | **A-Q17** opened. The A5 run record's step 10 row corrected; `6cd13a3645` keeps its own merits but **must not be described as fixing step 10** |
| 2026-09-04 | A11 | `f1d32d6ba6`, `25ce8b0ad4` | **A-Q15 closed from the source, and the record corrected.** In `create_inventory_item` the whole AIS arm is inside `#ifdef USE_AIS_FOR_NC` (`llviewerinventory.cpp:1120`-`:1166`) and the macro is not defined, so it never compiles and control falls unconditionally to the legacy UDP send at `:1169`. There is no runtime fallback to hold or fail, and the code is LL's, so stock viewers behave identically. **Also corrected my own claim** that "AIS answered 501": the log shows no `CreateInventory` at the notecard creation at all, only a `FetchItem` syncing the result - the 501 route was never reached. Then added DEBUG logging of every mutation's response status and delta keys, and the folder `FetchCOF` resolves `current` to, walked from a shared `DeltaKeys` list so the log cannot drift from what is emitted. 9 tests, 140 total. | Deployed `25ce8b0ad4`. The logging is what let A12 settle **A-Q17** |
| 2026-09-04 | A12 | `b18bc40dd4` (design only) | **The step-10 cause, found by contradicting the brief's own premise.** The brief said nothing detached the object; the object *was* detached - the viewer reconciles attachments itself in `updateAppearanceFromCOF` (`llappearancemgr.cpp:2656`) and attachpoint 18 is absent from the logout save batch that covers 11/40/7/8. What was lost is the **deferred appearance save**: `QueueAppearanceSave` defers by `m_savetime` (5 s) and `SaveAppearance` drops the write if the presence has gone, and `DeRezAttachments` never flushes it. Predates AIS; symmetric on wear. Recommended flushing on presence close, plus login-time reconciliation for the holes the viewer cannot cover. | **A-Q17** answered. Fix built and deployed as T-2; §4/§5 holes remain open. **B3 gains a second case (S8):** an unresolvable wearable now *keeps* its slot, but the asset id it keeps may be meaningless on this grid - a cross-grid item leaves the slot **populated but unrenderable** rather than missing. Strictly better than losing the record, not the same as correct, and nothing reconciles it. This is the same shape as the attachment holes B3 was recommended for and would be covered by the same login-time COF-vs-appearance diff |
| 2026-09-04 | T-2 | `dc4e417bb3`, merged `bfb50070d8` | **The appearance-save flush, and its deploy.** Flushes a pending save on `EventManager.OnRemovePresence` (`Scene.cs:3866`, presence resolvable until `:3898`), skipped for child agents so a teleport cannot publish a stale outfit over a newer one; WARN on both drop paths; 6 tests, 4 of which fail without it. Also added the `DisableTestParallelization` declaration `OpenSim.Region.CoreModules.Tests` was missing - that project failed **9 tests but not the same 9** per run, inside `SceneHelpers.SetupScene` rather than in assertions; now deterministic at 5 failed / 84 passed. Deployed as a clean merge of `fix/appearance-save-flush` into `feature/ais-v3` (merge-base `11a2456833`, zero conflicts, no file overlap); backup `regionserver-20260904-1703`. | The live region runs `bfb50070d8`, reachable **only** from `integration/ais-appearance` - reconcile before the next deploy. The 5 residual failures are **T-3** |
| 2026-09-04 | A13 | `6d5475de4d` | **Step 10 closed and the record corrected.** Clean run on `1.1.202-alpha+bfb50070d8`, no slam, ~20 s gap: the garment stayed off. The **absent** flush line at logout is the fix's cost guarantee (a close with nothing queued writes nothing) - the timer had drained the queue 15 s earlier; the flush covers logout *inside* the 5 s window, which is what happened at 14:09:40 and lost the dress. A5 run record: step 10 **pass**, tally **12 pass / 3 not run** (revised by A15 to 12 pass / 1 not reachable / 2 not run). Correction banners added to A10 and A12. Checklist step 10 now requires ~10 s in-world before relogging, or it races the save timer instead of testing AIS. | **A-Q17** closed. Open items carried in `A13-STEP10-CLOSED.md` §4, including an **unaudited `_backup\` folder**: any `.sql` written through a PowerShell text pipeline is corrupt |
| 2026-09-04 | A14 | `979ce75c66` | **Branches reconciled and the backup folder audited.** `fix/appearance-save-flush` merged into `feature/ais-v3` (clean, docs-only difference from the deployed tree; **build inputs identical**, so the live binaries are reproducible from this branch). `integration/ais-appearance` deleted and `D:	ranq-integration` removed - but the deployed merge `bfb50070d8` is **not** an ancestor of the new HEAD, so it was tagged `deployed/region-2026-09-04` first to keep the binaries' `+bfb50070d8` stamp resolvable. Backup audit: both `.sql` dumps good (no BOM, correct size, terminating line); all 7 directory backups intact - 1,070 binaries checked for a PE header, zero failures, and 4 probe hashes match what was recorded at deploy time. | **No database backup exists before 2026-09-04** - the one real gap. `BACKUP-AUDIT-2026-09-04.md` |
| 2026-09-04 | R1 | `108c05553a`, merge `a2c8fb63f3` | **Robust reconciled and redeployed (Road A).** Recon found the live Robust was 45 assemblies from four commits with **seven** hand-copied strays, not four - `0ac27e3f2d` also covered `OpenSim.Data`, `OpenSim.Data.MySQL` and `OpenSim.Data.SQLite`. No unreachable commits. The merge with `integration/legiongrid-trusted-hg` touched one shared file, `Tranquillity.sln`, resolved by keeping both sides - and repairing a pre-existing unclosed `Project(` that MSBuild had tolerated. Trusted-hypergrid proven preserved: `TrustedHypergridRuntime.cs` and `ExternalIPResolver.cs` byte-identical to their own branch. Solution 0 errors; AIS 140/140, appearance 6/6, TrustedHypergrid 25 passed + 3 skipped, HypergridService 25/25. Published RID-less and deployed: **45 assemblies now on one commit** `1.1.208-alpha+a2c8fb63f3`, none on any old commit, with ONLYIFTRASH, EnsureSystemFolder and trusted-hypergrid all probed in the **deployed** binaries. | **First gridserver backup ever taken**: `gridserver-20260904-1853` (765 files, 79 binaries MZ-clean) and `legiongrid-prerobust-20260904-1853.sql` (2.69 GB, no BOM). Checklist step 7 is now testable. Upstream v1.0 sync (Road B) deliberately not done |
| 2026-09-04 | A15 | `2697af65b0` | **Step 7 closed as NOT REACHABLE, and the Robust deploy confirmed live.** A resident cannot cause `DELETE /category` on a folder outside Trash: the viewer's only folder-removal routes are move-to-Trash (a `PATCH` changing `parent_id`), purge a single item in Trash, and Empty Trash - there is no shift-delete for folders. Verified in-world: two folders deleted, one nested and one at root, **both moved to Trash** and still there after a restart; the day's AIS log shows `CreateInventory` and `UpdateCategory` and **no `RemoveCategory`**. A2b's `ONLYIFTRASH` stays correct and is now live - the spec requires the route (`llaisapi.h`) and it must be honest when anything calls it - but it was **never gating a resident-visible operation**; the folder removal residents actually perform is Empty Trash, which is step 8 and passed before the Robust deploy. Robust restarted cleanly at 19:03:58 on `1.1.208-alpha+a2c8fb63f3`: trusted-hypergrid loaded its grid identity from `TrustedHypergridSecret.ini` (fingerprint `637ee209...`), the external IP resolver resolved `legiongrid.ddns.net`, Direct Delivery enabled, all connectors loaded, **zero load failures**. | Final tally **12 pass, 1 not reachable (7), 2 not run (14, 15)**. Corrected every place that called step 7 the payoff of the Robust deploy - the payoff was the single-commit inventory and the first rollback point. The seven-stray correction has been carried into John's own notes |
| 2026-09-04 | A16 / SSB S2 | `f4b18601d3` (merge), `<docs>` | **`feature/ssb-appearance` merged in; region redeployed with all three lanes for the first time.** merge-base `11a2456833`; 107 files changed on this side since then, 111 on the SSB side, and exactly two touched by both — `OpenSimDefaults.ini` (auto-merged additively: the `[Appearance]` block gained `ServerSideBaking = false`, `BakeSize = 1024`, `BakeQuality = 0.85`) and `Tranquillity.sln` (conflicted, resolved keep-both). **No `.cs` file conflicted** — the lanes were genuinely disjoint in code. Solution re-verified after the resolve: 101 `Project(` / 101 `EndProject`, 96 projects + 5 folders, `dotnet sln list | sort | uniq -d` empty, every project has config rows, no dangling `NestedProjects`, so the malformation an earlier merge repaired has not returned. Build 0 errors. Tests: AIS 140, appearance flush 6, TrustedHypergrid 25 (+3 skipped), HypergridService 25, SSB library 35, ServerSideBaking 17. Deploy: 146 of 843 published files differed by content plus 1 new (`DnsClient.dll`); 147 copied, `config-include/`, `Regions/`, `config/`, `ScriptEngines/`, `assetcache/`, `maptiles/`, `MeshCache/`, `j2kDecodeCache/`, `appsettings.json` and all 11 `*.example` preserved, and the live-only `WebRtcVoiceServiceModule.dll` untouched (same SHA-256 before and after). `AIS_Enabled = true` in `config/OpenSim.ini` untouched; `ServerSideBaking` stays `false`. Backup: `D:\legiongrid\_backup\regionserver-20260904-2059` (5008 files). Verified in the deployed binaries: `InventoryAPIv3`, `LibraryAPIv3` and the mutation-delta line `[AIS]: {Operation} -> {Status} {Deltas}`; the flush line `[AVFACTORY]: flushing pending appearance save for {AgentId} on close`; `appearance serverbake <first-name> <last-name>` and the new per-phase timing strings. | A-Q18 opened (this branch's name is not honest; do not rename yet) |
| 2026-09-04 | A17 / SSB S3 | `7554bf9b51` | **The AIS/SSB seam is now code, and the two lanes' COF version is one number — proven, not assumed.** SSB's `UpdateAvatarAppearance` cap reads the Current Outfit folder's `Version` through `IInventoryService`, which is the same field `AisMutation.ReportVersion` publishes as `_updated_category_versions` and `AisEnvelope.Category` as `version`; a test runs one fake inventory service through both sides and asserts the ints are equal, at 0, 1, 7, 4242 and 65535. **A-Q13 re-surfaces there**: `InventoryFolderBase.Version` is a `ushort`, so past 65535 both sides truncate alike — they keep agreeing with each other and both stop agreeing with the database, and a wrapped version makes an older outfit compare equal to a newer one. That is now a correctness question for the appearance handshake, not only an inventory one. Nothing in AIS changed; the region was redeployed at `1.1.216-alpha+7554bf9b51` with `ServerSideBaking` false everywhere and `AIS_Enabled = true` untouched. | A-Q13 promoted: it now affects the SSB cap handshake as well as inventory |
| 2026-09-05 | A18 — `UpdateItem` loses `asset_id` | `1a7dce26ca` (red), `159fd41d39` (fix), `<docs>` | **`PATCH /item` answered 200 and wrote nothing.** `AisMutation.ApplyToItem` accepted only `name`/`desc`/`flags`/`sale_info`, so a wearable save left `Applied.Any` false, `AisHandler` never called `backend.UpdateItem`, and the data layer that bumps the parent version on an item store (`MySQLXInventoryData.cs:238-246`) was never reached. Hence 200, the **old** asset still in the row, and the **same** folder version twice in `_updated_category_versions`. Live on 2026-09-05, agent `a7d2ff2e` on Ebony: asset `44f77403` uploaded at 16:27:24,899, PATCH of item `e2c03d62` answered 200 at :24,958 with `{a2aef7dc:9}`, row still `637022fb`; a second save at 16:28:36 returned version **9 again**; the legacy UDP save on Transylvania at 16:32:23 did persist and took the folder to 11. **Recon found the real field.** A wearable save does not send `asset_id` at all: `LLViewerInventoryItem::updateServer` erases `asset_id` **and** `shadow_id` from the body and puts `hash_id` — the xfer transaction id — in their place (`llviewerinventory.cpp:445-452`), and `update_inventory_item` does the same (`:1414-1421`). A2 recorded this in §1d and then never implemented it. Only the region's asset-transaction module knows which asset a transaction produced, so `IAisInventoryBackend` gained `ApplyAssetTransaction` and the region backend hands the pair to `IAgentAssetTransactions.HandleItemUpdateFromTransaction` — the legacy route's own call (`Scene.Inventory.cs:579-582`). The `Scene` lookups live in `AISv3Module`, not the backend, so **P-2 holds** and Phase 2 hosts the backend on Robust unchanged (no resolver there; a PATCH falls back to `asset_id`). **Three fields added, and only ones the viewer sends:** `asset_id`, `hash_id`, and `permissions` (next/everyone/group, each masked by the item's own base; base and current never taken from a body — the tree's existing rule at `Scene.Inventory.cs:497-548`, so both routes now agree). `shadow_id` deliberately not added: both body builders erase it. One simplification, commented in the code: the export bit is preserved as it stands rather than reproducing the legacy creator check. Ordering matches the legacy route — map fields stored first, transaction handed over second, item re-read before the envelope. **Siblings checked, none affected:** `CreateInventory`'s `items` is refused 501 before any write, its `links` and `SlamFolder` build from `linked_id` (`AisSlam.cs:117`), `CopyInventory` carries the source's asset (`AisCopy.cs:96`). Spec gained **§1d-ter**, the verified `UpdateItem` field set with the server's rule for each key; the op table row for op 8 is no longer UNVERIFIED. Tests **140 → 148**, solution 0 errors. | **The checklist had no asset-saving step** — steps 1-15 all passed while this was live, because a rename exercises the same route and does persist. **Step 16 added** (Phase 6): edit a wearable, change a colour, save, re-open — the colour persists. Found during Q-11 preparation, not by the checklist. Deployed the same day; not yet verified live |
| 2026-09-05 | A18 deploy | `cb27bc93ee` (deployed) | **Both roots, servers down throughout and not started.** Region `1.1.226-alpha+b13f15add3` -> **`1.1.233-alpha+cb27bc93ee`** (144 of 843 published files differed by content, **0 new**); grid `1.1.226-alpha+b13f15add3` -> the same stamp (90 of 688, **0 new**). Every copied file hash-verified against the publish after the copy: 144 and 90, none mismatched. **The region set is one assembly and 143 re-stamps**: `OpenSim.Region.ClientStack.LindenCaps.dll` is the only one whose IL changed, the rest differ solely because nbgv re-stamps every assembly per commit. `LindenCaps` is not in the grid publish at all, so the **grid deploy is a version re-stamp and nothing more** — done anyway to stop the two roots drifting, as in S4. **Held back by the preserve rule and proven byte-identical before and after:** 6 region files (`config-include/Grid.ini`, `GridHypergrid.ini`, `Standalone.ini`, `StandaloneHypergrid.ini`, `GridCommon.ini.example`, `StandaloneCommon.ini.example`) and 2 grid (`Robust.ini.example`, `Robust.HG.ini.example`), plus the named guards — live-only `WebRtcVoiceServiceModule.dll` (still `1.1.160-alpha+d95754509d`, absent from the publish), `config/OpenSim.ini`, `OpenSimDefaults.ini`, `Robust.ini.example.net10`, `Robust.HG.ini.example.net10`, `TrustedHypergridSecret.ini`, `config/Robust.ini`. File counts unchanged: region 5262, grid 767. Backups `D:\legiongrid\_backup\regionserver-20260905-1741` (5262) and `gridserver-20260905-1741` (767). **Verified in the deployed binary**, not just the publish: `LindenCaps.dll` carries `ApplyAssetTransaction` and `AssetTransactionResolver` as metadata names and the literals `hash_id`, `asset_id`, `permissions`, `next_owner_mask`, `everyone_mask`, `group_mask`. The only assemblies not at the new stamp are the six NuGet ones (filtered by `CompanyName`) and the live-only voice module. | **Live config on the region has `AIS_Enabled = true` AND `ServerSideBaking = true`** (`config/OpenSim.ini:163-164`); neither was touched by this deploy, so Ebony comes back with both lanes on. Checklist step 16 is ready to run |
| 2026-09-05 | S8 - appearance integrity | `483c2d7a13` | **Two save-path defects deleted four wearable slots from the stored record, and a third made the loss unrecoverable.** A stock viewer with a May-2 inventory cache from the previous grid named four item ids that exist nowhere in `inventoryitems`. **(1)** `SetAppearanceAssets` logged *"Can't find inventory item ... setting to default"* and then **removed** the wearable (`AvatarFactoryModule.cs:940-947`) - the message was never true. `SaveAppearance` persists on the next line and `AvatarService.SetAvatar` deletes every row before rewriting (`AvatarService.cs:93`), so slots 1-4 (Skin, Hair, Eyes, Shirt) left the record entirely while her COF `71c3c184` still linked valid items. Now the slot **keeps what it has** and a WARN names agent, slot and item. **This is A-R4/S0c's failure from the other side**: S0c fixed slots the viewer did not list, this fixes a slot the viewer listed whose item will not resolve - see the repo-audit note. **(2)** Neither `QueueAppearanceSave` nor `SaveAppearance` checked `IsChildAgent`; only `FlushAppearanceSaveOnClose` did (S0b). The timer drain is the path that fired. Both now return early, the barrier sitting at `SaveAppearance` so it holds whatever queued. **Both defects are inherited from upstream unchanged** - OpenSim-NGC develop `a68d59f232` has the same removal at `AvatarFactoryModule.cs:871-878` and the same unguarded `SaveAppearance` at `:811-828`. Tests: 4 new, **all three fixes verified RED first by reverting the source**. The two unresolvable-item tests need `CreateUserWithInventory`, because `SetAppearanceAssets` skips its whole loop for an agent with no inventory root (`:911`) and would otherwise pass against the broken code - which is precisely why the existing suite never caught this. | **Attribution settled 2026-09-05, after the row above was written:** the AVFACTORY warnings carry **no region tag** - `SetAppearanceAssets` logs agent, slot and item and nothing else - so both the brief's "Transylvania child" and this session's "Transylvania, just turned root" were inferences the log does not support. The timing settles it: Truly logged in at **18:06:40 on Ebony**, `DelayBeforeAppearanceSave` is 5 s, the login queues a save through the baked-texture cache check (`ScenePresence.cs:2291-2294`), and it drained at ~:46. The warnings are **Ebony's root presence**. **Fix #1 is therefore the cause and fix #2 is defence in depth** - the child guard closes a real, unguarded hole that was not the path taken here, and without fix #1 the loss recurs on a root presence where no child guard can reach it. See `Docs/feature/repo-audit/S8-WEARABLE-RESOLUTION.md`. **Deployed 2026-09-05 with PHYS-1** (`68af310cbd`), both roots `1.1.226-alpha+b13f15add3` -> **`1.1.241-alpha+68af310cbd`**; not yet verified live. Note the deploy does **not** repair Truly's record - slots 1-4 are already gone from `Avatars` and nothing reconciles a damaged record (A12 B3) |
| 2026-09-05 | S9 + PHYS-2 deploy | `05e915df4f` (deployed) | **Both roots, servers down throughout and not started.** `1.1.241-alpha+68af310cbd` -> **`1.1.246-alpha+05e915df4f`**. Region `-c Release -r win-x64 --self-contained false` (843 published, **144** differed by content, 0 new); grid `-c Release --self-contained false`, **no `-r`** (688 published, **90** differed, 0 new). Every copied file re-hashed **in the live root** against the hash recorded before the copy: 144 and 90, no mismatch. **What shipped:** S9 (`f9dee4592b`, editing a worn wearable rebakes that channel - `LindenCaps.dll`) and the PHYS-2 allocator owner check (`b70e77a895`, `Legion.Physics.dll`). **PHYS-2 is not fixed and this deploy does not claim to fix it**; the owner check is `AllocatorOwnerCheck`-gated, DEBUG-on and **inert in Release**, so nothing about the shipped simulator's behaviour changes from it. The eleventh-crossing crash can still happen. **Preserved:** `config`, `ScriptEngines`, `assetcache`, `maptiles`, `MeshCache`, `j2kDecodeCache` are not in the publish at all; `config-include` (6 files) and `Regions` are in it and held by the preserve rule; `config/OpenSim.ini`, `OpenSimDefaults.ini`, `TrustedHypergridSecret.ini`, `config/Robust.ini`, `Robust*.ini.example.net10` and the two grid `*.example` untouched. Live-only `WebRtcVoiceServiceModule.dll` still `1.1.160-alpha+d95754509d` (`9D7A1E605927BBF1...`), absent from the publish. **`joltc.dll` `16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5` before and after, and never in the copy set** - the patched per-system-allocator build is intact, which matters more than usual while PHYS-2 is open. File counts unchanged: region 5353, grid 767. Backups `D:\legiongrid\_backup\regionserver-20260905-2138` (5353) and `D:\legiongrid\_backup\gridserver-20260905-2138` (767). | **Note on the publish flags:** the deploy brief said the region gets a *self-contained* publish; the recorded procedure it also said to follow exactly uses `--self-contained false` with `-r win-x64`. The recorded flags were used - a true self-contained publish would add the runtime to the region root and the 843-file count would not match every prior deploy. The region/grid asymmetry that matters is the **RID**, and it is as specified |
| 2026-09-06 | S10 + PHYS-4b deploy | `77050c5372` (deployed) | **Both roots, servers down throughout and not started.** `1.1.246-alpha+05e915df4f` -> **`1.1.258-alpha+77050c5372`**. Region `-c Release -r win-x64 --self-contained false` (**842** published, **150** differed by content, **0** new, **144** copied after the preserve rule); grid `-c Release --self-contained false`, **no `-r`** (**688** published, **92** differed, **0** new, **90** copied). Every file of the **full** copy set re-hashed **in the live root** against the staged manifest: 144 and 90, **no mismatch**. **What shipped:** S10 (`73d25e3175`, every COF link of a type reaches the bake in the viewer's `@n` order - `OptionalModules.dll`, `Services.Interfaces.dll`) and the PHYS-4b set (`3178004076` the joltc publish fix, `d3f136e0b9` the guard fix, plus the PHYS-2b..4b analysis commits, which are docs). **The region publish is 842 where every prior one was 843**: the `3178004076` csproj fix stops the stock native reaching the output, and with it the arm64 natives and `joltc_double.dll` leave the publish. **The joltc gate, run for the first time and passed.** Staged publish: exactly two `joltc*` files, both **`16AF7638...43B5`** (ROOT and `runtimes\win-x64\native`), and **all 842 staged files hashed against the stock build - none is `67BECFC7...0910`**, so the stock native is not in the output at all. `assert-patched-joltc.ps1` exit 0 against the staged publish and against the live region root, before and after the copy; live root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`** throughout, and it was never in the copy set because it was already identical. **Stock leftovers removed from the live root** so it matches the publish exactly: `joltc_double.dll` (`8C68788D`), `runtimes\win-x64\native\joltc_double.dll` (`8C68788D`) and the whole `runtimes\win-arm64\` (5 files: `joltc.dll` `B0A7D051`, `joltc_double.dll` `A57A9A14`, `e_sqlite3.dll`, `libSkiaSharp.dll`, `libSkiaSharp.pdb`) moved to **`D:\legiongrid\_backup\joltc-stock-20260906\`**, beside the stock pair already kept there (`joltc.dll` `67BECFC7`). None was ever loadable here - the patch builds no double-precision variant and arm64 cannot load in this process - but they were the only files left in the root that did not come from the publish. The two `joltc_double.dll` copies are byte-identical and landed at the same name in the backup, so one copy of `8C68788D` is kept, not two. The guard re-run after the removal reports **2 joltc.dll checked, both OK, no notes**. **Preserved** and proven byte-identical against the pre-deploy backup: 6 region `config-include` files (`Grid.ini`, `GridHypergrid.ini`, `Standalone.ini`, `StandaloneHypergrid.ini`, `GridCommon.ini.example`, `StandaloneCommon.ini.example`), `Regions/` (in the publish and already identical), 2 grid `*.example`, plus `config/OpenSim.ini`, `config/Robust.ini`, both `Robust*.ini.example.net10`, `TrustedHypergridSecret.ini`, and live-only `WebRtcVoiceServiceModule.dll` still `1.1.160-alpha+d95754509d` (`9D7A1E605927BBF1...`), absent from the publish. `config`, `ScriptEngines`, `assetcache`, `assets`, `maptiles`, `MeshCache`, `j2kDecodeCache` are not in the publish at all. File counts: region **2351 -> 2344** (the 7 files moved out above), grid **768** unchanged. Backups `D:\legiongrid\_backup\regionserver-20260906-1357` (2351) and `D:\legiongrid\_backup\gridserver-20260906-1357` (768), both verified file-for-file against their source before the copy. | **The region copy hit its STOP condition on the first pass and was resumed, not forced.** 14 assemblies would not copy - *"being used by another process"* - and the post-copy re-hash reported 14 mismatches, so the run stopped with the region root **mixed**: 130 files at the new build, 14 still at `1.1.246`. No process was running out of `D:\legiongrid`; the holders were **three idle `dotnet-dump analyze` sessions on `OpenSim.Server.RegionServer.exe.43868.dmp`** (PIDs 41780 / 11004 / 43420, started 06:02-06:17), left at the interactive prompt from the PHYS-4b dump work. `dotnet-dump` maps the region's assemblies off disk for metadata, so it holds them locked without appearing as a loaded module of any process. **John killed the three readers**, and the copy was resumed and then re-verified over the **full** copy set - all 144 region and all 90 grid files re-hashed in the live root, not merely the 14 - which is why the numbers above are for the whole set. **Worth carrying forward:** a `dotnet-dump analyze` session on a region dump silently blocks a region deploy, and neither `Get-Process -Path` nor a loaded-module scan finds it. Check for `dotnet-dump` before the copy, or the deploy stops half-applied. **Not verified live:** nothing has been started. The first thing to run is checklist **step 18** (add a second shirt on Ebony -> one `CofChanged` bake with `Upper=Baked`), then **step 17**. PHYS-4b's own claim - that the region now loads the patched native - is only provable by a run, and this deploy is the first one whose output can support it. |
| 2026-09-06 | A19 - a refused save answered 200, and the refusal was wrong | `<this commit>` | **Two defects behind one log line.** 09:52:55 on Ebony: `ASSET XFER` upload, AIS `PATCH /item/<item>`, `[ASSET UPLOADER]: REJECTED update with texture 00000000-0000-2222-3333-100000001002 ... because they do not own the texture`, then `UpdateItem -> 200` with the item's `assetID` unchanged. The viewer was told the save succeeded. **(1) The refusal could not travel.** `AssetXferUploader.CompleteItemUpdate` already returned a bool (`AssetXferUploader.cs:409-437`) and every caller above it threw it away: `AssetXferUploader.RequestUpdateInventoryItem` discarded it (`:358`), and `AgentAssetsTransactions.RequestUpdateInventoryItem` (`:188`), `AssetTransactionModule.HandleItemUpdateFromTransaction` (`:184`) and the interface itself (`IAgentAssetTransactions.cs:36`) were all **`void`**. So `AISv3Module`'s resolver returned an unconditional `true` (`:183-184`) and `AisHandler.UpdateItem` discarded even that (`:355`). **(2) The refusal was wrong.** `ValidateAssets` asks `GetAssetPermissions(ourClient.AgentId, tx)` for every texture a clothing/bodypart asset names and requires Modify|Transfer|Copy (`AssetXferUploader.cs:566-572`). A **library** texture is in nobody's personal inventory, so that returns 0 and the whole save is refused - yet SL allows library textures in wearables, the library is readable by everyone by construction, and this grid ships one. The built-in system ids were already exempt via the `defaultIDs` list (`AssetXferUploader.cs:40-54`); a library asset was not. **What the viewer does with a non-200** (`llaisapi.cpp:851-951`): it logs `Inventory error: <status>` and still runs `onUpdateReceived`. It does **not** revert the local edit and does **not** notify - so the value of the correct status is that the error body carries no `_updated_category_versions`, the folder version the viewer holds does not advance, and its next fetch of that folder still sees the true state. The user-facing half already existed on the UDP side and fires either way: `CompleteItemUpdate` sends an alert and a `SendBulkUpdateInventory` to invalidate the viewer's cached change (`AssetXferUploader.cs:418-421`). **403**, not 500: the refusal is always a permission verdict, and the viewer has no special handling for this command that a 403 would misfire. **Shipped:** the chain now carries `AisAssetTransaction` - `Applied` / `NotResolvable` / `Refused` - because a bool could not tell *refused* from *nothing to apply*, and conflating them is exactly how the 200 happened; a region with no transaction module, no client, or the library backend all answer `NotResolvable` and still get 200, as before. And `ValidateAssets` now asks the **library owner** for a texture the resident does not hold, reading the owner off `ILibraryService.LibraryRootFolder.Owner` rather than hardcoding it (the rule `AISv3Module.LibraryOwnerOf` already uses); a region with no library service behaves exactly as it did. **Tests:** 3 new AIS cap tests - a refused transaction answers 403, does not bump the category version, and does not fire S9's rebake hook; the two status ones **verified red first** by disabling the check (2 of 3 failed, and the S9 one passed both before and after, which is the point of it). 4 new CoreModules tests for the library rule, including that another resident's private texture is **still** refused. AIS **157 -> 160**, CoreModules **88 -> 92** passing (the same 5 failures before and after - `InventoryArchiveLoadTests` x3 and `AvatarFactoryModuleTests` x2 - are pre-existing on a clean tree and unrelated), SSB 109, solution 0 errors. | **Not verified live and not deployed** - deploy is a separate session. **What I could not check:** whether upstream OpenSim-NGC has the same texture check or has since changed it; no upstream checkout is on this machine, so the provenance note the S8 row carries for `AvatarFactoryModule` has no counterpart here. Worth confirming before this is offered upstream. |
| 2026-09-07 | S11 + A19 deploy | `508ebc1b34` (deployed) | **Both roots, servers down throughout and not started.** `1.1.258-alpha+77050c5372` -> **`1.1.262-alpha+508ebc1b34`**. Region `-c Release -r win-x64 --self-contained false` (**842** published, **150** differed by content, **0** new, **144** copied after the preserve rule); grid `-c Release --self-contained false`, **no `-r`** (**688** published, **92** differed, **0** new, **90** copied). Every file of the full copy set re-hashed **in the live root** against the staged manifest: 144 and 90, **no mismatch**, both copied in a single clean pass. **What shipped.** **S11** (`b0b3c6b671`) - the `Avatars` writer persists wearable types **15 and 16**. `AvatarData(AvatarAppearance)` walked `i < LEGACY_VERSION_MAX_WEARABLES` (15) and so wrote types 0-14 only; Physics and Universal reached the COF, the presence's wearables and the bake and were never stored. It is the single writer for every path - `AvatarService.SetAppearance` and `AvatarServicesConnector.SetAppearance` both build an `AvatarData` - so a legacy-region save lost them too. The loop is now bounded by the array's own length. **A19** (`508ebc1b34`) - two defects behind one log line. (a) A **refused wearable save answered 200**: the asset-transaction chain was `void` from `IAgentAssetTransactions` down, so a validator refusal reached nobody; it now carries `AisAssetTransaction` (`Applied`/`NotResolvable`/`Refused`) and a refusal answers **403** and emits no `_updated_category_versions`, so the folder version the viewer holds does not advance. (b) The refusal was **wrong**: `ValidateAssets` asked `GetAssetPermissions` under the resident's id only, where a **library texture** can never resolve, though SL allows library textures in wearables and this grid ships a library; it now also asks the library owner, read off `ILibraryService.LibraryRootFolder.Owner`. Assemblies actually changed by these: `OpenSim.Services.Interfaces.dll` (S11), `OpenSim.Region.ClientStack.LindenCaps.dll`, `OpenSim.Region.CoreModules.dll` and `OpenSim.Region.Framework.dll` (A19); the rest of the 71/44 are nbgv re-stamps. **The joltc gate, second run, passed.** Staged publish: exactly two `joltc*` files, both **`16AF7638...43B5`** (ROOT and `runtimes\win-x64\native`), and **all 842 staged files hashed against the stock build - none is `67BECFC7...0910`**. `assert-patched-joltc.ps1` exit 0 against the staged publish and against the live region root, before and after the copy, **2 checked and no notes** on both - the stock leftovers moved out at 1.1.258 have stayed out. Live root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`** throughout, never in the copy set because it was already identical. **Preserved** and proven byte-identical against the pre-deploy backup: 6 region `config-include` files (`Grid.ini`, `GridHypergrid.ini`, `Standalone.ini`, `StandaloneHypergrid.ini`, `GridCommon.ini.example`, `StandaloneCommon.ini.example`), `Regions/` (in the publish and already identical), 2 grid `*.example`, `config/OpenSim.ini`, `config/Robust.ini`, both `Robust*.ini.example.net10`, `TrustedHypergridSecret.ini`, and live-only `WebRtcVoiceServiceModule.dll` still **`1.1.160-alpha+d95754509d`** (`9D7A1E605927BBF1...`), confirmed absent from the publish before the copy. `config`, `ScriptEngines`, `assetcache`, `assets`, `maptiles`, `MeshCache`, `j2kDecodeCache` are not in either publish. File counts unchanged: region **2331**, grid **769**. Backups `D:\legiongrid\_backup\regionserver-20260907-0633` (2331) and `gridserver-20260907-0633` (769), each verified file-for-file against its source before the copy. | **A new standing step: the pre-copy lock check, and it is step one of every region deploy from now on.** At 1.1.258 the region copy stopped half-applied because three idle `dotnet-dump analyze` sessions on a crash dump held 14 assemblies locked, and **neither a `Get-Process -Path` filter nor a loaded-module scan finds that** - `dotnet-dump` maps the assemblies off disk for metadata, so they are open files, not loaded modules of any process. The check is four parts, all run here and all clean: **(1)** debugger and dump-reader processes by name - `cdb`, `dotnet-dump`, `WinDbgX`, `windbg`, `ntsd`, `procdump`, `dotnet-sos`, `dotnet-counters`, `dotnet-trace`, `vsjitdebugger`, `devenv`: none. **(2)** any process imaged under `D:\legiongrid`: none. **(3)** any process with a loaded module under `D:\legiongrid`: none - kept for completeness, and noted as the one that **missed** the 1.1.258 holders. **(4)** an **exclusive-open probe** (`FileShare.None` open-and-close) over the 16 region assemblies the dump readers held last time plus 4 grid ones: all 20 opened and closed. **(4) is the check that actually catches this**, because it asks the only question that matters - can the copy write this file - rather than guessing at who might be holding it. It was re-run immediately before the copy, not only at staging time. **Not verified live:** nothing has been started. The first things to run are checklist **step 18** and **step 17**; then, for S11, confirm `Wearable 15:*` and `Wearable 16:*` rows appear in the `Avatars` record for `a7d2ff2e-dc32-44d8-aa61-3d22070a4964` **on the first appearance save after this deploy** - nothing repairs the rows already missing. For A19, a wearable saved with a library texture should now succeed rather than be refused, and a genuinely refused save should show `UpdateItem -> 403` in the region log instead of 200. |
| 2026-09-07 | S12 deploy | `bb4bcd03dc` (deployed), `162e117383` (one file, see below) | **Both roots, servers down throughout and not started.** `1.1.262-alpha+508ebc1b34` -> **`1.1.264-alpha+bb4bcd03dc`**. Region `-c Release -r win-x64 --self-contained false` (**842** published, **152** differed by content, **0** new, **146** copied after the preserve rule); grid `-c Release --self-contained false`, **no `-r`** (**688** published, **92** differed, **0** new, **90** copied). Full copy set re-hashed **in the live root** against the staged manifest: 146 and 90, **no mismatch**, both in one clean pass. **What shipped: S12** - one switch for the grid. The two global lines `[AIS] Enabled = true` and `[Appearance] ServerSideBaking = true` turn both lanes on for the whole simulator with no region named; a `[<Region Name>]` section is an optional opt-*out*. The resolution already worked this way and was not changed; what changed is everything an operator consults. `OpenSimDefaults.ini` said the opposite in both blocks (*\"Prefer turning it on for ONE region first\"*, *\"try it on one region first\"*) and is rewritten around **TO TURN IT ON FOR THE WHOLE SIMULATOR**; `AISv3Module.Initialise` logged a **warning** ending *\"Prefer a per-region override\"* and now logs INFO stating the rule; and **each lane now logs one line per region at load naming which config decided** - `region <name>: AIS v3 ON (global)` and `region <name>: server-side baking ON (global)`, or `(region section)` - which is what the flip verify reads. **The region copy is 152 where the last three deploys were 150**, and the two extra files are the deliverable: **`OpenSimDefaults.ini`** (the rewritten `[AIS]` and `[Appearance]` guidance) and **`OpenSim.ini.example`** (new `[AIS]`/`[Appearance]` stubs carrying the two commented lines). Both sit at the region root, are in the publish and are **not** on the preserve list - they are shipped reference files, not operator config - so they had simply been byte-identical on every previous deploy. Confirmed landed: the live `OpenSimDefaults.ini` carries the new heading twice. **A defect I shipped and then corrected in the same session.** The S12 edit to `OpenSim.ini.example` matched the **first literal `[Architecture]` in the file, which is inside the header comment** (*\"pick an architecture in the [Architecture] section at the end of this file\"*), so the block was spliced mid-sentence: the header sentence was broken and `[Appearance]` ended up at **column 0 inside the comment block**, where Nini would read it as a real section. It reached the live root before I caught it - the post-deploy check that asks *\"did the guidance actually land\"* is what found it, by looking for `^\[AIS\]` and getting **0**. Fixed in `162e117383` (block moved before the real section header, matched at a line start; header sentence restored verbatim), and the corrected file copied to the live root on its own and re-hashed: staged and live both `C64CF038B5EA4442...`, `[AIS]` at 1289, `[Appearance]` at 1299, `[Architecture]` at 1311. **The assemblies are untouched by that fix** - it is a comment-only documentation file with no version stamp - so every binary in the live root is still exactly `1.1.264-alpha+bb4bcd03dc` and the recorded stamp is honest. **The joltc gate, third run, passed.** Two `joltc*` in the staged publish, both **`16AF7638...43B5`**; **all 842 staged files hashed - none is `67BECFC7...0910`**. Guard exit 0 against staged and live, before and after the copy, **2 checked and no notes** each time. Live root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`** throughout, never in the copy set. **Pre-copy check, now the standing first step, clean on all four parts:** no debugger or dump-reader process by name; nothing imaged under `D:\legiongrid`; nothing with a module there; and the **exclusive-open probe extended to 22 files** - the 16 from the 1.1.258 incident plus `joltc.dll` plus **`OpenSimDefaults.ini` and `OpenSim.ini.example`, added because they entered the copy set this time** - all opened and closed. **`config/OpenSim.ini` was not touched**, by rule and in fact: it is not in the publish at all, the preserve predicate holds `config\*`, and it is byte-identical to the pre-deploy backup (`99E562E6140580FF...`) with `[Ebony] AIS_Enabled = true` and `ServerSideBaking = true` still at `:163-164`. The flip edit is John's, by hand, after this deploy. **Also preserved** and proven byte-identical against the backup: the 6 region `config-include` files, 2 grid `*.example`, `config/Robust.ini`, both `Robust*.ini.example.net10`, `TrustedHypergridSecret.ini`, and live-only `WebRtcVoiceServiceModule.dll` still `1.1.160-alpha+d95754509d`. File counts unchanged: region **2327**, grid **769**. Backups `D:\legiongrid\_backup\regionserver-20260907-0826` (2327) and `gridserver-20260907-0826` (769), each verified file-for-file before the copy. | **Not started, and not verified live.** **The flip is the next action and it is a hand edit:** add `[AIS] Enabled = true` and `[Appearance] ServerSideBaking = true` to `config/OpenSim.ini`, **remove the `[Ebony]` section's `AIS_Enabled` and `ServerSideBaking` lines** (`:163-164`) so Ebony proves the global path rather than its own section, restart, and read the console: **three `(global)` lines per lane**, one per region. A region still logging `(region section)` is one whose section was missed; a region logging `off` after the global lines are in is one that opted out. **Worth carrying forward:** the post-deploy step that asks *\"did the thing this deploy was for actually land, in the deployed file\"* - not just *\"do the hashes match\"* - is what caught the malformed example file. Hashes matched perfectly the whole time; they only ever prove the copy was faithful, never that what was copied was right. |
| 2026-09-07 | PHLOX-2/2b/2c deploy | `c94c561cf1` (deployed) | **Both roots, servers down throughout and not started.** `1.1.264-alpha+bb4bcd03dc` -> **`1.1.275-alpha+c94c561cf1`**. Region `-c Release -r win-x64 --self-contained false` (**842** published, **150** differed, **0** new, **144** copied after the preserve rule); grid `-c Release --self-contained false`, **no `-r`** (**688** published, **92** differed, **0** new, **90** copied). Full copy set re-hashed in the live root against the staged manifest: 144 and 90, **no mismatch**, one clean pass. **What shipped, three sessions of Phlox work.** **PHLOX-2** (`34fb6d201b`): the first test project for the Phlox compiler, and **owner-visible compile errors** - `PhloxCompileErrorReport` composes the SL-shaped message and sends it once via `IDialogModule.SendAlertToUser`, where before a script that would not compile was a log line and nothing else. **PHLOX-2b** (`125120b8bf`): the table becomes name -> list with the overloads appended above the historical 674 so no `TableIndex` moves, plus the shims and the `LSLSystemAPI` implementations. **PHLOX-2c** (`6db4fd9a44`, `8b954cd4c5`, `ac029bc17f`, `dfb9286fc3`): built-in calls now resolve by name **and** signature - the two faults were that a statement-level call reaches `VisitFuncCall` rather than `VisitMethodCallPostfix` and only the latter had been changed, and that `$` is illegal in the assembler's identifier rule, since the emitted instruction is literally `syscall <symbol name>`. Also `botRemoveBot`'s table key, which was `\"Shim_botRemoveBot\"` and made every name-keyed lookup miss it. **The manhole (`9898c41e-...`) now compiles** and will start on the next region start; it had failed at every one since it was rezzed. **The airship (`3eb0c62b-...`) still fails by ruling**, on user-function overloading, which SL rejects too - content to fix, not a compiler defect. **PART 0, done first:** the 17 live script bodies were moved out of the repo onto the SSB golden convention - `LiveScripts/manifest.json` committed with each asset id and its SHA-256, bodies gitignored and pulled by `fetch-live-scripts.sh`, which refuses a body whose hash does not match. They are residents' content and should not have been committed. Verified green both with the bodies fetched and with `scripts/` removed: 39 passed each way. **\"Did it land\", checked in the DEPLOYED binaries and not the publish.** Both Phlox assemblies byte-identical to the staged manifest. `Phlox.ScriptEngine.dll` carries `PhloxCompileErrorReport` and `ToOwner` as metadata names; `InWorldz.Phlox.dll` carries `OverloadSeparator`, `SymbolNameFor` and `ResolveCallForGen`, and the three overload table keys **`osTeleportAgent__3`, `osTeleportAgent__5`, `llLinkPlaySound__3`** as UTF-16 string literals - while **`Shim_botRemoveBot` is gone (0 occurrences)** and `botRemoveBot` is present. *A false alarm worth recording: the first probe used `[Reflection.Assembly]::LoadFrom` and reported `PhloxCompileErrorReport` ABSENT. Windows PowerShell 5.1 is .NET Framework and cannot load a net10.0 assembly, so `GetTypes()` threw. The metadata-name check is the technique that works here, and it is the one the 1.1.233 row used. A \"did it land\" assertion that can fail for its own reasons needs its negative result checked before it is believed.* **The joltc gate, fourth run, passed.** Two `joltc*` in the staged publish, both **`16AF7638...43B5`**; all 842 staged files hashed, none is `67BECFC7...0910`. Guard exit 0 against staged and live, before and after the copy, 2 checked and no notes. Live root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`** throughout. **Pre-copy probe clean on all four parts**, with the exclusive-open probe extended to `InWorldz.Phlox.dll` and `Phlox.ScriptEngine.dll` because they are what this deploy changes. **Preserved** and byte-identical against the backup: the 6 region `config-include` files, `config/OpenSim.ini`, `OpenSimDefaults.ini`, `OpenSim.ini.example`, `joltc.dll`, live-only `WebRtcVoiceServiceModule.dll` (`1.1.160-alpha+d95754509d`), the 2 grid `*.example`, both `Robust*.ini.example.net10` and `config/Robust.ini`. File counts unchanged: region **2403**, grid **769**. Backups `D:\legiongrid\_backup\regionserver-20260907-1103` (2403) and `gridserver-20260907-1103` (769). | **The S12 flip is already in the live config, done by hand since the last deploy.** `config/OpenSim.ini` now carries **`[AIS] Enabled = true` (:163-164)** and **`[Appearance] ServerSideBaking = true` (:166-167)**, and the `[Ebony]` section's own two lines are gone - which is exactly the flip S12 specified. This deploy did not touch that file and it is byte-identical to the pre-deploy backup. **So the next start is the S12 verify as well as the Phlox one:** expect three `(global)` lines per lane in the console, one per region, and no region logging `(region section)`. **Not started, and nothing verified live.** On the next start, in order: the three `ON (global)` lines per lane (S12); the manhole compiling rather than erroring at 16:12 and 20:12 (PHLOX-2c); the airship still failing, and its owner now receiving the message rather than only the log (PHLOX-2). **`InWorldz.Phlox.dll` carries no nbgv stamp** - it reports `1.0.0.0`, not the build number - so its hash is the only evidence it is the right build, which is why the check above is by hash and metadata rather than by version. |
| 2026-09-07 | PHLOX-2d deploy | `c1ce74e9dc` (deployed) | **Both roots, servers down throughout and not started.** `1.1.275-alpha+c94c561cf1` -> **`1.1.277-alpha+c1ce74e9dc`**. Region `-c Release -r win-x64 --self-contained false` (**842** published, **150** differed, **0** new, **144** copied after the preserve rule); grid `-c Release --self-contained false`, **no `-r`** (**688** published, **92** differed, **0** new, **90** copied). Full copy set re-hashed in the live root against the staged manifest: 144 and 90, **no mismatch**, one clean pass. **What shipped: one line.** `PhloxExecutionScheduler.FinishedLoading` set a freshly started script to `RunState = Running` and then posted its `state_entry` (`:189-195`), but `ProcessEventQueue` **starts** an event only for a script that is `Waiting` (`:681`) and otherwise **queues** it (`:687`) - and that queue is drained by `TransitionToWait` (`:558-565`), which runs only for a script already on the run queue, which `StartEvent` alone puts it on. So a fresh script sat in `Running` with its `state_entry` in a queue nothing would ever drain: **no execution, no error, no log line**. It is now `Waiting`, exactly as the restored-state branch has always been (`:200`). `Phlox.ScriptEngine.dll` is the only assembly with a behaviour change; `InWorldz.Phlox.dll` is byte-identical this time, nothing in the compiler having moved. **Not a PHLOX-2/2b/2c regression.** `git blame` puts **both** `:189` and `:681` at **`02cf1370df`** - the original *Add Phlox: LSL/SLua compiler, VM, and region script engine (#182)* import. The defect has been there since Phlox landed. What 2c changed is that the manhole *compiles* now, so it reached the fresh-start path for the first time; every other script on the region had been restored from saved state, which is the branch that works. That is also why script `96c2d98a` ran all day while the manhole and a brand-new script were silent. **The joltc gate, fifth run, passed.** Two `joltc*` in the staged publish, both **`16AF7638...43B5`**; all 842 staged files hashed, none is `67BECFC7...0910`. Guard exit 0 against staged and live, before and after the copy. Live root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`** throughout. **Pre-copy probe clean on all four parts** - and, because the PHLOX-2d brief had said the region was live, the negative was **checked rather than trusted**: no process matching `OpenSim|Robust`, nothing listening on 9000/9010/8002/8003, and the region log's last write at **12:09:36**, the end of the PHLOX-2d evidence window. The region had genuinely been stopped since; the clean gate was real, not a probe failing quietly the way the 1.1.275 `LoadFrom` check did. **Preserved** and byte-identical against the backup: the 6 region `config-include` files, `config/OpenSim.ini`, `OpenSimDefaults.ini`, `OpenSim.ini.example`, `joltc.dll`, live-only `WebRtcVoiceServiceModule.dll` (`1.1.160-alpha+d95754509d`), the 2 grid `*.example`, both `Robust*.ini.example.net10` and `config/Robust.ini`. File counts unchanged: region **2414**, grid **769**. Backups `D:\legiongrid\_backup\regionserver-20260907-1623` (2414) and `gridserver-20260907-1623` (769). | **\"Did it land\" for this deploy is BEHAVIOURAL and is John's, not a hash: a default New Script on a fresh prim says \"Hello, Avatar!\".** Nothing else demonstrates the fix - the hashes only show that `Phlox.ScriptEngine.dll` arrived, which is exactly the check that was mistaken for verification at 1.1.275. **That correction stands on the record:** the 1.1.275 row reported the manhole \"fixed, pending deploy\" and its deploy verified metadata names and overload keys; it could not have shown a script running, because nothing in the suite ran one, and the manhole compiled and then did nothing. **On the next start, in order:** a default New Script on a fresh prim saying *Hello, Avatar!* (this deploy); the manhole's touch working and its `llSetTouchText` having run (PHLOX-2c plus this); the airship still failing to compile **and its owner receiving the message** (PHLOX-2, still never exercised against a real client); and the three `ON (global)` lines per lane (S12, still unverified from the 1.1.264 flip). **Still open and untested:** evidence 3 from PHLOX-2d - whether an edited-and-saved script's asset actually stores and reruns. The save path is the `UpdateScriptTask` cap into `CapsUpdateTaskInventoryScriptAsset`, which calls `CreateScriptInstanceEr` directly and never raises `OnRezScript`, so the missing `OnRezScript` line was expected rather than a fault; A19's `void->bool` is not on that path and was not in this deploy's range. |
| 2026-09-07 | PHLOX-2f deploy | `6fc297c182` (deployed) | **Both roots, servers down throughout and not started.** `1.1.277-alpha+c1ce74e9dc` -> **`1.1.280-alpha+6fc297c182`**. Region `-c Release -r win-x64 --self-contained false` (**842** published, **150** differed, **0** new, **144** copied after the preserve rule); grid `-c Release --self-contained false`, **no `-r`** (**688** published, **92** differed, **0** new, **90** copied). Full copy set re-hashed in the live root against the staged manifest: 144 and 90, **no mismatch**, one clean pass. **What shipped.** **The touch fix, one line at the root:** `RuntimeState(int numGlobals)` - the constructor for every fresh interpreter (`Interpreter.cs:96`) - never set `GeneralEnable`, which only `Reset()` set (`RuntimeState.cs:286`). `FinishedLoading` computes the event mask at `:184` and the freshStart branch resets at `:190`, **after**, and `LSLSystemAPI.SetScriptEventFlags` gates the whole mask on `GeneralEnable` (`LSLSystemAPI.cs:96-101`) - so a fresh instance sent the part a mask of **zero**. `PrimFlags.Touch` is derived from `anytouch` in that mask (`SceneObjectPart.cs:5254-5269`) and the region's own touch dispatch tests the same mask (`Scene.PacketHandlers.cs:334`), so there was no touch cursor and `touch_start` could never fire - while `state_entry` still ran, because `ProcessEventQueue` lets `STATE_ENTRY` past a disabled script (`:664`). Restored-state instances carry a persisted `GeneralEnable`, which is why they were always touchable. **Also shipped: `phlox status <item|object>`**, read-only - RunState, `Enabled`/`GeneralEnable`/suspended, the item's Running flag, queued events, LSL state, timer interval in ms, and the region's event mask for the prim. Both Phlox assemblies changed and both are byte-identical to the staged manifest in the live root. **The timer half was NOT reproduced and no fix was invented for it.** `TimerCadenceTests` asserts a 3 s timer fires at most twice in 3.5 s and **passes on the unmodified tree**; the units are consistent end to end - `(int)(sec * 1000)` ms stored (`PhloxExecutionScheduler.cs:421`), added to `Util.EnvironmentTickCount()` which is also ms (`:434`), compared directly (`:601-605`). The live ~5 ticks/second therefore lives somewhere this harness cannot see by construction, most likely `PhloxMasterScheduler`'s thread and its wake arithmetic, which already carries one documented lost-wakeup fix (`PhloxMasterScheduler.cs:70-79`). **It is still open.** **The joltc gate, sixth run, passed.** Two `joltc*` in the staged publish, both **`16AF7638...43B5`**; all 842 staged files hashed, none is `67BECFC7...0910`. Guard exit 0 against staged and live, before and after the copy. Live root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`** throughout. **Pre-copy probe clean on five checks**, the negative checked rather than assumed: no debugger or dump reader; **no sim process by path or by name**; nothing with a module under `D:\legiongrid`; the exclusive-open probe on 15 files including both Phlox assemblies; and the region log's last write at **18:56:43**, consistent with the region having been stopped after the 15:48-15:53 testing. **Preserved** and byte-identical against the backup: the 6 region `config-include` files, `config/OpenSim.ini`, `OpenSimDefaults.ini`, `OpenSim.ini.example`, `joltc.dll`, live-only `WebRtcVoiceServiceModule.dll` (`1.1.160-alpha+d95754509d`), the 2 grid `*.example`, both `Robust*.ini.example.net10` and `config/Robust.ini`. File counts unchanged: region **2181**, grid **769**. Backups `D:\legiongrid\_backup\regionserver-20260907-1904` (2181) and `gridserver-20260907-1904` (769). | **\"Did it land\" is BEHAVIOURAL and is John's, and it is named here BEFORE the deploy rather than after:** **(1)** a fresh New Script prim shows a **touch cursor** and `touch_start` fires; **(2)** the airship hatch's menu reads **\"Enter\"** - its `llSetTouchText` from `state_entry` finally taking effect on the region's side; **(3)** **`phlox status`** on a fresh script shows a **non-zero event mask**. Hashes show only that the assemblies arrived, which at 1.1.275 was mistaken for verification; that correction stands. **(3) is also the diagnostic for the open timer question:** the same output prints the timer interval the scheduler is actually holding, so if a `llSetTimerEvent(3.0)` script reports **3000 ms** and still fires about five times a second, the fault is in the master thread's wake loop and not in the units - which is the next thing to chase. **Still pending from earlier deploys, all in one start:** the three `ON (global)` lines per lane (S12, since 1.1.264); the manhole compiling and now touchable (PHLOX-2c + 2f); the airship still failing to compile **and its owner receiving the message** (PHLOX-2, never exercised against a real client); and a default New Script saying *Hello, Avatar!* (PHLOX-2d). |
| 2026-09-07 | PHLOX-2g deploy | `f6d90db6d7` (deployed) | **Both roots, servers down throughout and not started.** `1.1.280-alpha+6fc297c182` -> **`1.1.282-alpha+f6d90db6d7`**. Region `-c Release -r win-x64 --self-contained false` (**842** published, **150** differed, **0** new, **144** copied after the preserve rule); grid `-c Release --self-contained false`, **no `-r`** (**688** published, **92** differed, **0** new, **90** copied). Full copy set re-hashed in the live root against the staged manifest: 144 and 90, **no mismatch**, one clean pass. **What shipped, two defects.** **(1) 24 async syscalls never signalled completion.** The contract is that a long-running shim sets `RunState = Status.Syscall` and the API implementation calls `SysReturn` to bring the script back (`PhloxEngine.cs:1100` -> `PostSyscallReturn:461` -> `ProcessSyscallReturns:893-913`); nothing enforced it, and of the 36 shims that set `Syscall`, **24 called implementations that never return** - so the script stayed parked for ever with no error and no timeout, later events piling up. That is the manhole at 19:21: `RunState=Syscall`, four queued events, parked in `osTeleportAgent` from its `touch_start`. `DrainAsyncQueue` also swallows a throwing body and logs it (`:1107-1111`), stranding the script just as thoroughly. **Fixed at the mechanism, not per function**: `SyscallShim.RunAsync` signals completion in a **finally** through a new `ISystemAPI.CompleteSyscall()`, so a throwing body cannot strand a script either; **22 shims converted**, and the **14 that still set Syscall by hand are correct as they are** because their implementations post a real value and delay. It is safe alongside those: `ProcessSyscallReturns:897` ignores a return for a script no longer in `Syscall`, so a duplicate is a no-op. **Two of the twenty-four were introduced by PHLOX-2b**, copied from the already-broken `Shim_osTeleportAgent`. **(2) llSetTouchText never told the viewer.** It set `m_host.TouchName` and stopped (`LSLSystemAPI.cs:1749-1753`); the context menu comes from the object update, so the menu kept whatever it last received - which is why the manhole's menu still read Touch after its `state_entry` had run `llSetTouchText` perfectly. Now marks the group changed and schedules a full update, as `llSetClickAction` three lines below already did. **A correction to the brief's framing:** `llSetTouchText` is a *synchronous* shim (`SyscallShim.cs:3075-3081`) and does complete - it was never what parked the script. **Also shipped, and it is the diagnostic that was missing:** `phlox status` now names the pending call - `RunState : Syscall  (in osTeleportAgent)` - from a `LastSyscallIndex` recorded at dispatch and mapped back through `Defaults.AllMethods`; and `engine=True`, which printed `ScriptRunning` twice, now prints the engine name. Both Phlox assemblies changed and both are byte-identical to the staged manifest in the live root. **The joltc gate, seventh run, passed.** Two `joltc*` in the staged publish, both **`16AF7638...43B5`**; all 842 staged files hashed, none is `67BECFC7...0910`. Guard exit 0 against staged and live, before and after the copy. Live root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`** throughout. **Pre-copy probe clean on five checks**, the negative checked rather than assumed: no debugger or dump reader; no sim process **by path or by name**; nothing with a module under `D:\legiongrid`; the exclusive-open probe on 15 files including both Phlox assemblies; and the region log's last write at **19:34:00**, consistent with the region having been stopped after the 19:21 `phlox status` reading. **Preserved** and byte-identical against the backup: the 6 region `config-include` files, `config/OpenSim.ini`, `OpenSimDefaults.ini`, `OpenSim.ini.example`, `joltc.dll`, live-only `WebRtcVoiceServiceModule.dll` (`1.1.160-alpha+d95754509d`), the 2 grid `*.example`, both `Robust*.ini.example.net10` and `config/Robust.ini`. File counts unchanged: region **2191**, grid **769**. Backups `D:\legiongrid\_backup\regionserver-20260907-2007` (2191) and `gridserver-20260907-2007` (769). | **Did it land is BEHAVIOURAL and is John's, named here BEFORE the copy:** **(1)** the airship hatch's menu reads **Enter**; **(2)** **`phlox status`** shows ManholeAccess **Waiting with 0 queued**; **(3)** touching the hatch produces the **OSSL permission refusal, not silence**. **(3) is the sharp one** and is why it is on the list: a refusal means `osTeleportAgent` ran and *returned* through the new completion path. Silence would mean it is still parked - the same observable as before, from a different cause. Hashes show only that the assemblies arrived, which at 1.1.275 was mistaken for verification. **The manhole across four sessions, for the record:** it **compiled** at 2c (the 3-argument `osTeleportAgent` overload), **registered touch** at 2f (`GeneralEnable` on a fresh instance), and was **unparked** at 2g - with its menu text never reaching the viewer throughout, also 2g. Every one of those fixes was correct and **none of them alone made the object work**, which is the thing to remember about the next one. **Still pending from earlier deploys, all closable in one start:** the three `ON (global)` lines per lane (S12, since 1.1.264); a default New Script saying Hello, Avatar (PHLOX-2d); a fresh prim showing a touch cursor (PHLOX-2f); the airship failing to compile **with its owner notified** (PHLOX-2, never exercised against a real client); and the **timer cadence**, still unreproduced (PHLOX-2f) - `phlox status` now prints the interval the scheduler holds, so if a 3.0 s timer reports **3000 ms** and still fires about five times a second, the fault is in the master thread's wake loop and not the units. |
| 2026-09-08 | PROPS-1 deploy | `a902cd7a82` (deployed) | **Both roots, servers down throughout and not started.** `1.1.282-alpha+f6d90db6d7` -> **`1.1.285-alpha+a902cd7a82`**. Region `-c Release -r win-x64 --self-contained false` (**842** published, **150** differed, **0** new, **144** copied after the preserve rule); grid `-c Release --self-contained false`, **no `-r`** (**688** published, **92** differed, **0** new, **90** copied). Full copy set re-hashed in the live root against the staged manifest: 144 and 90, **no mismatch**, one clean pass. **What shipped, the briefed part.** PROPS-1: **a script that changes a prim's label never told the viewer.** The label a viewer shows is whatever it was told the last time the object was **selected** - `SelectPrim` sends the full ObjectProperties (`Scene.PacketHandlers.cs:223`), and the touch name rides only that packet (`LLClientView.cs:6387`). A right-click sends `RequestObjectPropertiesFamily`, and **the Family reply carries no touch-name field at all**, so a `state_entry` that sets the label on an object nobody has re-selected is invisible for ever. The viewer agrees from the other side: `LLSelectMgr::processObjectProperties` fills `mTouchName` (`llselectmgr.cpp:6110`) and the menu reads it (`llviewermenu.cpp:3096-3105`). PHLOX-2g's `ScheduleFullUpdate` could not have helped - an ObjectUpdate carries no touch name. New `SceneObjectPart.SendPropertiesToAllClients()` pushes the full block to every client in the region; **four** setters now call it - `llSetTouchText` (`3da426f6d7`) and, in the deploy commit, `llSetSitText`, `llSetObjectName` and `llSetObjectDesc`, which had the identical shape. Each pinned by a test that adds a real client to the test scene and asserts the reply was sent for that part, and each **proved red first** by disabling the push, not merely green after it. **This is latent upstream OpenSim behaviour, not a Phlox regression** - `LSL_Api.llSetTouchText` (`:8420-8426`) also just assigns the field. **Also shipped, and it was NOT in the brief: the `AsyncCommandManager` thread-start lock.** `StartThread()` was check-then-assign-then-start on a **static** `cmdHandlerThread` with no mutual exclusion, and it is called from the constructor at `AsyncCommandManager.cs:115` - **outside** the `lock (m_ScriptEngines)` that guards everything above it. Two engines constructing at once both pass the `IsAlive` check, the second overwrites the static before the first reaches `Start()`, and one of them calls `Start()` on a thread the other already started: `ThreadStateException: Thread is running or terminated; it cannot restart`, out of `PhloxEngine.RegionLoaded` (`PhloxEngine.cs:124`). **Found by the suite, not by reading code** - it is the `TimerCadenceTests` failure I twice wrote off as flakiness in PHLOX-2f and widened a bound for; that widening is reverted here, bound back to **2**. **Every region in a process builds its own `AsyncCommandManager`, and this process runs three - Ebony, Elm and Transylvania (`config/regions/Regions.ini`) - so the `RegionLoaded` race path is real on this deploy, not theoretical.** It has not been observed in a live log and no such claim is made. **Reported to John before 'go' with the option to split it into its own session; he chose to ship it as staged.** **The joltc gate, eighth run, passed.** Two `joltc*` in the staged publish, both **`16AF7638...43B5`**; all 842 staged files hashed, none is `67BECFC7...0910`. Guard exit 0 against staged and live, before and after the copy. Live root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`** throughout, still exactly two copies after the PHYS-4b stock removal. **Pre-copy probe clean on five checks**, the negative checked rather than assumed: no debugger or dump reader; no sim process **by path or by name**; nothing with a module under `D:\legiongrid`; the exclusive-open probe on 15 files including both Phlox assemblies and `OpenSim.Region.Framework.dll`; and both logs last written **05:51:5x**, the region log ending `[SHUTDOWN]: Shutdown processing on main thread complete` - a clean stop, not a crash. **Preserved** and byte-identical against the backup: the 6 region `config-include` files, `config/OpenSim.ini`, root `OpenSimDefaults.ini` and `OpenSim.ini.example` (neither of the last two even entered the changeset - staged and live were already identical), `joltc.dll`, live-only `WebRtcVoiceServiceModule.dll`, the 2 grid `*.example`, both `Robust*.ini.example.net10` and `config/Robust.ini`. File counts unchanged: region **2209**, grid **770**. Backups `D:\legiongrid\_backup\regionserver-20260908-0621` (2209) and `gridserver-20260908-0621` (770). | **Did it land is BEHAVIOURAL and is John's, named here BEFORE the copy:** **(1)** a fresh prim whose script calls `llSetTouchText(\"Enter\")` shows **Enter** in the right-click menu **without re-selecting**; **(2)** Balpien's **Fibre Optic Access** prim shows **Enter**. **(2) is the sharp one:** it is an object already in the world that nobody is going to re-select, which is exactly the case the Family reply cannot serve. If it reads Enter, the push reached a client that never selected it. **Still pending from earlier deploys, all closable in one start:** the three `ON (global)` lines per lane (S12, since 1.1.264); a default New Script saying Hello, Avatar (PHLOX-2d); a fresh prim showing a touch cursor (PHLOX-2f); the airship failing to compile **with its owner notified** (PHLOX-2, never exercised against a real client); the three PHLOX-2g checks if 1.1.282 was not exercised before this copy; and the **timer cadence**, still unreproduced (PHLOX-2f). |
| 2026-09-09 | MERGE-1 deploy | `691a52bb9c` (deployed) | **Both roots, servers down throughout and not started.** `1.1.285-alpha+a902cd7a82` -> **`1.1.287-alpha+691a52bb9c`**. Region `-c Release -r win-x64 --self-contained false` (**855** published, **159** differed, **13 new**, **166** copied after the preserve rule); grid `-c Release --self-contained false`, **no `-r`** (**693** published, **98** differed, **5 new**, **101** copied). Full copy set re-hashed in the live root against the staged manifest: 166 and 101, **no mismatch**, one clean pass. **What shipped: the merge of upstream/develop `a8b956742c`, 33 commits**, not a feature. Resolved 22 conflicts - 18 trusted-HG paths taken from upstream (byte-identical to `origin/feature/trusted-hypergrid`; ais-v3 held an older snapshot), `Robust.HG.ini.example` taken from upstream, `.gitignore` and `Tranquillity.sln` kept ours as strict supersets, and `MapImageModule.cs` **hand-merged**. **The hand-merge is the part worth remembering.** The brief said keep ours; that rule was wrong and was corrected before anything was staged. Ours already had the varregion size fix (`new SKBitmap(Heightmap.Width, Heightmap.Height)`), but upstream's #201 (`ae089ab7a7`) is a **two-part** fix and only the first part conflicted: it also replaces the fixed `encoderConfig` with `BuildEncoderConfig(width, height)`, pinning the J2K tile size to the image. Upstream's own remark: correcting the bitmap size without this \"would have replaced an under-sized map with an unrenderable one\", because **SL viewers cannot render multi-tile JPEG2000 and show a blank square with no error anywhere in the pipeline**. `git checkout --ours` would have silently dropped that cleanly-merged half and shipped a **16-tile** codestream for Elm at 1024. Resolution: ours' config-driven `CreateMapTile` plus upstream's encoder, verified `SetSize(256, 256)` count **0**. **Also carried:** `Warp3D.csproj` drops `OpenMetaverse.Rendering.Meshmerizer 1.0.6`, which upstream's v1.0 merge `e462a07511` reintroduced after `58424056dd` had moved LibOMV to UtopiaSkye 1.1.7 on nuget.org; that package lives only on the org-private feed and **failed the build with NU1301/401 until it was removed** - the repo-wide `Directory.Build.props` reference covers it. Reviewed auto-merges: Phlox `AsyncCommandManager` (our PROPS-1 thread-start lock **and** upstream `939662396c`'s per-engine HTTP/XMLRPC poll both present - and upstream's rationale is the same three-regions-one-process fact that made our race real), `RegionServer.csproj` (**PHYS-4b `ExcludeAssets=\"native\"` intact** alongside upstream's five new Addons ProjectReferences), `VectorRenderModule.cs` (identical to upstream). Build 0 errors; **403 passed / 0 failed / 4 skipped** across TrustedHypergrid (71+4 MySql-gated), Phlox (55), AIS (160) and SSB (117); `DispatchIndexGuardTests` 3/3. **Pre-go gate on the four NEW addon assemblies, run because upstream's ProjectReferences put them in the region root for the first time** - `Gloebit.GloebitMoneyModule.dll`, `OpenSim.Addons.OfflineIM.dll`, `OpenSimMutelist.Modules.dll`, `OpenSimSearch.Modules.dll`. Each entry point read and each gate checked against the LIVE config, read-only: **Gloebit** needs `[Economy] economymodule` to equal `GloebitMoneyModule` or `Gloebit` (`GloebitMoneyModule.cs:206-226`) - live `[Economy]` (`config/OpenSim.ini:70`) holds only `SellEnabled = true`, so the key is absent and it returns disabled. **OfflineIM** needs `[Messaging] OfflineMessageModule` = `Offline Message Module V2` (`OfflineIMRegionModule.cs:50-56`) - `[Messaging]` exists (`GridCommon.ini:55`, `GridHypergrid.ini:79`) but has no such key. **Mutelist** needs `[Messaging] MuteListModule` = `OpenSimMutelist` **and** a non-empty `MuteListURL` (`OpenMutelist.cs:65-87`) - key absent, default `None`. **OpenSearch** needs a `[Search]` section at all (`OpenSearch.cs:33-46`) - **absent from every live config file**; note its `Module` key defaults to `OpenSimSearch` when missing, so the section's absence is what gates it, not the key. **All four inert; nothing excluded from the copy set.** No config was edited. **The joltc gate, ninth run, passed.** Two `joltc*` in the staged publish, both **`16AF7638...43B5`**; all **855** staged region files and all **693** staged grid files hashed, none is `67BECFC7...0910`; **both live roots swept too, before and after the copy - 0 stock matches**. Guard exit 0 against staged and live, before and after. Live root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`** throughout. **Pre-copy probe clean on five checks:** no debugger or dump reader; no sim process by path or by name; nothing with a module under `D:\legiongrid`; the exclusive-open probe on 15 files; and both logs last written **18:52-18:53 on 09-08**, the region log ending `[SHUTDOWN]: Shutdown processing on main thread complete` - a clean stop. **Two root `.ini` files WERE updated this time, and previous rows' claim that they are \"preserved\" was luck, not the rule.** `OpenSimDefaults.ini` and `OpenSim.ini.example` live in the region ROOT; `finish.ps1`'s preserve rule only covers `config\*`, `config-include\*` and `Regions\*`. Until now they were identical between staged and live so they never entered a changeset. Upstream changed both, so both were copied: pure additions - a new active default **`StrictStateMigrationVersion = false`** (best-effort restore of a `.state` snapshot on a version mismatch instead of discarding it) and a documented `OnlyGenerateIfMissing` map-tile option. Both live copies are byte-identical to staged. **`config/OpenSim.ini` was verified byte-identical to the backup** - John's flip edit stands. The rule should be widened before a release that changes a shipped `.example` we do want held. **Preserved** and byte-identical against the backup: the 6 region `config-include` files, `config/OpenSim.ini`, `joltc.dll`, live-only `WebRtcVoiceServiceModule.dll`, the 2 grid `*.example`, both `Robust*.ini.example.net10` and `config/Robust.ini` - 14 files checked, 0 changed. Note the upstream `Robust.HG.ini.example` taken in the merge therefore **does not reach the live grid**; that is the preserve rule working as designed, but the new trusted-HG stanza will not be visible on the server. **File counts, with the caveat this row adds:** region **2017** and grid **775** after the copy. The region figure includes `assetcache`, which is a rotating runtime cache - **956** of those 2017 files - so it is not a stable deploy metric. Yesterday's row recorded 2209; the drop to 2004 before this copy was **entirely** assetcache expiry (218 gone, 13 added), checked file-by-file against the backup rather than assumed. Backups `D:\legiongrid\_backup\regionserver-20260909-0431` (2004) and `gridserver-20260909-0431` (770). | **Did it land is BEHAVIOURAL and is John's, named here BEFORE the copy:** **(1)** the region starts and all three regions (Ebony, Elm, Transylvania) come up on `1.1.287`; **(2)** **Elm's map tile renders** rather than showing a blank square - the direct test of the hand-merged single-tile encoder, and the reason that resolution mattered; **(3)** `[GLOEBITMONEYMODULE]` logs *disabled* and no `[SEARCH]`/`[OS MUTELIST]`/`[OfflineIM.V2]` enable line appears, confirming the four new addons stayed inert in practice as the source said they would. **(2) is the sharp one** and it is new: no previous deploy could have shown it, because the multi-tile bug only appears once the tile is larger than 256 - which our own size fix is what caused. If Elm's map is blank, the encoder half did not take. **Still pending from earlier deploys:** the three `ON (global)` lines per lane (S12, since 1.1.264); a default New Script saying Hello, Avatar (PHLOX-2d); a fresh prim showing a touch cursor (PHLOX-2f); the airship failing to compile with its owner notified (PHLOX-2); the three PHLOX-2g checks; the two PROPS-1 checks (`llSetTouchText(\"Enter\")` without re-selecting, and Balpien's Fibre Optic Access prim) if 1.1.285 was not exercised before this copy; and the **timer cadence**, still unreproduced (PHLOX-2f). **LIVE VERIFICATION, 2026-09-09 04:37 - all three named checks observed on `1.1.287-alpha+691a52bb9c`.** **(1)** Ebony, Transylvania and Elm all came up, each logging `AIS v3 "ON" ("global")` and `server-side baking "ON" ("global")` - which **also closes S12's pending item**, open since 1.1.264: the global flip is done and every region reads `("global")`, none `("region section")`. **(2) Elm's map tile renders in the world map**, so the hand-merged single-tile J2K encoder took. This is the check no earlier deploy could have run. **(3)** `llHTTPRequest` completed end to end - `[Phlox HTTP]: http_response ... status 200 -> prim 464506328 (accepted=True)` at 04:41:38 - which exercises the PHLOX-2g completion path through a real network round trip rather than a test double. **(4)** All four new addon assemblies **load and stay disabled**, as the pre-go source read said they would: `[GLOEBITMONEYMODULE]: The Gloebit MoneyModule is disabled (not selected in OpenSim.ini)`, and **no** `[SEARCH]`, `[OS MUTELIST]` or `[OfflineIM.V2]` enable line appears at all. **One thing to know about, harmless today:** two different classes register under the same shared-module name **`MuteListModule`** - core's `OpenSim.Region.CoreModules.Avatar.InstantMessage.MuteListModule` and the new `OpenSimMutelist` - and the loader logs `Found shared region module "MuteListModule"` twice, adding the core class to each scene twice. Both are gated off, so it is inert; it would matter the day someone turns either on, and it is a name collision rather than a config problem. |
| 2026-09-08 | Timer cadence - CLOSED, no session needed | `1.1.285` live | **The `llSetTimerEvent(3.0)` cadence defect does not exist.** PHLOX-2f logged a 3.0 s timer appearing to fire about **five times a second** and left it unreproduced; the harness could not see it because it replaces `PhloxMasterScheduler`'s wake loop by construction, so the suspicion was that the fault lived in that loop. **Measured on Ebony under 1.1.285: 20 ticks per minute**, which is exactly 3.0 s, and `phlox status` reports the interval the scheduler holds as **3000 ms**. Units are consistent end to end and the wake loop is correct. The 09-07 observation was a **one-off** and is recorded as such rather than explained - most likely a burst of queued events draining after the script was unparked by PHLOX-2g, which is precisely what that session changed. **No session is warranted**; if it recurs, the thing to capture first is `phlox status` at the moment it is fast, because that separates a wrong interval from a queue drain. | Closes the last open item from PHLOX-2f. |
| 2026-09-08 | A19 vs upstream - it is a BEHAVIOUR change, not two signatures | `upstream/develop` | **What the PR description has to say changed.** `AssetXferUploader` on `upstream/develop` still **rejects any texture the uploader does not own**, library-owned included, by returning `0` - and the caller turns a `0` into an HTTP **200**, so the viewer is told the save succeeded and the wearable silently keeps its old texture. That is the defect A19 fixed. **So A19 is not merely the two signature changes** (`UpdateItem` returning a result rather than void, and the ownership check taking the library as a legitimate owner): it changes observable behaviour twice over - **library textures are now accepted** in a wearable save, and **a genuine refusal now surfaces as a 403** instead of a lying 200. Any upstream PR must say so, because a reviewer reading only the diff of the signatures will not see it. | Open: the PR itself is unwritten. |
| 2026-09-08 | A12 B3 re-scoped to B3-attach - low priority, not a session | - | **S10 already did most of B3's job.** The COF read on the `UpdateAvatarAppearance` POST covers **wearables at every login**, because the viewer POSTs at login on a bit-0 region; so the reconciliation B3 was written to perform for wearables happens now, on every login, as a side effect of the appearance path. **What is left is attachments only, and it is smaller than B3 was.** Call it **B3-attach**: after that POST, detach the attachments in the stored record that have **no COF link** - and only ever on *disagreement*, never on an empty or unreadable COF, because an empty COF is far more likely to mean the read failed than that the resident is wearing nothing. That guard is the whole risk of the item. **S8's unrenderable-slot case is reconciled by neither, by design** - a wearable whose asset cannot be resolved is not the same as a wearable that should not be worn, and quietly stripping it would destroy an outfit over a transient asset failure. | Low priority. Not scheduled. |
| 2026-09-05 | Trusted Hypergrid is already upstream - "upstreaming undecided" is STALE | `ee105dad21` | **Recorded because this ledger still says the question is open, and it is not.** Trusted Hypergrid landed on `upstream/develop` as **PR #200**, commit `ee105dad21`, by **Legion Hienrichs**, on **2026-09-05**. MERGE-1 took all 18 conflicting trusted-HG paths from upstream precisely because upstream's copies are byte-identical to `origin/feature/trusted-hypergrid` and ais-v3 was carrying an older snapshot of the same branch. Any note in this ledger or the trusted-HG docs that treats upstreaming as undecided should be read as superseded by this row. | Closed. |
| 2026-09-09 | Orphaned test project - `Tests/OpenSim.Services.InventoryService.Tests` | `0914c8104a` (upstream) | **It cannot ever have built in this tree, and nobody noticed because it is not in the solution.** Found while running MERGE-1's test sweep. It is absent from `Tranquillity.sln` on **both** sides of the merge, so `dotnet build Tranquillity.sln` produces no output for it and `--no-build` reports the test source file was not found. Built standalone it fails at restore: it references `..\..\Source\OpenSim.Services\OpenSim.Services.csproj`, **a project that exists on neither side**, and pins `Microsoft.Extensions.Logging.Console 9.0.7` and `Microsoft.NET.Test.Sdk 17.14.1` against `OpenSim.Tests.Common`'s 10.0.10 and 18.8.1 - **NU1605** package downgrade, escalated to an error. It arrived with upstream's `0914c8104a` (dotnet10 SDK/runtime update) and the merge does not touch it. **Open: delete it or repair it, in its own session - and raise it with Mike**, since it is upstream's file and the answer may be that it was superseded and should go. | Open. Not a MERGE-1 regression. |
| 2026-09-09 | Deploy procedure - four corrections, and there is NO procedure doc to put them in | - | **Reported rather than invented: this repo has no deploy-procedure document.** The procedure has only ever been carried in the deploy briefs and in the narrative of the rows above; `A5-LIVE-CHECKLIST.md` is the AIS **in-world** checklist and is not it. These four corrections are therefore recorded here, and a standalone doc is a decision for John, not something to guess at. **(1) The standing check's regex is wrong.** The flip verify is documented as grepping for `AIS v3 ON (global)`, but the structured logger **quotes every argument**, so the line actually written is `[AIS]: region "Ebony": AIS v3 "ON" ("global")` (`AISv3Module.cs:114`, `ServerSideBakingModule.cs:125`). The working form is **`grep -E 'AIS v3 "ON"\|server-side baking "ON"'`**. The example block in `DESIGN-BRIEF-ssb-appearance.md` s4.7 has been corrected to match the log. **(2) The preserve rule does not cover `OpenSimDefaults.ini` or `OpenSim.ini.example` in the region root, and that is intended.** They are **upstream** files, not operator config, and they ARE overwritten when upstream changes them - which first happened at the MERGE-1 deploy. The preserve rule covers `config\*`, `config-include\*` and `Regions\*`. **`config/OpenSim.ini` is the only region config the operator owns**; it is the one file a deploy must never touch, and it is checked byte-identical against the backup after every copy. **(3) "Did it land" is a required step, not a nicety.** Every deploy names, **before the copy**, at least one behaviour visible in-world or in the log that the change is supposed to produce - and **the deploy is not verified until that behaviour is observed**. Hashes prove that bytes arrived; at 1.1.275 that was mistaken for verification and four sessions were spent on an object that had never worked. **(4) Region-root file counts are not a deploy metric.** They include `assetcache`, a rotating runtime cache - 956 of the 2017 files after the MERGE-1 deploy. A count that moves between deploys should be checked against the backup file-by-file before it is treated as a discrepancy. | Open: whether to promote this into a standalone procedure doc. |
| 2026-09-09 | Startup review of 1.1.287 - five findings, none blocking | `691a52bb9c` live | **Read from the first two starts on 1.1.287 (04:37 and 04:56) after John brought the grid up. Nothing here stops anything; it is written down so none of it is investigated twice.** **(1) "No appender named Console" on every start is pre-existing and cosmetic.** `RegisterCommonAppenders` (`ServerBase.cs:181`) still walks the **log4net** repository for an appender named `Console`, finds none, and prints the warning at `:208`. It has been dead code since `cbdfba2811` "Feature/ilogger migration (#198)" moved logging to Serilog/`ILogger`; `OpenSimConsoleLogSink` two lines above is what actually wires the console now. Two details worth having: the file is `Source/OpenSim.Framework.Servers/ServerBase.cs`, **not** `OpenSim.Server.Base`; and the message goes through `Notice(...)`, which writes to the **console only** - it appears in no `.log` file, which is why grepping the logs for it returns nothing. **Upstream note, not a fix for this fork.** **(2) The Slow timeslice warnings had one cause, and it was John's own test prim.** Every `[PhloxExe]: Slow timeslice for "96c2d98a-f091-4035-aabe-f0ff34ce918d"` line - 1000-1600 ms apiece - came from **"Timer test (2)"** on Ebony, item `f3ab8b13...`, running **`llSetTimerEvent(0.01)`**. A 10 ms timer. **This is also the source of the 09-07 "five ticks a second" reading** that PHLOX-2f logged as an unreproduced cadence defect and that the row above closed as a one-off: it was neither a scheduler bug nor a queue drain, it was a script asking for 100 Hz. **Prim deleted.** The cadence question is now closed twice over. **(3) Sculpt `IMesher returned null` x55 per start on Ebony is pre-existing.** Identical count on 1.1.285 (110 lines across two starts, both days), so the merge did not cause it. **Physics lane**, not appearance and not scripting. **(4) `AddMapTile` takes 6-15 s per tile** against the local map service (`14695ms`, `13230ms`, `6412ms` observed). **Perf lane.** Worth noting it is the same subsystem the MERGE-1 map-tile fix touched, but a different question - that fix was about what the tile *contains*, this is about how long storing it takes. **(5) A hypergrid round trip worked, and left a small appearance data point.** Legion returned from `Shopping BloodMoon @ bloodmoonpack.com:8002` with teleportflags `"ViaHome, ViaHGLogin"` into Ebony at 04:47:45, and the appearance cap then reported `"UpdateAvatarAppearance" for "Legion Hienrichs": stale - "client cof_version 737, server 734"` - a **3-step gap**, which **self-corrected**. Recorded as a data point for the section 4.6 open item on cof_version drift after a hypergrid hop, not as a defect: one observation, and the system recovered on its own. **Where the deploy procedure now lives.** It is written up as `D:\legiongrid\_ops\procedures\DEPLOY-PROCEDURE.md`, from what the PROPS-1 and MERGE-1 deploys actually did. **It is deliberately untracked** - `D:\legiongrid\_ops\` sits outside every worktree so operational notes never enter a branch or an upstream merge, with `handoffs\` alongside it for John's session briefs. The consequence is accepted: git does not back it up. | Open: (3) and (4) are unowned lanes. (5) feeds section 4.6. |
| 2026-09-09 | PHLOX-3a - `state default;` crashed the Phlox compiler | `3efe425650`, `7ae8910a41`, `39f1a0c113` | **One null dereference stopped any script that returns to its default state from compiling, and the resident was told it was their fault.** `TypesVisitor.VisitStateChangeStmt` read `context.ID().GetText()` unguarded (`TypesVisitor.cs:206`), but the grammar matches the default state as a **keyword**, not an identifier - `LSL.g4:87`, `stateNode='state' (ID \| 'default') SEMI` - so `ID()` is null for exactly `state default;` and the call threw a `NullReferenceException` out of the entire compile. The line below it already read `stateName == "default"`, so the check was written expecting `default` to arrive as an ID; it never does. **The back end was never wrong**: `GenVisitor.cs:237` already used `context.ID()?.GetText()` and `ByteCodeEmitter.StateChange` emits `statechg @default` for a null id. One null-conditional, no restructuring. `state default;` is valid LSL - the SL wiki's State page demonstrates it - so the fix is to compile it. **Found from the live log, fixed from the live asset.** `lmap4` on Ebony (item `4e51f068...`, asset `1ee3b9b1...`) failed on every start under 1.1.287, twice. The asset was fetched **read-only** from the grid's own asset service and is committed byte-for-byte as `Tests/InWorldz.Phlox.Tests/Fixtures/lmap4.lsl` with a `.gitattributes` pinning it as `-text`, so a Windows checkout cannot normalise the bytes the test compiles. The resident is not named. This is deliberately **not** the PHLOX-2c `LiveScripts/` convention, which surveys the grid in bulk and commits no bodies; a fixture here pins one defect and is worthless without its body. **The second defect is the one that will matter again.** `CompilerFrontend.Compile` ends in a blanket `catch (Exception e)` reporting `e.Message` and nothing else, so a compiler crash reached the resident through PHLOX-2's owner path as *"Object reference not set to an instance of an object."* - **indistinguishable from a verdict on their script**. New `InWorldz.Phlox.Types.CompilerCrash` marks it: the log gets the exception type and **stack**, the owner gets `Script <name>: compiler error (not a script syntax error) - <type>; reported to the grid operator` and no stack, and an ordinary error **still** reports as `line N:C <message>` - pinned by its own test, since without that the distinction is only a second wording. Phlox suite **55 -> 60**, all green; solution 0 errors. Three commits, red test first: the fixture crashed before the fix and the reduced two-state repro crashed on its own, which is what identified the construct. | **Not deployed.** Did-it-land for the next deploy, named now: **(1)** a region start logs **no** `[PhloxCompile]` error for `4e51f068`; **(2)** `phlox status 4e51f068-...` shows an interpreter; **(3)** `lmap4` behaves as a lamp - touch cycles it through high, medium, low and off. **Filed, not chased:** the SL wiki warns *"NEVER do a state change from within a touch_start event"*, and `lmap4` does exactly that in all four states. Script-quality matter for its owner; the resident's script was not changed. |
| 2026-09-09 | PHLOX-3a deploy | `47a0f3a497` (deployed) | **Both roots, servers down throughout and not started. First deploy run from the written procedure** (`D:\legiongrid\_ops\procedures\DEPLOY-PROCEDURE.md`), every numbered step with its pass condition. `1.1.287-alpha+691a52bb9c` -> **`1.1.294-alpha+47a0f3a497`**. Region `-c Release -r win-x64 --self-contained false` (**855** published, **158** differed, **0** new, **152** copied after the preserve rule); grid `-c Release --self-contained false`, **no `-r`** (**693** published, **92** differed, **0** new, **90** copied). Full copy set re-hashed in the live root against the staged manifest: 152 and 90, **no mismatch**, one clean pass. **What shipped: PHLOX-3a, and only PHLOX-3a.** `state default;` crashed the compiler - `TypesVisitor.VisitStateChangeStmt` dereferenced a null `ID()` because the grammar matches the default state as a keyword, not an identifier - so any script returning to its default state failed to compile, and the resident was told it was their fault. Plus the crash/error distinction on the owner-visible path. Four source files, two assemblies. **The claim that only two assemblies really changed was measured, not asserted.** 158 files differ in the region root and 92 in the grid because **every** assembly carries the nbgv stamp and the stamp moved; that is a rebuild, not a change. To tell them apart, every changed `.dll` in both changesets - **75 region, 44 grid** - was compared byte-for-byte against its live counterpart and its differing byte-RUNS counted. A stamp-only rebuild sits in a tight band of **84-95 runs** (version string, MVID, PDB checksum). **Exactly two files fall outside it, both in the region: `InWorldz.Phlox.dll` (size +512, so a real change on length alone) and `Phlox.ScriptEngine.dll` (16,876 runs, two orders of magnitude above the baseline). The grid has ZERO outliers**, which is right - it ships no Phlox. That matches the source diff exactly: `git diff 691a52bb9c..47a0f3a497 -- Source/` touches 4 files in those 2 projects and nothing else. Both landed byte-identical to the staged manifest (`InWorldz.Phlox.dll` `F785D464...`, `Phlox.ScriptEngine.dll` `78868D1A...`). **Worth writing down so it is not re-diagnosed:** `InWorldz.Phlox.dll` reports `ProductVersion 1.0.0.0`, not the nbgv stamp - it carries its own version and always has. Its identity is established by the SHA-256 re-hash, not by the stamp, and a future deploy should not read `1.0.0.0` as a stale file. **Step 7, the new-assembly rule: nothing to gate.** **0 new files** in either root, so no assembly is new to the live root and no enable gate needed quoting. Said explicitly because the rule exists precisely for the case where a deploy quietly adds a module - as MERGE-1 did with four. **The joltc gate, tenth run, passed.** Two `joltc*` in the staged publish, both **`16AF7638...43B5`**; all **855** staged region files and all **693** staged grid files hashed, none is `67BECFC7...0910`; **both live roots swept before and after the copy - 0 stock matches** across 2065 and 776 files. Guard exit 0 against staged and live, before and after. Live root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`** throughout. **Pre-copy probe clean on five checks:** no debugger or dump reader; no sim process by path or by name; nothing with a module under `D:\legiongrid`; the exclusive-open probe on 15 files including both Phlox assemblies; and both logs last written **05:57:2x**, the region log ending `[SHUTDOWN]: Shutdown processing on main thread complete` - a clean stop four minutes before the copy. **Preserved and byte-identical against the backup, 17 files checked, 0 changed:** the 6 region `config-include` files, `config/OpenSim.ini`, `config/regions/Regions.ini`, root `OpenSimDefaults.ini` and `OpenSim.ini.example` (unchanged by this build, so they did not enter the changeset this time), `joltc.dll`, live-only `WebRtcVoiceServiceModule.dll`, the 2 grid `*.example`, both `Robust*.ini.example.net10` and `config/Robust.ini`. File counts: region **2065** (of which **1000** are `assetcache`, the rotating runtime cache - not a deploy metric), grid **776**. Backups `D:\legiongrid\_backup\regionserver-20260909-0602` (2065) and `gridserver-20260909-0602` (776). **One process note, recorded because it nearly mattered:** the copy-set report script had been copied forward from an older stage and still pointed at `stage8`, so it printed the previous deploy's counts (150/144) for a moment. It is a reporting script only - `finish.ps1` computes the copy set itself from the changeset file it is handed - so the copy was never at risk, but a stale path in a carried-forward script is exactly the shape of error the procedure's re-hash step exists to catch. Corrected and re-run before the copy. | **Did it land is BEHAVIOURAL and is John's, named in the PHLOX-3a record BEFORE this deploy:** **(1)** a region start logs **no** `[PhloxCompile]` error for `4e51f068`; **(2)** `phlox status 4e51f068-...` shows an **interpreter**; **(3)** `lmap4` behaves as a lamp - touch cycles it high, medium, low, off. **(1) is the one that proves the fix and not merely the copy**, because the error fired on *every* start under 1.1.287, twice observed. Its absence is a real signal. **(3) is the one that proves the script runs rather than merely compiles**, and it exercises `state default;` on the last touch - the exact construct that crashed. **Also worth a glance on the next start, free with this deploy:** the crash/error distinction only shows itself when something throws, and nothing should now. If any `[PhloxCompile]` line appears carrying `internal compiler error:` and a stack, that is the new path working and a second defect to chase. **Still pending from earlier deploys:** a default New Script saying Hello, Avatar (PHLOX-2d); a fresh prim showing a touch cursor (PHLOX-2f); the airship failing to compile with its owner notified (PHLOX-2, never exercised against a real client); the three PHLOX-2g checks; and the two PROPS-1 checks. The S12 flip and the MERGE-1 checks are **closed** - see the startup-review row. **LIVE VERIFICATION, 2026-09-09 15:02 on `1.1.294-alpha+47a0f3a497` - all three named checks observed, and a fourth that was not asked for.** **(1)** `OnRezScript "4e51f068..."` at 15:02:55 and then **`INFO [PhloxLoader]: Compiled "1ee3b9b1-39c7-4f20-a5f3-6dbd79046218" (206ms)`** at 15:02:56.791. The asset that failed to compile on **every** start under 1.1.287 now compiles, and **no `[PhloxCompile]` line mentions `4e51f068` at all**. **(2)** `phlox status` shows an interpreter with `touch_start` and `state_entry` registered. **(3)** `lmap4` cycled **High -> Medium -> Low -> Off** across four touches. **The fourth touch is the `state default;` that crashed the compiler**, so the construct is exercised at runtime and not merely compiled - which is why (3) was on the list. **(4), the free one named in this row before the deploy:** a deliberate syntax error was reported in the **ordinary** form - `line 6:4 mismatched input '}' expecting ')'` followed by `1 syntax error(s)` (item `6db0eb8e`, 15:14:35) - and **no line carrying `internal compiler error:` was logged anywhere today, 0 occurrences**. So the crash form stayed silent while the error form worked, which is the whole of the PHLOX-3a step-4 distinction, demonstrated rather than asserted. **AND THE OWNER ALERT REACHED THE VIEWER.** John confirms the blue alert appeared for that deliberate error. **This closes an item open since PHLOX-2**, which shipped the owner-visible compile-error path and had **never once been exercised against a real client** - it has been on the pending list of every deploy since 1.1.275. It is now verified in world. **Two compile failures remain in the log and both are expected:** the airship `3eb0c62b` with `line 96:0 Symbol 'SetVehicleSettings()' already defined` (PHLOX-3 candidate (ii), verdict still needs the script compiled under YEngine), and John's own test script `6db0eb8e`, which is check (4). |
| 2026-09-09 | Corrections and live state - three items | `1.1.294` live | **(1) The AddMapTile range in the startup-review row is wrong: it is 4-15 s, not 6-15 s.** The log holds **4081 ms** and **4861 ms**. I recorded 6-15 s from the three values I happened to sample and said so at the time - the correction is that the sample was not the range. The finding is unchanged and still a perf-lane item; only the lower bound moves. **(2) Trusted Hypergrid is LIVE, and this is its first recorded state.** MERGE-1 brought PR #200's code in; the live `config/Robust.ini:305-306` carries `[TrustedHypergrid] Enabled = true`, and the **04:37 Robust start was the first run of that registry code on this grid**: `[MIGRATIONS]: Upgrading "TrustedGrid" to latest revision 1` then `Creating "TrustedGrid" at version 1` at 04:37:24. The grid identity loads on every start since - `[TRUSTED HG]: loaded grid identity from "TrustedHypergridSecret.ini", fingerprint "637ee209bfa39b0ac941e99b344e4693c165ff4f29817a1849f60ececdad0c67"` - and by 15:02 the tables report `already up to date at revision 1`. **`hgtrust list` is empty, and that is correct, not a fault.** Only *signed* peers are recorded. The one real hypergrid caller today, `bloodmoonpack.com` at 04:47, is **unsigned**, and unsigned callers are not recorded by design. So an operator turning this on sees an empty list and no evidence it is working, which is a poor first impression of a security feature. **Suggestion, explicitly not a defect:** one **DEBUG** line per unsigned inbound gatekeeper call, naming the caller, so hypergrid traffic is visible **before** any signed peer exists. That makes the empty list legible instead of ambiguous. Upstream's code, upstream's call - raise with Legion Hienrichs (#200's author) rather than patching here. **(3) The `Timer test (2)` prim - already recorded, no change needed.** John reported it deleted in the DOCS-2 brief and the startup-review row already says so. Repeated here only because it is the other half of the closed timer-cadence question: the 10 ms timer that produced every `Slow timeslice` line and the 09-07 "five ticks a second" reading is gone, and `llSetTimerEvent` still has no floor - that stands as PHLOX-3 candidate (iii). | Open: the unsigned-caller DEBUG line, with Legion Hienrichs. |
| 2026-09-09 | PHLOX-3b - `llSetTimerEvent` had no floor | `ef064538a0`, `516c6a9be8` | **A resident asked a region for 100 Hz and got it.** `llSetTimerEvent(0.01)` on Ebony, honoured exactly - `phlox status` read `timer: 10 ms` - and `Slow timeslice` warnings of **1-1.8 s** followed for two days across three regions until the prim was deleted. Nothing in the engine said no. **The correction matters more than the fix.** PHLOX-3 candidate (iii) claimed *"SL and InWorldz clamp"*. **Both halves are false**, and this time all three sources were quoted rather than remembered: the **SL wiki** documents no minimum for `llSetTimerEvent` and none on the `timer` event page either; **Halcyon** assigns `TimerInterval = (int)(sec * 1000)` with no clamp anywhere (`ExecutionScheduler.cs:1846-1853`); and the only real precedent is **upstream in this repo** - `LSL_Api.cs:4005-4011` clamps at `m_MinTimerInterval`, shipped as `[YEngine] MinTimerInterval = 0.1` and **live on this grid**. Until now the same call behaved differently depending on which engine ran the script. Because neither named source supported any constant, the floor was **John's choice, not a derivation**: config-driven, default 0.1. **Shipped:** `MinTimerInterval` in `[InWorldz.Phlox]`, same key and default as the other engine so one number serves both, `0` disables it, value logged at startup. One clamp **at the API entry, not the scheduler**, so `phlox status` reads back what was applied. **Zero and negative untouched** - 0.0 still stops the timer per the wiki, negative still arms nothing; a floor that resurrected a stopped timer would be worse than the defect. One **DEBUG** line per script instance, not per tick. **Kept for the record:** `(int)(sec * 1000)` truncates, so `llSetTimerEvent(0.0001)` previously meant **timer off**, not *fast*. The clamp changes what such a script does, not only its rate. Suite **60 -> 66**, solution 0 errors. | **Not deployed. Did-it-land, named now:** a fresh prim calling `llSetTimerEvent(0.01)` shows **`timer: 100 ms`** in `phlox status`; **one** DEBUG clamp line for it in the log; **no `Slow timeslice`** for that script. **Filed, not fixed - and deliberately not called flakiness:** the harness-based tests fail intermittently under xUnit's collection parallelism, **2 failures in 8 full runs**, on two different tests, each passing in isolation, and **3/3 green with parallelism off**. PHLOX-3b added six harness tests and so raised exposure, but one of the two failures is in a test that predates it. **The last intermittent failure in this suite was a real race** - PHLOX-2f's `TimerCadenceTests`, which was the unguarded `AsyncCommandManager.StartThread`, twice dismissed as flakiness with a bound widened to hide it. New PHLOX-3 candidate (iv), with the rule written down: do not raise a bound and do not disable parallelism in the csproj to make it green. |
| 2026-09-09 | PHLOX-3b deploy | `98db4a3bf6` (deployed) | **Both roots, servers down throughout and not started.** `1.1.294-alpha+47a0f3a497` -> **`1.1.300-alpha+98db4a3bf6`**. Second deploy run from `D:\legiongrid\_ops\procedures\DEPLOY-PROCEDURE.md`, and the first to run **8a** as written. Region `-c Release -r win-x64 --self-contained false` (**855** published, **158** differed, **0** new, **152** copied after the preserve rule); grid `-c Release --self-contained false`, **no `-r`** (**693** published, **92** differed, **0** new, **90** copied). Full copy set re-hashed in the live root against the staged manifest: 152 and 90, **no mismatch**, one clean pass. **What shipped: PHLOX-3b, the `llSetTimerEvent` floor.** `MinTimerInterval` in `[InWorldz.Phlox]`, default **0.1 s** - the same key and default the other engine on this grid already uses - clamped at the API entry, zero and negative untouched, one DEBUG line per script when a value is raised. Two source files, one assembly. **8a worked exactly as it is supposed to: the prediction was made first, in writing, and the measurement matched it.** From `git diff 47a0f3a497..98db4a3bf6 -- Source/` - two files, both in `Phlox.ScriptEngine` - the predicted outlier set was **region: `Phlox.ScriptEngine.dll` only; grid: none**. Measured across **75 changed region DLLs and 44 grid DLLs**: region **one** outlier, `Phlox.ScriptEngine.dll` (**size +1024**); grid **zero**. **Predicted set == measured set**, so no STOP. **And it produced a corroboration worth keeping.** `InWorldz.Phlox.dll` was last deploy's outlier (+512 bytes, the PHLOX-3a compiler fix). This time **it has the lowest run count in the entire tree - 6** - because no source in that project changed and, carrying no nbgv stamp (`ProductVersion 1.0.0.0`, recorded last deploy), it has no version string to move either: MVID and PDB checksum are the whole of its difference. The stamp-only band therefore reads **6-103 runs** this time, not the 84-95 the procedure records from one deploy; the band is a per-tree observation, not a constant, and the outlier test survives that because a real change is orders of magnitude away, not a few runs. **8b, the carried-forward-script rule, applied and passed:** every stage path was re-pointed to `stage11` and the copy-set counts read back against step 2's changeset output (158-6=152, 92-2=90) before the copy, which is the check the 05:58 slip on the previous deploy exists to prevent. **Step 7, the new-assembly rule: nothing to gate.** **0 new files** in either root. **The joltc gate, eleventh run, passed.** Two `joltc*` in the staged publish, both **`16AF7638...43B5`**; all 855 staged region and 693 staged grid files hashed, none is `67BECFC7...0910`; **both live roots swept before and after the copy - 0 stock matches** across 2078 and 776 files. Guard exit 0 against staged and live, before and after. Live root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`** throughout. **Pre-copy probe clean on five checks:** no debugger or dump reader; no sim process by path or by name; nothing with a module under `D:\legiongrid`; the exclusive-open probe on 15 files; both logs last written **15:33:3x**, the region log ending `[SHUTDOWN]: Shutdown processing on main thread complete` - a clean stop 24 minutes before the copy. **Preserved and byte-identical against the backup, 17 files checked, 0 changed**, `config/OpenSim.ini` and `config/Robust.ini` among them. `Phlox.ScriptEngine.dll` landed byte-identical to staged (`0D042C3A...`). File counts: region **2078** (of which **1012** `assetcache`), grid **776**. Backups `D:\legiongrid\_backup\regionserver-20260909-1557` (2078) and `gridserver-20260909-1557` (776). | **Did it land is BEHAVIOURAL and is John's, named in the PHLOX-3b record BEFORE this deploy:** **(1)** a fresh prim calling `llSetTimerEvent(0.01)` shows **`timer: 100 ms`** in `phlox status`; **(2)** **one** DEBUG clamp line in the log for that item, naming requested and applied; **(3)** **no `Slow timeslice`** for that script. **(2) is the discriminating one.** (1) alone could be a coincidence of reading; the DEBUG line proves the clamp ran and names what it changed. And **one** line rather than a stream is the second half of the check - a 10 ms timer that logged per tick would write a hundred lines a second, which is why the log is per script instance. **Also readable on the next start, free with this deploy:** `[PhloxEngine]: MinTimerInterval = 0.1s` at load, so what a region is applying is legible without inference. **Still pending from earlier deploys:** a default New Script saying Hello, Avatar (PHLOX-2d); a fresh prim showing a touch cursor (PHLOX-2f); the three PHLOX-2g checks; and the two PROPS-1 checks. **The PHLOX-2 owner-notification check is CLOSED** - verified in world on 1.1.294, see the PHLOX-3a deploy row. |
| 2026-09-09 | PHLOX-4 deploy | `98ca84915b` (deployed) | **Both roots, servers down throughout and not started.** `1.1.300-alpha+98db4a3bf6` -> **`1.1.307-alpha+98ca84915b`**. Third run of the written procedure. Region `-c Release -r win-x64 --self-contained false` (**855** published, **158** differed, **0** new, **152** copied after the preserve rule); grid `-c Release --self-contained false`, **no `-r`** (**693** published, **92** differed, **0** new, **90** copied). Full copy set re-hashed in the live root against the staged manifest: 152 and 90, **no mismatch**, one clean pass. **What shipped: PHLOX-4, 4b and 4c together - restored scripts resume where they stopped, and the engine has one clock.** A script saved Sleeping is re-armed from its restored `NextWakeup`; saved Running goes back on the run queue; saved Waiting with events on its own queue has them delivered; saved mid-syscall is resumed with that function's default return value, from a new `[ProtoMember(22)] LastSyscallIndex` that pre-existing rows load as -1. Every tick source in both schedulers, the serializer and the API is `InWorldz.Phlox.Util.Clock` over `Environment.TickCount64` - the 30-bit masked uptime tick and the P/Invoke are gone. Six source files, two assemblies. **8a: predicted first, then measured, and they matched.** From `git diff 98db4a3bf6..98ca84915b -- Source/` - three files in `InWorldz.Phlox`, three in `Phlox.ScriptEngine` - the predicted outlier set was **region: `InWorldz.Phlox.dll` and `Phlox.ScriptEngine.dll`; grid: none.** Measured across **75 changed region DLLs and 44 grid DLLs**: region **two** outliers, `InWorldz.Phlox.dll` (size +512) and `Phlox.ScriptEngine.dll` (size +1024); grid **zero**. Stamp-only band this time **73-82 runs**. Both landed byte-identical to staged (`87576B7F...`, `1D0C93C9...`). **8b applied:** stage path re-pointed to `stage12`, counts read back against the changeset (158-6=152, 92-2=90). And one more instance of exactly the hazard 8b names: the chained post-copy step broke on a **trailing slash from `ls -1t`** feeding a `sed` expression, so the verify and the after-copy guard did not run in the chain and had to be run separately - they passed, but the chain's own `guard exit: 1` line was the shell's, not the guard's. Recorded so the next person does not read that line as a gate failure. **Step 7, the new-assembly rule: nothing to gate** - 0 new files in either root. **The joltc gate, twelfth run, passed.** Two `joltc*` in the staged publish, both **`16AF7638...43B5`**; all 855 + 693 staged files hashed, none stock; **both live roots swept before and after the copy - 0 stock matches** across 2090 and 776 files. Guard exit 0 against staged and live, before and after. Live root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`** throughout. **Pre-copy probe clean on five checks;** both logs last written **16:33:1x**, the region log ending `[SHUTDOWN]: Shutdown processing on main thread complete` - a clean stop 47 minutes before the copy. **Preserved and byte-identical against the backup, 17 files, 0 changed.** File counts: region **2090** (of which **1023** `assetcache`), grid **776**. Backups `D:\legiongrid\_backup\regionserver-20260909-1720` (2090) and `gridserver-20260909-1720` (776). **Baseline for the restart, taken from the LAST start on 1.1.300 (16:01:34) so the counts are comparable:** `Starting from disk cache` **18**, `Starting shared` **1**, `Compiled` **1**, `Script load FAILED` **0**, `Compilation failed` **2** (the airship `3eb0c62b` and John's deliberate `6db0eb8e`), `Cache schema version` **0**, `[PhloxState]` warn/error **0**, timer-related lines in the first 60 s **3**. | **Did it land is from the LOG after John's restart, no in-world step, counts not adjectives:** **(1)** every `[PhloxLoader]` line is `Compiled` / `Starting from disk cache` / `Starting shared`; **0** `Script load FAILED`; **0** `[PhloxState]` errors; `Starting from disk cache` **= 18** - same number means every script came back. **(2)** `Cache schema version on disk` fires **0** times - tag 22 is state, not bytecode; if it fires, report it. **(3)** any `resuming with that call's default return value` line names the function; any `saved mid-syscall with no recorded function` warning is a pre-tag-22 row and must appear on **this restart only**. **(4)** within 60 s of start, at least one `[PhloxExe]` timer fire from a known timer script - the TickCount64 basis proved on the live process. The verify script is `D:\legiongrid\_ops\procedures\verify-phlox4-restart.sh`; run it once the region is up. Handoff written to `_ops\handoffs\SESSION-HANDOFF-ssb-ais-2026-09-09.md`. |
| 2026-09-09 | PHLOX-5/6/7a/7b deploy | `88725d7a16` (deployed) | **Both roots, servers down throughout and not started.** `1.1.307-alpha+98ca84915b` -> **`1.1.312-alpha+88725d7a16`**. Fourth run of the written procedure. Region `-c Release -r win-x64 --self-contained false` (**855** published, **158** differed, **0** new, **152** copied after the preserve rule); grid `-c Release --self-contained false`, **no `-r`** (**693** published, **92** differed, **0** new, **90** copied). Full copy set re-hashed in the live root against the staged manifest: 152 and 90, **no mismatch**, one clean pass. **What shipped: four sessions in one deploy.** PHLOX-5 - six built-ins under their SL names and arities (`llsRGB2Linear`, `llListSortStrided`, `llSHA256String(string)`, `llTargetedEmail(3)`, `llUpdateKeyValue(4)`, `llDerezObject(2)`), older forms kept, indices 677-682 appended. PHLOX-6 - five SL events recognised (`path_update`, `on_damage`, `final_damage`, `on_death`, `game_control`), two delivered (on_death from `OnAvatarKilled`, path_update beside bot_update), four new mask bits. PHLOX-7a - `llCollisionFilter`, object animations, `llGetStartString` (with `REZ_PARAM_STRING` now parsed and 21 `REZ_*` constants that did not exist), `llXorBase64Strings`. PHLOX-7b - sit flags, `llMinEventDelay` as a real floor, the memory profiler, `llRequestSimulatorData` for other regions. Thirteen source files, four assemblies. **8a: predicted first, in writing, then measured - and they matched on both roots.** From `git diff 98ca84915b..88725d7a16 -- Source/` the prediction was **region: `InWorldz.Phlox.dll`, `Phlox.ScriptEngine.dll`, `OpenSim.Region.Framework.dll` (mask bits, `SitFlagsStored`), `OpenSim.Region.OptionalModules.dll` (BotManager); grid: `OpenSim.Region.Framework.dll` only**, since the grid publish ships Region.Framework and none of the other three. Measured across **75 changed region DLLs and 44 grid DLLs**, stamp-only band 84-95 runs: region **four** outliers - `InWorldz.Phlox.dll` +6144, `Phlox.ScriptEngine.dll` +7168, `OpenSim.Region.Framework.dll` +512, `OpenSim.Region.OptionalModules.dll` 102,589 runs; grid **one** - `OpenSim.Region.Framework.dll` +512. All five landed byte-identical to staged. **The cache-schema gate the brief named as a possible STOP - settled from the code before the copy, not a stop.** `CACHE_SCHEMA_VERSION = 2` (`PhloxScriptLoader.cs:37-42`) covers the *serialized bytecode* format - \"bump any time SerializedScript / SerializedLSLPrimitive schema changes\" - not the dispatch table. Cached bytecode carries each syscall's `TableIndex` baked in (`Interpreter.Actions.cs:1275`), and PHLOX-5/6 only **appended** 677-682 and new event indices; nothing moved, and `DispatchIndexGuardTests` pins that. The new state tags 22-25 are on the SQLite state row, which protobuf tolerates. **No bump required, none made.** Expectation for the restart, written here: `Cache schema version on disk` **0** lines, and the recompile the brief anticipated should **not** happen - disk-cache 19 + Compiled 1, as at 18:12. **8b applied:** stage path re-pointed to `stage13`, copy-set counts read back against the changeset (158-6=152, 92-2=90); the post-copy chain used a literal backup stamp this time, so the trailing-slash trap from the 1.1.307 run did not recur. **Step 7: nothing to gate** - 0 new files in either root. **The joltc gate, thirteenth run, passed.** Two `joltc*` in the staged publish, both **`16AF7638...43B5`**; all 855 + 693 staged files hashed, none stock; **both live roots swept before and after the copy - 0 stock matches** across 2111 and 776 files. Guard exit 0 against staged and live, before and after. Live root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`** throughout. **Pre-copy probe clean on five checks;** both logs last written **20:12:3x**, the region log ending `[SHUTDOWN]: Shutdown processing on main thread complete` - a clean stop four minutes before the copy. **Preserved and byte-identical against the backup, 17 files, 0 changed.** File counts: region **2111** (of which **1042** `assetcache`), grid **776**. Backups `D:\legiongrid\_backup\regionserver-20260909-2016` (2111) and `gridserver-20260909-2016` (776). **Baseline for the restart, from the LAST start on 1.1.307 (18:12:18), scoped to that start:** `Starting from disk cache` **19**, `Starting shared` **2** (the brief said 1; the log says 2), `Compiled` **1**, `Script load FAILED` **0**, `Compilation failed` **1** (the airship `3eb0c62b` only - John's `6db0eb8e` test script is gone), `Cache schema version` **0**, `Restored state for` **18**, `State restore failed` **0**, `[PhloxState]` warn/error **0**. | **Did it land is from the LOG after John's restart, counts not adjectives - `D:\legiongrid\_ops\procedures\verify-phlox5-7-restart.sh`:** **(1)** loader pair disk-cache + Compiled **= 20** with FAILED 0 and PhloxState 0 - the pair is the check, not the split; **(2)** `Cache schema version on disk` **0**; **(3)** `Restored state for` **18**, `State restore failed` **0**, and **0** protobuf / deserialization lines - tags 22-25 are the risk; **(4)** **0** `[PhloxCompile]` errors outside the allowed airship (and the test script if still rezzed). **(3) is the sharp one:** three new fields on every restored script's state row, and the first thing a bad tag does is fail a restore silently into a fresh start. **Then the in-world checks each session named:** `llSHA256String(\"abc\")` says `ba7816bf...` (PHLOX-5); a prim with all five handlers compiles and `phlox status` lists them (PHLOX-6); a prim whose script sets `llCollisionFilter` ignores the named object (7a); two fast touches under `llMinEventDelay(1.0)` answer a second apart (7b). **Still pending from earlier deploys:** a default New Script saying Hello, Avatar (PHLOX-2d); a fresh prim showing a touch cursor (PHLOX-2f); the three PHLOX-2g checks; the two PROPS-1 checks; PHLOX-3b's `timer: 100 ms` and one DEBUG clamp line; PHLOX-4's verify script if it was not run against 1.1.307. |
| 2026-09-10 | PHLOX-8/9 deploy | `a87d6bf84b` (deployed) | **Both roots, servers down throughout and not started.** `1.1.312-alpha+88725d7a16` -> **`1.1.315-alpha+a87d6bf84b`**, copied 04:14. Fifth run of the written procedure. Region `-c Release -r win-x64 --self-contained false` (**855** published, **158** differed, **0** new, **152** copied after the preserve rule); grid `-c Release --self-contained false`, **no `-r`** (**693** published, **92** differed, **0** new, **90** copied). Full copy set re-hashed in the live root against the staged manifest: 152 and 90, **no mismatch**, one clean pass. **What shipped: two sessions.** PHLOX-8 - the constants audit: 308 upstream names added (296 int, 12 string), `TOUCH_INVALID_FACE` 0x7FFFFFFF -> -1 (wiki 0xFFFFFFFF), the other three B hits settled as not-defects, `Docs/audit/phlox-constants-audit.py` re-runs to A 0 / B 0. PHLOX-9 - run-time and API errors on **DEBUG_CHANNEL** instead of channel 0 (every `ShoutError` caller moved together, ~100 API sites included - the SL behaviour), `quaternion` as a type, `<<=`/`>>=` removed, `list != list` is the length difference; parser **regenerated** with ANTLR 4.13.1 through the new `grammar/buildgrammar4.sh` (token numbers shifted). Two projects, two assemblies. **8a: predicted first, in writing, then measured - matched on both roots.** From `git diff 88725d7a16..a87d6bf84b -- Source/`: 24 files in `InWorldz.Phlox`, 1 in `Phlox.ScriptEngine` (`LSLSystemAPI.cs`), nothing else -> prediction **region: `InWorldz.Phlox.dll`, `Phlox.ScriptEngine.dll`; grid: none** (the grid publish ships neither - a `phlox` name search of the grid root is empty). Measured across **75 changed region DLLs and 44 grid DLLs**, stamp-only band **80-89 runs** this time (84-95 before; the band moved down a little, still a tight band at exact length): region **two** outliers - `InWorldz.Phlox.dll` **+22,528 bytes, 68,020 runs** (308 constants + the regenerated lexer/parser), `Phlox.ScriptEngine.dll` **+0 bytes, 30,648 runs** (a one-constant change that kept the length); grid **zero**. Both landed byte-identical to staged. **The cache question, settled from the code before the copy.** PHLOX-9 regenerated the parser with shifted token numbers. The bytecode cache is compiler *output*: `.plx` files are `SerializedScript` protobuf under `CACHE_DIR/<prefix>/<assetId>.plx` (`PhloxScriptLoader.cs:561-565`), opened by `TryStartFromDiskCache` (`:336-372`) with no check beyond deserialize-and-convert, and gated only by `CACHE_SCHEMA_VERSION = 2` in the version file (`:42`, `:110-123`). **Nothing in the key or the blob derives from parser tokens** - tokens exist only inside `LSLLexer`/`LSLParser` at compile time. **Expectation for the restart, written here: loader pair 18 + 1 = 19, no recompile**, `Cache schema version` 0. (The baseline moved: the 20:29 start of 1.1.312 loaded 18 from disk cache + 1 Compiled and restored 17, one script fewer than the 18:12 start of 1.1.307.) The one cache caveat is PHLOX-8's, already recorded: a cached script that used `TOUCH_INVALID_FACE` carries the old `iconst` until it is recompiled. **8b applied:** stage path `stage14` passed to every script; copy-set counts read back against the changeset (158-6=152, 92-2=90); the post-copy script took the backup stamp `20260910-0413` as a substitution from the backup script's own output, not a literal. **Step 7: nothing to gate** - 0 new files in either root. **The joltc gate, fourteenth run, passed.** Two `joltc*` in the staged publish, both **`16AF7638...43B5`**; all 855 + 693 staged files hashed, none stock; **both live roots swept before and after the copy - 0 stock matches** across 2122 and 777 files. Guard exit 0 against staged and live, before and after. Live root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`** throughout. **Pre-copy probe clean on five checks** (0 debuggers, 0 sim processes by name or path, 0 processes holding a module under `D:\legiongrid`, 15 live files opened exclusively and released, logs last written **04:09:5x**, the region log ending `[SHUTDOWN]: Shutdown processing on main thread complete` - a clean stop four minutes before the publish). Servers-down gate re-run inside the backup script. **Preserved and byte-identical against the backup, 17 files, 0 changed**, `config/OpenSim.ini` among them. File counts: region **2122** (of which **1051** `assetcache`), grid **777**. Backups `D:\legiongrid\_backup\regionserver-20260910-0413` (2122) and `gridserver-20260910-0413` (777). **Origin corrected:** `origin/feature/ais-v3` is at **`85c8504de2`** (John's pushes: `98ca84915b`, then `85c8504de2`); local is ahead by exactly the two session commits `524c95b117` (PHLOX-8) and `a87d6bf84b` (PHLOX-9) plus this row. The 09-09 handoff's `691a52bb9c` was stale. | **Did it land - named before the copy, from the LOG after John's restart via `D:\legiongrid\_ops\procedures\verify-phlox8-9-restart.sh`:** **(1)** loader pair disk-cache + Compiled **= 19** with FAILED 0 and PhloxState 0 - the pair is the check; **(2)** `Cache schema version on disk` **0**; **(3)** `Restored state for` **17**, `State restore failed` **0**, **0** protobuf / deserialization lines; **(4)** **0** `[PhloxCompile]` errors outside the airship `3eb0c62b`; **(5)** `Script error` lines on channel 0 **0** - with the caveat, printed by the script, that ChatModule does not log chat text, so the log cannot contradict item 1 and the in-world check below is the real one. **In-world, John's:** a prim with `integer z = 0; llSay(0, (string)(1/z));` - the **owner sees a script-error line and a second avatar nearby does NOT see it in local chat** (PHLOX-9 item 1; before this deploy everyone in 100 m read it); `quaternion q = <0,0,0,1>; llSetRot(q);` compiles (item 2); `llSay(0, (string)TOUCH_INVALID_FACE)` says **-1** and `llSay(0, (string)PRIM_PROJECTOR)` says **42** (PHLOX-8: the B fix and one added A name). **Still pending from earlier deploys:** PHLOX-5/6/7 in-world checks (`llSHA256String(\"abc\")` = `ba7816bf...`, five-handler prim, `llCollisionFilter`, `llMinEventDelay`), PHLOX-3b's `timer: 100 ms`, PHLOX-2d/2f/2g, PROPS-1. |
| 2026-09-10 | PHLOX-9b/10 deploy | `ffc8be19b3` (deployed) | **Both roots, servers down throughout and not started.** `1.1.315-alpha+a87d6bf84b` -> **`1.1.319-alpha+ffc8be19b3`**, copied 05:42. Sixth run of the written procedure. Region `-c Release -r win-x64 --self-contained false` (**855** published, **158** differed, **0** new, **152** copied after the preserve rule); grid `-c Release --self-contained false`, **no `-r`** (**693** published, **92** differed, **0** new, **90** copied). Full copy set re-hashed in the live root against the staged manifest: 152 and 90, **no mismatch**, one clean pass. **What shipped: three commits.** PHLOX-9b (`4271ca3f9e`) - object chat on DEBUG_CHANNEL is delivered to the object's owner only, in `ChatModule`, so the outcome no longer depends on the viewer. PHLOX-10 PART 1 (`9bef63058a`) - one door for avatar damage, `ScenePresence.ApplyDamage` over `DamageEntry`, the collision frame batched; PART 2 (`ffc8be19b3`) - `on_damage` (synchronous, adjustable, waited on up to 500 ms) / `final_damage` / `on_death` in SL order, `llDetectedDamage` a list, `llAdjustDamage(number, new_damage)`, `llDamage` through the door, sixteen `DAMAGE_TYPE_*`. Five projects, five region assemblies. **8a: predicted first, in writing, then measured - matched on both roots.** From `git diff a87d6bf84b..ffc8be19b3 -- Source/`: `InWorldz.Phlox` 6 files, `OpenSim.Region.Framework` 3, `Phlox.ScriptEngine` 3, `OpenSim.Region.CoreModules` 1, `OpenSim.Region.ScriptEngine.Shared` 1 -> prediction **region: those five; grid: `OpenSim.Region.Framework.dll` only**, because of the five only Region.Framework exists in the grid root (checked file by file). Measured across **75 changed region DLLs and 44 grid DLLs**, stamp-only band **85-96 runs**: region **five** outliers - `OpenSim.Region.CoreModules.dll` +512 / 126,322 runs, `InWorldz.Phlox.dll` +2048 / 89,999, `OpenSim.Region.Framework.dll` +2560 / 75,373, `OpenSim.Region.ScriptEngine.Shared.dll` +0 / 62,406, `Phlox.ScriptEngine.dll` +4096 / 17,805; grid **one** - `OpenSim.Region.Framework.dll` +2560 / 75,373, byte-identical to the region's. All six landed byte-identical to staged. **The state-row question, settled from the code and a test before the copy.** `DetectVariables` gained ProtoMembers 27-29 (`Damage`, `DamageType`, `OriginalDamage`) plus a `[ProtoIgnore]` callback - and it **is** on the persisted row: every queued or running event is saved as `SerializedPostedEvent` whose `DetectVars` (tag 3) are `DetectVariables` (`SerializedPostedEvent.cs:19-20`), inside `SerializedRuntimeState.EventQueue` / `RunningEvent`. Every row on the grid predates tag 27. Proven the way tag 22 was: `DetectVariablesRowTests` serialises a detect record shaped exactly as the old code wrote it (tags 3, 5, 6, 9 only) inside a pre-27 posted event and deserialises it as the current type - old fields intact, the three new ones 0, the callback null; and the three round-trip while the callback does not. **2/2, run before the copy.** Expectation for the restart, written here: loader pair **20 + 1 = 21**, `Cache schema version` **0** (nothing in this deploy touches cached bytecode - two functions changed *type* at their old indices, and no cached script calls either; both were stubs), `Restored state for` **18**, 0 restore failures, 0 protobuf lines. The baseline moved again: the 04:20:49 start of 1.1.315 loaded 20 from disk cache + 1 compiled and restored 18 (the two new test prims), and stopped cleanly 05:37:21. **8b applied:** stage path `stage15` passed to every script; copy-set counts read back against the changeset (158-6=152, 92-2=90); the post-copy script took the backup stamp `20260910-0541` from the backup script's output. **Step 7: nothing to gate** - 0 new files in either root. **The joltc gate, fifteenth run, passed.** Two `joltc*` in the staged publish, both **`16AF7638...43B5`**; all 855 + 693 staged files hashed, none stock; **both live roots swept before and after the copy - 0 stock matches** across 2139 and 777 files. Guard exit 0 against staged and live, before and after. Live root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`** throughout. **Pre-copy probe clean on five checks** (0 debuggers, 0 sim processes by name or path, 0 processes holding a module under `D:\legiongrid`, 15 live files opened exclusively and released, logs last written **05:37:2x** - John stopped 1.1.315 a minute before the brief; the region log ends `Hosting stopped` after the clean `[SHUTDOWN]`). Servers-down gate re-run inside the backup script. **Preserved and byte-identical against the backup, 17 files, 0 changed**, `config/OpenSim.ini` among them. File counts: region **2139** (of which **1067** `assetcache`), grid **777**. Backups `D:\legiongrid\_backup\regionserver-20260910-0541` (2139) and `gridserver-20260910-0541` (777). **Origin:** `origin/feature/ais-v3` is at `58c787cb8f` (John pushed the 1.1.315 record); local ahead by PHLOX-9b, PHLOX-10 PART 1, PART 2 and this row. | **Did it land - named before the copy, from the LOG after John's restart via `D:\legiongrid\_ops\procedures\verify-phlox9b-10-restart.sh`:** **(1)** loader pair disk-cache + Compiled **= 21** with FAILED 0 and PhloxState 0; **(2)** `Cache schema version on disk` **0**; **(3)** `Restored state for` **18**, `State restore failed` **0**, **0** protobuf / deserialization lines - tags 27-29 sit inside every saved event's detect records, so this is the sharp one; **(4)** **0** `[PhloxCompile]` errors outside the airship; **(5)** **0** `on_damage ... still running after 500 ms` timeouts - trivially at load, and **again after the in-world damage check**. **In-world, John's:** PHLOX-9b - the 1.1.315 prim (`integer z = 0; llSay(0, (string)(1/z));`) with Truly nearby: **Legion gets the script-error line, Truly gets no box** (on 1.1.315 Truly did). PHLOX-10 - Legion wearing an attachment whose script says the values in `on_damage` and `final_damage`, Truly's prim calling `llDamage(Legion, 10, DAMAGE_TYPE_GENERIC)`: **Legion sees both lines and Health drops by 10**; then re-run the verify script and section 5 must still read 0. **Still pending from earlier deploys:** PHLOX-8/9's `quaternion` / `TOUCH_INVALID_FACE` / `PRIM_PROJECTOR` checks if not run on 1.1.315; PHLOX-5/6/7's; PHLOX-3b's `timer: 100 ms`; PHLOX-2d/2f/2g; PROPS-1. |
| 2026-09-10 | PHLOX-11 deploy | `788d52ed1f` (deployed) | **Both roots, servers down throughout and not started.** `1.1.319-alpha+ffc8be19b3` -> **`1.1.321-alpha+788d52ed1f`**, copied 15:21. Seventh run of the written procedure. Region `-c Release -r win-x64 --self-contained false` (**855** published, **158** differed, **0** new, **152** copied after the preserve rule); grid `-c Release --self-contained false`, **no `-r`** (**693** published, **92** differed, **0** new, **90** copied). Full copy set re-hashed in the live root against the staged manifest: 152 and 90, **no mismatch**, one clean pass. **What shipped: PHLOX-11** (`788d52ed1f`) - `script_state.db`: WAL + synchronous=NORMAL set once per manager, busy_timeout 5000 on every connection, one process-wide writer lock with a persistent writer and reader per engine (the WAL is never torn down between calls), and a load that fails twice **holds the script Disabled** (`phlox status`: `HELD: StateLoadFailed`) with its row untouched instead of a fresh state_entry saving over it. One project pair, two region assemblies. **8a: predicted first, in writing, then measured - matched on both roots.** From `git diff ffc8be19b3..788d52ed1f -- Source/`: `Phlox.ScriptEngine` 3 files (StateManager, the scheduler's hold path, the `phlox status` line - which lives in `PhloxEngine.cs`, so still that assembly) and `InWorldz.Phlox` 1 (`RuntimeState.LocalDisableFlag.StateLoadFailed`) -> prediction **region: `Phlox.ScriptEngine.dll` and `InWorldz.Phlox.dll`; grid: none** (neither is in the grid root). Measured across **75 changed region DLLs and 44 grid DLLs**, stamp-only band **87-100 runs**: region **two** outliers - `InWorldz.Phlox.dll` **+0 bytes, 64,274 runs** (one enum member), `Phlox.ScriptEngine.dll` **+3072 bytes, 28,444 runs**; grid **zero**. Both landed byte-identical to staged. **The journal-mode question, answered before the copy - and the premise corrected.** The live `script_state.db` is **already in WAL mode**: header bytes 18/19 read **2/2** (read-only check; `fileformat2.html`: 1 for rollback journalling, 2 for WAL), because the pre-PHLOX-11 code issued `PRAGMA journal_mode=WAL` on every open, and WAL is persistent (`wal.html`: a process that sets WAL mode and reopens the database gets it back in WAL mode). So the first open of 1.1.321 rewrites nothing; the `-wal`/`-shm` sidecars are absent now only because the last connection's clean close checkpointed and removed them. Answered anyway, for the operators' guide: **(a)** converting an existing database is safe - the only change to the file is bytes 18/19 going from 1 to 2 (`wal.html`, 'Converting to WAL'; reversible with `journal_mode=DELETE`); **(b)** a hot `script_state.db-journal` from an unclean stop is played back the next time SQLite opens the file, before the conversion or anything else (`fileformat2.html`, 'Hot journals'; the pragma runs after open); **(c)** WAL needs all processes on one host sharing memory - it does not work over a network filesystem, and a VFS without shared-memory support fails the conversion and leaves the prior mode (`wal.html`); `PRAGMA locking_mode=EXCLUSIVE` before the first WAL access is the single-connection way round it. Legion Grid's state dir is local NTFS; a Linux operator putting `ScriptEngines/` on NFS or a bind-mounted share would lose WAL, not data. **8b applied:** stage path `stage16` passed to every script; copy-set counts read back against the changeset (158-6=152, 92-2=90); post-copy took the backup stamp `20260910-1521` from the backup output. **Step 7: nothing to gate** - 0 new files in either root. The state DB is not in the publish and was **not touched by the copy** (368,640 bytes, sha256 `c64e16a1...`, header 2/2 before and after). **The joltc gate, sixteenth run, passed.** Two `joltc*` in the staged publish, both **`16AF7638...43B5`**; all 855 + 693 staged files hashed, none stock; **both live roots swept before and after the copy - 0 stock matches** across 2163 and 777 files. Guard exit 0 against staged and live, before and after. Live root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`** throughout. **Pre-copy probe clean on five checks** (0 debuggers, 0 sim processes by name or path, 0 processes holding a module under `D:\legiongrid`, 15 live files opened exclusively and released - `script_state.db` among them this time - logs last written **06:10:3x/4x** after `Hosting stopped`; the grid had been down nine hours). Servers-down gate re-run inside the backup script. **Preserved and byte-identical against the backup, 17 files, 0 changed**, `config/OpenSim.ini` among them. File counts: region **2163** (of which **1088** `assetcache`), grid **777**. Backups `D:\legiongrid\_backup\regionserver-20260910-1521` (2163) and `gridserver-20260910-1521` (777). **Baseline for the restart, from the 05:47:01 start of 1.1.319:** disk cache 20, shared 2, Compiled 3, restored **16**, `database is locked` **4**, `[PhloxState]` WARN/ERROR **3** (two locked, one `Collection was modified`), held 0. **Origin:** `origin/feature/ais-v3` is at `831e97bb28`; local ahead by PHLOX-11 and this row. | **Did it land - named before the copy, from the LOG via `D:\legiongrid\_ops\procedures\verify-phlox11-restart.sh`, run after THIS start and AGAIN after the next:** **(1)** `[PhloxState]` WARN/ERROR **0** and `database is locked` **0** on **two consecutive restarts** (baseline 3 and 4); **(2)** `script_state.db-wal` and `-shm` present beside the DB while the process runs (section 8), header 18/19 still 2/2; **(3)** `State load FAILED` / `Holding ... DISABLED` **0**, `Restored state for` **>= 17** - the row that failed to load on 1.1.319 loads now; loader pair 20 + 3 unchanged, schema 0, airship the only compile error. **Informational:** `Collection was modified` count (a dropped save, PHLOX-11 candidate). **Still pending from earlier deploys:** PHLOX-9b/10's two in-world checks on 1.1.319 (Truly gets no box; llDamage lines and Health -10) if not run; PHLOX-8/9's; PHLOX-5/6/7's; PHLOX-3b's `timer: 100 ms`; PHLOX-2d/2f/2g; PROPS-1. |
| 2026-09-10 | PHLOX-12/13 deploy - **STOPPED at the servers-down gate** | `ab6d6b86cc` (staged, NOT copied) | **The grid was running.** At 16:47 the five-check probe found `OpenSim.Server.RegionServer` and `OpenSim.Server.GridServer` alive (the 15:30 start of 1.1.321), 13 of 15 probe files locked, the region log written that second. Rule 1 (never start or stop a server) and rule 2 (a failed gate stops the deploy) both apply: **nothing was copied, no backup was taken, both live roots are untouched at `1.1.321-alpha+788d52ed1f`.** Everything that is read-only or staged was done so the resume is a copy: **Staged** `stage17`: region `-c Release -r win-x64 --self-contained false` (**855** published, **158** differ from live, **0** new, copy set **152**); grid `-c Release --self-contained false`, **no `-r`** (**693**, **92**, **0**, **90**). Staged stamp **`1.1.326-alpha+ab6d6b86cc`**. Manifests and changesets written beside the stage. **8a: predicted first, then measured against the running root - matched.** From `git diff 788d52ed1f..ab6d6b86cc -- Source/`: `InWorldz.Phlox` 5 files (ISystemAPI, SyscallShim, SerializedRuntimeState, Defaults - 53 table entries 683-735, RuntimeState) and `Phlox.ScriptEngine` 5 (LSLSystemAPI, OsslGate, the csproj, PhloxEngine, PhloxExecutionScheduler); neither assembly is in the grid root -> prediction **region: `InWorldz.Phlox.dll`, `Phlox.ScriptEngine.dll`; grid: none**. Measured across **75 changed region DLLs and 44 grid DLLs**, stamp-only band **84-94 runs**: region **two** outliers - `InWorldz.Phlox.dll` **+16,896 bytes, 65,228 runs**, `Phlox.ScriptEngine.dll` **+13,312 bytes, 22,913 runs**; grid **zero**. **joltc gate on the staged publish passed** (root and `runtimes\` both **`16AF7638...43B5`**, 855 + 693 staged files hashed, 0 stock); the **live sweep is deferred to the resume** - with the process up, the logs and the state DB cannot be read, and a sweep that skips files is not a sweep. **The [OSSL] question, answered from the preserved config before any copy - verbatim.** `config/OpenSim.ini` lines 77-79: `[OSSL]` / `; Resolves to /app/config-include/osslDefaultEnable.ini (shipped image copy).` / `Include-osslDefaultEnable = \"config-include/osslDefaultEnable.ini\"`. That file (120 non-comment lines) sets **`AllowOSFunctions = true`**, `AllowMODFunctions = true`, `AllowLightShareFunctions = true`, **`PermissionErrorToOwner = false`**, **`OSFunctionThreatLevel = VeryLow`**, `osslParcelO = \"\"`, `osslParcelOG = \"\"`, `osslNPC = the OSSL-section variable osslParcelOG followed by ESTATE_MANAGER,ESTATE_OWNER`, then 111 `Allow_` lines. `config-include/osslEnable.ini` (the operator override, 82 lines, 5 live) repeats `OSFunctionThreatLevel = VeryLow` and the three variables and adds nothing. `Util.ExpandKeyValues` (`Util.cs:2205, 2321`) expands the `osslParcelO` reference (empty) before any engine reads the source, so both engines see `ESTATE_MANAGER,ESTATE_OWNER`. **What that resolves to for the 49 functions Phlox gained:** allowed for everyone by `Allow_ = true` - `osGetGridName`, `osGetGridNick`, `osGetSimulatorVersion`, `osGetPhysicsEngineType`, `osGetScriptEngineName`, `osFormatString`, `osMatchString`, `osRegexIsMatch`; **estate manager / owner only** - `osGetGridHomeURI`, `osGetGridLoginURI`, `osGetGridGatekeeperURI`, `osGetGridCustom`, `osGetRegionStats`, `osGetSimulatorMemory`, `osGetSimulatorMemoryKB`, `osGetAgents` (and the pre-existing `osGetAvatarList`); every other name has no `Allow_` line and falls to the threat-level rule at VeryLow: the ungated helpers (AES, maths, string, list, `osIsUUID`, `osSHA256`, ...) run for everyone, the master-switch ones (`osGetRegionSize`, `osGetMapTexture`, `osGetPhysicsEngineName`, `osCheckODE`, the `osString*` forms) run for everyone, `osGetHealth` (None) runs for everyone, and `osMax` follows `Allow_osGetRezzingObject` - absent, so None <= VeryLow: allowed. **Nothing is denied to a plain resident except the estate-manager set above** - not \"every os* call denied\"; the shipped default file allows the safe ones. **Consequence for the did-it-land:** `Allow_osGetSimulatorVersion = true` is live, so the check prim would NOT be denied as things stand. The check needs **`Allow_osGetSimulatorVersion = false` in `config-include/osslEnable.ini`** - the operator's file, the operator's edit; Claude Code does not touch config. **Baseline for the restart, the 15:30:02 start of 1.1.321 (still running):** disk cache 17, shared 1, Compiled 0, restored 17, `[PhloxState]` WARN/ERROR 0, `database is locked` 0, `Collection was modified` 0, `[PhloxExe] terminated` **1** - `a0d421cb` at 15:30:05, Legion's divide-by-zero test prim from the PHLOX-9b/10 checks, which restores and re-throws once per start; the verify script names it. **Origin:** `origin/feature/ais-v3` is at `ab6d6b86cc` - everything is pushed; local ahead only by this row. | **Resume, when the grid is down:** re-run the five-check probe (all clean), the joltc gate against BOTH live roots (`gate17.ps1` - the staged half is done), the servers-down gate inside `backup.ps1`, `finish.ps1` region then grid from `stage17`'s changesets, `postcopy` with the new backup stamp, guard after, preserved-files check. Then the did-it-land via `D:\legiongrid\_ops\procedures\verify-phlox12-13-restart.sh`: loader pair 17 + 0, schema 0, restored 17, `[PhloxState]` 0, `database is locked` 0, `Collection was modified` **0**, `[PhloxExe] terminated` **0 at load bar a0d421cb**, six AIS/SSB lines; **in-world:** with `Allow_osGetSimulatorVersion = false` in osslEnable.ini, a prim calling `osGetSimulatorVersion` gives the owner **exactly one** script-error line (`OSSL Permission Error: osGetSimulatorVersion disabled in region configuration`) and section 6 shows **exactly one** terminated line for that item. |
| 2026-09-10 | PHLOX-12/13 deploy - **resumed and completed** | `ab6d6b86cc` (deployed) | **Both roots, servers down (clean stop 16:56:43) and not started.** `1.1.321-alpha+788d52ed1f` -> **`1.1.326-alpha+ab6d6b86cc`**, copied 16:58, from the `stage17` publish measured at 16:49 while the grid was still up (the row above). Eighth run of the written procedure, the first resumed one. **Resume, as the stopped row prescribed:** five-check probe clean (0 debuggers, 0 sim processes by name or path, 0 module holders, 15 live files opened exclusively and released - the 13 locked at 16:47 included - logs last written 16:56:4x after `Hosting stopped`); **joltc gate on BOTH live roots this time, every file readable** - 2171 + 777 hashed, 0 stock; staged root and `runtimes\` both **`16AF7638...43B5`**; guard exit 0 staged and live, before and after. Servers-down gate re-run inside the backup script. Copy set from `stage17`'s changesets: region **152** (of 158 differing, 6 preserved), grid **90** (of 92, 2 preserved); **152 of 152 and 90 of 90 re-hashed in the live root against the staged manifest, mismatches 0**, one clean pass. Both roots stamp **1.1.326-alpha+ab6d6b86cc**. **Preserved and byte-identical against the backup, 17 files, 0 changed**, `config/OpenSim.ini` among them. Backups `D:\legiongrid\_backup\regionserver-20260910-1658` (2171) and `gridserver-20260910-1658` (777). File counts: region **2171** (of which **1094** `assetcache`), grid **777**. Step 7: 0 new files. **8a, closed:** the prediction and the measurement stand from the stopped row (region `InWorldz.Phlox.dll` +16,896 / 65,228 runs, `Phlox.ScriptEngine.dll` +13,312 / 22,913; grid none). Landed byte-identical to staged: `InWorldz.Phlox.dll` **`802F2934A9064AB3B29F2DE5378F3676EA62DF110FD59E6C3D79C68B672A39C2`**, `Phlox.ScriptEngine.dll` **`4203A1D2B52358E67068892252582144AA648D56F15E250E975D902098220722`**. **8b applied:** stage path `stage17` passed in; the copy-set counts read back against the 16:49 changeset (158-6=152, 92-2=90); the post-copy script took the backup stamp `20260910-1658` from the backup output. The live root did not change between the 16:49 measurement and the 16:58 copy - no deploy ran, the process only wrote its logs, `assetcache` and the state DB, none of which is in the publish - and `finish.ps1` re-hashed every copy-set file regardless. | **Did it land - from the stopped row, unchanged:** `D:\legiongrid\_ops\procedures\verify-phlox12-13-restart.sh` after John's restart - loader pair **17 + 0**, schema **0**, restored **17**, `[PhloxState]` WARN/ERROR **0**, `database is locked` **0** (PHLOX-11's second clean start), `Collection was modified` **0**, `[PhloxExe] terminated` **0 at load** bar `a0d421cb`, six AIS/SSB lines; **in-world**, with `Allow_osGetSimulatorVersion = false` in `config-include/osslEnable.ini` (live default `true`, operator's edit): a prim calling `osGetSimulatorVersion` gives the owner **exactly one** script-error line and section 6 **exactly one** terminated line for that item. |
| 2026-09-10 | PHLOX-14 deploy - **STOPPED at the servers-down gate** | `56ae7f0214` (staged, NOT copied) | **The GridServer was running.** The brief said both servers were down; at 17:30 the probe found the region server stopped cleanly (`Hosting stopped` 17:29:01) but **`OpenSim.Server.GridServer` pid 47544 alive** (started 17:01:24), its last log line 17:28:57 `Deregistering region \"Elm\"` - a shutdown that never exited. Re-probed at 17:32:57, still there. Rule 1 and rule 2: **nothing copied, no backup, both live roots untouched at `1.1.326-alpha+ab6d6b86cc`.** A region-only copy was not made either: the procedure moves both roots together, and this deploy changes the grid's `Region.Framework.dll` (`IBotManager`). **Staged** `stage18`, stamp **`1.1.330-alpha+56ae7f0214`**: region `-c Release -r win-x64 --self-contained false` (**855** published, **158** differ, **0** new, copy set **152**); grid `-c Release --self-contained false`, **no `-r`** (**693**, **92**, **0**, **90**). Manifests and changesets beside the stage. **8a: predicted first, then measured against the live roots - matched.** From `git diff ab6d6b86cc..56ae7f0214 -- Source/`: `InWorldz.Phlox` 3 files (ISystemAPI, SyscallShim, Defaults - 25 entries 736-760), `Phlox.ScriptEngine` 1 (LSLSystemAPI), `OpenSim.Region.OptionalModules` 1 (BotManager), `OpenSim.Region.Framework` 1 (IBotManager) -> prediction **region: those four assemblies; grid: `OpenSim.Region.Framework.dll` only** (the grid ships Region.Framework and none of the other three). Measured across **75 changed region DLLs and 44 grid DLLs**, stamp-only band **91-104 runs**: region **four** outliers - `OpenSim.Region.OptionalModules.dll` +1,024 / 85,410 runs, `InWorldz.Phlox.dll` +8,192 / 72,704, `OpenSim.Region.Framework.dll` +0 / 60,069, `Phlox.ScriptEngine.dll` +5,120 / 20,434; grid **one** - `OpenSim.Region.Framework.dll` +0 / 60,069, byte-identical to the region's. **joltc gate on the staged publish passed** (855 + 693 hashed, 0 stock, root and `runtimes\` `16AF7638...43B5`, guard exit 0); the region live root swept clean (2254 files, 0 stock); the grid live sweep hit its open log and is deferred to the resume. **The [NPC] config question, answered from the code - and the premise updated.** `config/OpenSim.ini` now ends with `[NPC]` / `Enabled = true` (lines 169-170; sha256 `7795212f...`, 7,538 bytes) - a preserved file, so the copy never touches it; the post-copy preserved-files check names it. **Keys consulted:** `NPCModule.Initialise` (`NPCModule.cs:56-74`) reads `[NPC]` `Enabled` (default **true once the section exists**; absent section = disabled), `AllowNotOwned` (true), `AllowSenseAsAvatar` (true), `AllowCloneOtherAvatars` (true), `NoNPCGroup` (true), `MaxNumberNPCsPerScene` (**40**). `BotManager.Initialise` (`BotManager.cs:134-135`) reads only `[NPC]` `Enabled` (true). `BotPersistenceManager.Initialize` (`BotPersistenceManager.cs:117-140`) reads `[BotPersistence]`: **section absent -> persistence disabled, logged as INFO, BotManager carries on**; present: `Enabled` (**false**), `DatabaseFile` (`botpersistence.db`), `MaxPerParcel` (10), `MaxPerOwner` (15), `MaxPerRegion` (50), `SeparateAgentPool` (false), `DefaultTTL` (604800 s), `PositionSaveInterval` (60), `CleanupInterval` (21600), `RespawnRate` (2). **So `[NPC] Enabled = true` alone brings both NPCModule and BotManager up; `[BotPersistence]` is only for bots that should survive a restart.** And the log proves it: the **17:01:31 start of 1.1.326 already had it** - `NPCModule` and `BotManager` added to all three scenes, `[BotPersistence] No [BotPersistence] section in config - disabled` three times, no `INPCModule not found`. The PHLOX-14 record's \"neither is live\" was true of the 15:30 start and every start before it; **BotManager has been live since 17:01 on 09-10**, once John appended the section. **Baseline for the restart, the 17:01:31 start of 1.1.326:** disk cache 20, shared 1, Compiled 1, restored 19, `[PhloxState]` 0, locked 0, `Collection was modified` 0, terminated **1** (`a0d421cb`, as every start), NPCModule/BotManager added x3, persistence disabled x3. **Origin:** `origin/feature/ais-v3` is at `56ae7f0214` - everything pushed; local ahead only by this row. | **Resume, when the GridServer is really down:** probe (0 processes, 0 locked), `gate18.ps1` on both live roots, `backup.ps1`, `finish.ps1` region then grid from `stage18`, post-copy with the new stamp, guard, preserved check (`config/OpenSim.ini` sha256 `7795212f...` must be unchanged). Then `D:\legiongrid\_ops\procedures\verify-phlox14-restart.sh`: loader pair 20 + 1, restored 19, `[PhloxState]` 0, locked 0, `Collection was modified` 0, **NPCModule and BotManager added x3, `INPCModule not found` 0, persistence-disabled x3**, terminated 1 (`a0d421cb`), six AIS/SSB lines. **In-world, from an estate manager's prim** (the live `Allow_osNpc*` lines resolve to `ESTATE_MANAGER,ESTATE_OWNER`): `osNpcCreate(\"Test\",\"Npc\", llGetPos()+<2,0,0>, \"\")` then `osNpcSay(npc, \"hello\")` - an avatar named **Test Npc** appears, says hello, and `botGetBotsWithTag(\"\")` lists its key; `osNpcRemove(npc)` takes it away. |
| 2026-09-10 | PHLOX-14 deploy - **resumed and completed** | `56ae7f0214` (deployed) | **Why the 19:17 start ran 1.1.326:** the 17:30 attempt stopped at the servers-down gate - the GridServer (pid 47544) had not exited with the region - and by rule nothing was copied, so the next start ran what was there. This is the resume, from the same `stage18` publish measured at 17:31. **Both roots, servers down (clean stops 19:56:11 region, 19:56:23 grid) and not started.** `1.1.326-alpha+ab6d6b86cc` -> **`1.1.330-alpha+56ae7f0214`**, copied 19:58. Ninth run of the written procedure, the second resumed one. Five-check probe clean (0 debuggers, 0 sim processes by name or path, 0 module holders, 15 live files opened exclusively and released, logs last written 19:56:1x/2x after `Hosting stopped`). **joltc gate on BOTH live roots, every file readable:** 2274 + 777 hashed, 0 stock; staged root and `runtimes\` **`16AF7638...43B5`**; guard exit 0 staged and live, before and after. Servers-down gate re-run inside the backup script. Copy set from `stage18`'s changesets: region **152** (of 158 differing, 6 preserved), grid **90** (of 92, 2 preserved); **152 of 152 and 90 of 90 re-hashed in the live root against the staged manifest, mismatches 0**, one clean pass. Both roots stamp **1.1.330-alpha+56ae7f0214**. **Preserved and byte-identical against the backup, 17 files, 0 changed** - and **`config/OpenSim.ini` keeps its `[NPC]` / `Enabled = true` (lines 169-170), sha256 `7795212f522d6967` before and after the copy.** Backups `D:\legiongrid\_backup\regionserver-20260910-1957` (2274) and `gridserver-20260910-1957` (777). File counts: region **2274** (of which **1196** `assetcache`), grid **777**. Step 7: 0 new files. **8a, closed:** prediction and measurement stand from the stopped row (region `OptionalModules` +1,024 / 85,410 runs, `InWorldz.Phlox` +8,192 / 72,704, `Region.Framework` +0 / 60,069, `Phlox.ScriptEngine` +5,120 / 20,434; grid `Region.Framework` only). Landed byte-identical to staged: `InWorldz.Phlox.dll` **`02FFA46D2BB23C4EB7FAF0FD6E5C309D4283C7D40B3FF9D25687428987B36675`**, `Phlox.ScriptEngine.dll` **`18E3A71C70FC03DEFF77489EC6987E9BC21F7AB32259F7C6801608999E7746A2`**, `OpenSim.Region.OptionalModules.dll` `AF051E0E...E1E75F`, `OpenSim.Region.Framework.dll` `014270BD...345541` on both roots. **8b applied:** stage path `stage18` passed in; copy-set counts read back against the 17:31 changeset (158-6=152, 92-2=90); the post-copy script took the backup stamp `20260910-1957` from the backup output. Between the 17:31 measurement and the copy the grid ran once more (19:17-19:56) and wrote only logs, `assetcache` (1094 -> 1196) and the state DB, none of which is in the publish; `finish.ps1` re-hashed every copy-set file regardless. | **Did it land - unchanged from the stopped row:** `D:\legiongrid\_ops\procedures\verify-phlox14-restart.sh` after John's restart - loader pair **20 + 1**, restored **19**, `[PhloxState]` **0**, locked **0**, `Collection was modified` **0**, **NPCModule and BotManager added to a scene x3, `INPCModule not found` 0, persistence-disabled x3**, terminated **1** (`a0d421cb`), six AIS/SSB lines. **In-world, from an estate manager's prim:** `osNpcCreate(\"Test\",\"Npc\", llGetPos()+<2,0,0>, \"\")` then `osNpcSay(npc, \"hello\")` - **Test Npc appears, says hello, `botGetBotsWithTag(\"\")` lists its key**; `osNpcRemove(npc)`. |
| 2026-09-10 | SSB-NPC-1 deploy | `dc0b7c03f1` (deployed) | **Both roots, servers down (clean region stop 20:44:59, grid 20:44:44) and not started.** `1.1.330-alpha+56ae7f0214` -> **`1.1.333-alpha+dc0b7c03f1`**, copied 20:49. Tenth run of the written procedure. Region `-c Release -r win-x64 --self-contained false`, grid `-c Release --self-contained false`, staged as `stage19` at 20:46. Five-check probe clean (0 debuggers, 0 sim processes by name or path, 0 module holders, 15 live files opened exclusively and released, region log last written 20:44:59 ending `[SHUTDOWN]: Shutdown processing on main thread complete`, grid 20:44:44 `Hosting stopped`). **joltc gate:** staged root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`**, `runtimes\` the same; stock hash in **0** of 855 staged region, 693 staged grid, 2283 live region and 777 live grid files; guard exit 0 on the staged root and on the live root before and after the copy. Servers-down gate re-run inside the backup script and again immediately before the copy. Copy set: region **855 published, 158 differed, 0 new, 152 copied** (6 preserved `config-include\*`), grid **693 / 92 / 0 / 90** (2 preserved `*.example`); **152 of 152 and 90 of 90 re-hashed in the live root against the staged manifest, mismatches 0**, one clean pass. Both roots stamp **1.1.333-alpha+dc0b7c03f1**. **Preserved and byte-identical against the backup, 17 files, 0 changed - `config/OpenSim.ini` keeps `[NPC]` / `Enabled = true` (lines 169-170), sha256 `7795212f522d6967` before and after.** No new assemblies (step 7: 0). Backups `D:\legiongrid\_backup\regionserver-20260910-2048` (2283, of which 1204 `assetcache`) and `gridserver-20260910-2048` (777). **8a, predicted then measured:** `git diff 56ae7f0214..dc0b7c03f1 -- Source/` touches one file, `OpenSim.Region.Framework/Scenes/ScenePresence.cs` (+17/-4), so the prediction was **`OpenSim.Region.Framework.dll` on BOTH roots** and nothing else - the grid root ships that assembly too. Measured: region 75 changed DLLs, grid 44, **one outlier each and it is that one**: `OpenSim.Region.Framework.dll` **77,771 runs at size +0** on both, against a stamp-only band of 81-90 runs (grid, 43 files) and 7-90 (region, 74 files; the 7 is a resource-only satellite). Landed byte-identical to staged: `OpenSim.Region.Framework.dll` **`9A51F3C4CB9E59FEBCC5762725A416C624DAAEC3DC0A2A6D10C5942D326D7535`** on both roots; `OpenSim.Server.RegionServer.dll` `3CFDCEE8...F8B9BE`, `OpenSim.Server.GridServer.dll` `35682DA5...3308B4`. (`InWorldz.Phlox.dll` reports `1.0.0.0` as it always has - that project carries no nbgv stamp; it was in both changesets and measured in band, a stamp-only rebuild like the other 73.) **8b:** `stage19` passed as a parameter to every script; counts read back against the 20:46 changeset (158-6=152, 92-2=90); the post-copy script took the backup stamp `20260910-2048` from the backup output. | **What shipped:** SSB-NPC-1 (`dc0b7c03f1`) - `ScenePresence.SendAppearanceToAgentNF` sends an NPC's appearance with visual parameter 251 (`AppearanceMessage_Version`) as 0 on a copy, so a never-baked NPC no longer claims a server bake and Firestorm fetches the owner's stored bake assets from the region instead of 404ing at the appearance service under the NPC's UUID. Partial by design: the LL viewer still shows a cloud (SSB ledger row SSB-NPC-1, residue 1). **Did it land - named before the copy:** `D:\legiongrid\_ops\procedures\verify-ssbnpc1-restart.sh` after John's restart - version line 1.1.333, six AIS/SSB `(global)` lines, loader 20 + 1, restored 19, 0 locked, 0 modified, NPCModule and BotManager x3, and **0 `[SSB]: bake for` lines naming any bot**. **In-world, Firestorm, from a baked estate manager's prim:** `osNpcCreate(\"Test\",\"Npc\", llGetPos()+<2,0,0>, \"\")` - **Test Npc renders with the creator's outfit within seconds, no cloud**; `osNpcRemove(npc)`. A bot a script creates before its owner arrives is still a cloud (older upstream cause: no textures in the avatar record). |
| 2026-09-10 | PHLOX-15 deploy | `d1ed93ac51` (deployed) | **Both roots, servers down (clean region stop 21:17:53, grid 21:17:57) and not started.** `1.1.333-alpha+dc0b7c03f1` -> **`1.1.335-alpha+d1ed93ac51`**, copied 21:22. Eleventh run of the written procedure. Region `-c Release -r win-x64 --self-contained false`, grid `-c Release --self-contained false`, staged as `stage20` at 21:19. Five-check probe clean (0 debuggers, 0 sim processes by name or path, 0 module holders, 15 live files opened exclusively and released, region log last written 21:17:53 ending `[SHUTDOWN]: Shutdown processing on main thread complete`, grid 21:17:57 `Hosting stopped`). **joltc gate:** staged root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`**, `runtimes\` the same; stock hash in **0** of 855 staged region, 693 staged grid, 2294 live region and 777 live grid files; guard exit 0 on the staged root and on the live root before and after. Servers-down gate re-run inside the backup script and again immediately before the copy. Copy set: region **855 published, 158 differed, 0 new, 152 copied** (6 preserved `config-include\*`), grid **693 / 92 / 0 / 90** (2 preserved `*.example`); **152 of 152 and 90 of 90 re-hashed in the live root against the staged manifest, mismatches 0**, one clean pass. Both roots stamp **1.1.335-alpha+d1ed93ac51**. **Preserved and byte-identical against the backup, 17 files, 0 changed - `config/OpenSim.ini` keeps `[NPC]` (sha256 `7795212f522d6967` before and after), and `config-include/osslDefaultEnable.ini` and `osslEnable.ini` are identical to the backup.** No new assemblies (step 7: 0). Backups `D:\legiongrid\_backup\regionserver-20260910-2121` (2294, of which 1214 `assetcache`) and `gridserver-20260910-2121` (777). **8a, predicted then measured:** `git diff dc0b7c03f1..d1ed93ac51 -- Source/` touches four files in two projects - `InWorldz.Phlox` (Defaults, ISystemAPI, SyscallShim) and `Phlox.ScriptEngine` (LSLSystemAPI), 1313 insertions - so the prediction was **`InWorldz.Phlox.dll` and `Phlox.ScriptEngine.dll`, region root only** (the grid ships no Phlox). Measured: region 75 changed DLLs, **exactly those two outliers**: `InWorldz.Phlox.dll` **size +17,920, 67,069 runs** and `Phlox.ScriptEngine.dll` **size +11,776, 23,758 runs** against a stamp-only band of 74-83 runs (73 files); grid 44 changed DLLs, **0 outliers**, all 44 in the 74-83 band. Landed byte-identical to staged: `InWorldz.Phlox.dll` **`06CFB9D9BA652E17E4DE4641DB4F15224212224CE109F9CB84D22D197F768649`**, `Phlox.ScriptEngine.dll` **`6A878ACBA0F521887C4614DC280E7898C8D468ADF4C222228D873A4A57ABB982`**, `OpenSim.Server.RegionServer.dll` `6DEAB4C8...7D7D5A`, `OpenSim.Server.GridServer.dll` `AFB9F224...7D9338`. **8b:** `stage20` passed as a parameter to every script; counts read back against the 21:19 changeset (158-6=152, 92-2=90); the post-copy script took the backup stamp `20260910-2121` from the backup output. | **What the live `[OSSL]` resolves to for every osForce* that landed** (read-only: `config/OpenSim.ini:77-79` includes `config-include/osslDefaultEnable.ini`, whose line 234 includes `osslEnable.ini`; `osslEnable.ini` carries **no `Allow_` line at all**, only the switches and the three macros; both files set `osslParcelO = \"\"` and `osslParcelOG = \"\"`, so the macro prefix expands to nothing): **`ESTATE_MANAGER,ESTATE_OWNER`** for `osForceCreateLink` (:158), `osForceBreakLink` (:99), `osForceBreakAllLinks` (:98), `osForceAttachToAvatar` (:156), `osForceAttachToAvatarFromInventory` (:157), `osForceDropAttachment` (:159), `osForceDropAttachmentAt` (:160); **`false`** for `osForceAttachToOtherAvatarFromInventory` (:206) and `osForceDetachFromAvatar` (:207). **Who that means:** the prim's OWNER must be the estate owner or on the estate's manager list (`OsslGate.cs:191-194`, `IsEstateManagerOrOwner(ownerId)`) - so on Legion Grid an estate manager's scripted object can attach itself to that manager's avatar, from the prim or from its inventory, and drop it again, with no permission dialog; nobody can attach to ANOTHER avatar or force a detach, those two are off for everyone. The manager list itself lives in the estate DB and was not read. **Also found, and it changes the did-it-land:** `Allow_osSetRot = false` (:216) - the brief's 90-degree turn is denied for everyone until an operator adds `Allow_osSetRot = ESTATE_MANAGER,ESTATE_OWNER` to `osslEnable.ini`; `osConsoleCommand` (:220) and `osSetContentType` (:227) are `false` too; `osTeleportObject` (:223), `osSetSpeed`/`osSetOwnerSpeed` (:146-147), `osRequestURL`/`osRequestSecureURL` (:148-149), `osGetLinkPrimitiveParams` (:161), `osMessageObject` (:122) are `ESTATE_MANAGER,ESTATE_OWNER`; `osReplaceString` (:102) and `osSetPrimFloatOnWater` (:110) are `true`. **Did it land - named before the copy:** `D:\legiongrid\_ops\procedures\verify-phlox15-restart.sh` after John's restart - version line 1.1.335, six `(global)` AIS/SSB lines, loader 20 + 1 with **0 `Cache schema version` lines** (entries appended, none moved), restored 19, 0 locked, 0 modified, terminated 1, 0 `[SSB]` lines naming a bot. **In-world, from an estate manager's prim:** `osForceCreateLink(<second prim>, 1)` then `llGetNumberOfPrims` says **2** with no dialog; `osSetRot` in the same script **stops with `osSetRot permission denied`** and nothing rotates - that is the live config, not the port; the rotation half of the brief's check needs the operator line above. |
| 2026-09-11 | PHLOX-16 + PHLOX-17 deploy | `cc898050f2` (deployed) | **Both roots, servers down (clean region stop 05:08:02, grid 05:08:07) and not started.** `1.1.335-alpha+d1ed93ac51` -> **`1.1.338-alpha+cc898050f2`**, copied 05:34. Twelfth run of the written procedure. Region `-c Release -r win-x64 --self-contained false`, grid `-c Release --self-contained false`, staged as `stage21` at 05:31. Five-check probe clean (0 debuggers, 0 sim processes by name or path, 0 module holders, 15 live files opened exclusively and released, region log last written 05:08:02 ending `[SHUTDOWN]: Shutdown processing on main thread complete`, grid 05:08:07 `Hosting stopped`). **joltc gate:** staged root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`**, `runtimes\` the same; stock hash in **0** of 855 staged region, 693 staged grid, 2312 live region and 778 live grid files; guard exit 0 on the staged root and on the live root before and after. Servers-down gate re-run inside the backup script and again immediately before the copy. Copy set: region **855 published, 158 differed, 0 new, 152 copied** (6 preserved `config-include\*`), grid **693 / 92 / 0 / 90** (2 preserved `*.example`); **152 of 152 and 90 of 90 re-hashed in the live root against the staged manifest, mismatches 0**, one clean pass. Both roots stamp **1.1.338-alpha+cc898050f2**. **Preserved and byte-identical against the backup, 17 files, 0 changed - `config/OpenSim.ini` keeps `[NPC]` (sha256 `7795212f522d6967` before and after), `osslDefaultEnable.ini` and `osslEnable.ini` identical to the backup.** No new assemblies (step 7: 0). Backups `D:\legiongrid\_backup\regionserver-20260911-0533` (2312, of which 1228 `assetcache`) and `gridserver-20260911-0533` (778 - one more than 777: the day's new GridServer log). **8a, predicted then measured:** `git diff d1ed93ac51..cc898050f2 -- Source/` touches five files in three projects - `InWorldz.Phlox` (Defaults, ISystemAPI, SyscallShim), `Phlox.ScriptEngine` (LSLSystemAPI) and **`OpenSim.Region.OptionalModules` (`BotManager.cs`, one line: `OutfitKey` made public)** - so the prediction was `InWorldz.Phlox.dll`, `Phlox.ScriptEngine.dll` and `OptionalModules.dll`, region root only (the grid ships none of the three). Measured: region 75 changed DLLs, **two outliers**: `InWorldz.Phlox.dll` **size +17,920, 66,737 runs** and `Phlox.ScriptEngine.dll` **size +15,872, 22,987 runs** against a stamp-only band of 81-93 runs (73 files); grid 44 changed DLLs, 0 outliers. **`OptionalModules.dll` sat inside the band - and the band test is right to say so, because the change is one byte:** measured by hand against a pure-stamp neighbour, `OptionalModules` differs in **82 runs / 168 bytes** where `Region.Framework` (stamp only) differs in **81 / 167** - the accessibility flag on one method, exactly the diff. A one-byte IL change is below what the run-count band can separate from stamp noise; the prediction is confirmed by the byte count, not the run count. Landed byte-identical to staged: `InWorldz.Phlox.dll` **`916089CB1EEFBF79FA0177C0A91C7D018C58F5F395CE5FE1CB808F56E217F2D6`**, `Phlox.ScriptEngine.dll` **`DD2E5399E6DA1B1CD2065DA1614BB47FFF87CA4F0C0266C992DC305649430C01`**, `OpenSim.Region.OptionalModules.dll` `A5BC1A92...EA0712A`, `OpenSim.Server.RegionServer.dll` `A0400FF1...F04676`, `OpenSim.Server.GridServer.dll` `AF1BB791...2C2EBF`. **8b:** `stage21` passed as a parameter to every script; counts read back against the 05:31 changeset (158-6=152, 92-2=90); the post-copy script took the backup stamp `20260911-0533` from the backup output. | **What the live `[OSSL]` resolves to for the seven the brief named** (read-only; `osslEnable.ini` still carries no `Allow_` line, both parcel macros are `\"\"`): **`ESTATE_MANAGER,ESTATE_OWNER` for all seven** - `osCauseDamage` (:152, via the empty `osslParcelO`), `osTeleportOwner` (:89, via the empty `osslParcelOG`), `osKickAvatar` (:221), `osSetParcelDetails` (:217), `osSetTerrainHeight` (:194, literal), `osRegionRestart` (:190, literal), `osSetTerrainTexture` (:195, literal). **Who that means:** the prim's owner must be the estate owner or on the estate's manager list (`OsslGate.cs:191-194`); no parcel owner or group member qualifies while the macros are empty. **Also found:** the live file has **no `Allow_` line for the deprecated names `osTerrainSetHeight` and `osParcelSetDetails`**, so those fall to the `OSFunctionThreatLevel = VeryLow` comparison and are **denied for everyone** (both are High) - the did-it-land uses the new names; `osAvatarPlayAnimation` (:204) and `osForceOtherSit` (:208) are `false` for everyone; `osGetAgentIP` is `true` (:226) but the port requires a god owner as well; `osCauseHealing`/`osSetHealth` (:153-154), `osOwnerSaveAppearance` (:187), `osDie` (:127), `osParcelJoin`/`Subdivide` (:188-189), `osRegionNotice` (:191), `osSetRegionWaterHeight` (:193), `osSetTerrainTextureHeight` (:196), `osTerrainFlush` (:112), the parcel media/music URLs (:107-108), the group invite/eject (:97, :101) and the name/key lookups (:115, :117) are all `ESTATE_MANAGER,ESTATE_OWNER`. **Did it land - named before the copy:** `D:\legiongrid\_ops\procedures\verify-phlox16-17-restart.sh` after John's restart - version line 1.1.338, six `(global)` AIS/SSB lines, loader 20 + 1 with 0 `Cache schema version` lines (56 entries appended at 811-866, none moved), restored 19, 0 locked, 0 modified, terminated 0, 0 `[SSB]` lines naming a bot. **In-world, from an estate manager's prim:** `osTeleportAgent(llGetOwner(), \"Transylvania\", <128,128,30>, <1,0,0>)` moves you and `osCauseDamage(llGetOwner(), 10.0)` shows `on_damage` in a worn attachment (damage on, not invulnerable); `osSetTerrainHeight(10,10, osGetTerrainHeight(10,10)+1.0); osTerrainFlush();` reads back 1.0 higher with the ground visibly moved, and `osSetParcelDetails(llGetPos(), [PARCEL_DETAILS_NAME, ...])` renames the parcel in About Land. |
| 2026-09-11 | PHLOX-18 deploy - **8a stopped, explained, resumed** | `270ff6d30c` (deployed) | **Both roots, servers down (clean region stop 15:17:05, grid 15:17:16) and not started.** `1.1.338-alpha+cc898050f2` -> **`1.1.341-alpha+270ff6d30c`**, copied 15:24. Thirteenth run of the written procedure. Region `-c Release -r win-x64 --self-contained false`, grid `-c Release --self-contained false`, staged as `stage22` at 15:19. Five-check probe clean (0 debuggers, 0 sim processes by name or path, 0 module holders, 15 live files opened exclusively and released, region log last written 15:17:05 ending `[SHUTDOWN]: Shutdown processing on main thread complete`, grid 15:17:16 `Hosting stopped`). **joltc gate:** staged root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`**, `runtimes\` the same; stock hash in **0** of 855 staged region, 693 staged grid, 2336 live region and 778 live grid files; guard exit 0 on the staged root and on the live root before and after. Servers-down gate re-run inside the backup script and again immediately before the copy. Copy set: region **855 published, 158 differed, 0 new, 152 copied** (6 preserved `config-include\*`), grid **693 / 92 / 0 / 90** (2 preserved `*.example`); **152 of 152 and 90 of 90 re-hashed in the live root against the staged manifest, mismatches 0**, one clean pass. Both roots stamp **1.1.341-alpha+270ff6d30c**. **Preserved and byte-identical against the backup, 17 files, 0 changed - `config/OpenSim.ini` keeps `[NPC]` (sha256 `7795212f522d6967` before and after), `osslDefaultEnable.ini` and `osslEnable.ini` identical to the backup.** No new assemblies (step 7: 0). Backups `D:\legiongrid\_backup\regionserver-20260911-1523` (2336, of which 1248 `assetcache`) and `gridserver-20260911-1523` (778). **8a - STOPPED at the measurement, then explained.** Prediction from `git diff cc898050f2..270ff6d30c -- Source/` (eight files, all `InWorldz.Phlox` and `Phlox.ScriptEngine`): `InWorldz.Phlox.dll` and `Phlox.ScriptEngine.dll`, region only. **Measured: 22 region outliers and 8 grid outliers, and a stamp-only band of 190-954 runs instead of 80-95** - `OpenSim.Framework.dll` 6,679 runs at size +0, `OpenSim.Data.MySQL.dll` 2,617, every stamped assembly on both roots. By rule nothing was copied until the reason was known. **Ruled out, read-only:** same SDK (10.0.400, installed 08-20), same Roslyn (`compiler-version 5.9.0-1.26379.115` in both stages' PDBs), same packages (`SkiaSharp.dll`, `Microsoft.Extensions.Logging.dll`, `System.Data.SQLite.dll` byte-identical between `stage21` and `stage22`, no NuGet folder newer than the 05:31 publish), the build deterministic (a rebuild of `OpenSim.Framework` at 15:2x is byte-identical to the 15:18 publish), `stage21` still byte-identical to live (0 runs). **The reason:** `AssemblyFileVersion` is `1.1.<height>.<n>` with `<n>` the first 16 bits of the commit id in decimal - `cc898050f2` gave **52361**, `270ff6d30c` gives **9999** - so the string is one character shorter in every stamped assembly, the metadata heaps after it shift, and the run count explodes with the size unchanged (`FileVersion 1.1.338.52361` live before, `1.1.341.9999` staged; the majority delta in `OpenSim.Framework` is a uniform +2, an offset shift). About one commit in six will do this. The two real outliers stand as predicted: `InWorldz.Phlox.dll` **size +12,288, 82,302 runs** (no stamp - `1.0.0.0` - so all of it is IL) and `Phlox.ScriptEngine.dll` **size +6,656, 23,346 runs**; **no other assembly moved in size**, which is the test the band cannot give when the stamp width changes. The procedure's 8a carries the caveat now (`DEPLOY-PROCEDURE.md`, \"The stamp-width caveat\"). Landed byte-identical to staged: `InWorldz.Phlox.dll` **`250E87249924D6D06F93F2A5ADF3537F4046F1029C5DD86382F06484E5B87CDA`**, `Phlox.ScriptEngine.dll` **`B3FBCAB1EB2FE6C0D60385F8049B750B0D1AE021D6BEFB50015D7B059BF30968`**, `OpenSim.Framework.dll` `FBB79EF7...0C653E`, `OpenSim.Server.RegionServer.dll` `080D0F6A...186744`, `OpenSim.Server.GridServer.dll` `05ABCAA4...289119`. **8b:** `stage22` passed as a parameter to every script; counts read back against the 15:19 changeset (158-6=152, 92-2=90); the post-copy script took the backup stamp `20260911-1523` from the backup output. | **What the live `[OSSL]` resolves to:** `osSetDynamicTextureData` (and `DataFace`, `DataBlend`, `DataBlendFace`, :103-106) = **`ESTATE_MANAGER,ESTATE_OWNER`** through the empty `osslParcelOG` macro - the prim's owner must be the estate owner or on the manager list; the three `URL` forms (:213-215) are the same two literally. **`osDrawText` has no `Allow_` line and no threat level** - it is upstream's bare `CheckThreatLevel()`, the `AllowOSFunctions = true` master switch - so **anyone may build a draw list** with any of the twenty helpers; only handing it to a face is gated. `ReuseDynamicTextures` is the default `false` (`OpenSimDefaults.ini:1080`) and the region runs `FlotsamAssetCache` (`GridCommon.ini:28`), which the dynamic-texture updater requires. **Did it land - named before the copy:** `D:\legiongrid\_ops\procedures\verify-phlox18-restart.sh` after John's restart - version line 1.1.341, six `(global)` AIS/SSB lines, loader 20 + 1 with 0 `Cache schema version` lines, restored 19 (the rows carry tag 26 now; older rows load with it null), **terminated 0 at load** (a crashed script is held now, not re-run), and **section 3: every script the loader did NOT start because its item's Running flag was off** - the `loaded STOPPED` lines, listed as item id and prim localId (the log carries no more; `phlox status <item>` gives the prim name, and the row's MySQL one-liner gives name and owner - the DB itself was not read by CC). **In-world:** a manager's prim running `osSetDynamicTextureData(\"\", \"vector\", \"MoveTo 20,20; PenColour RED; FontSize 24; Text Hello;\", \"\", 0)` shows Hello in red on its faces; `phlox status a37d3c87-...` after the osSetRot prim's first crash shows `Running flag : False` and the `terminated :` line, and a viewer reset runs it once more. |
| 2026-09-11 | DRAW-1 + PHLOX-19 deploy | `654981f0c7` (deployed) | **Both roots, servers down (clean region stop 16:22:17, grid 16:22:01) and not started.** `1.1.341-alpha+270ff6d30c` -> **`1.1.344-alpha+654981f0c7`**, copied 16:27. Fourteenth run of the written procedure. Region `-c Release -r win-x64 --self-contained false`, grid `-c Release --self-contained false`, staged as `stage23` at 16:25. Five-check probe clean (0 debuggers, 0 sim processes by name or path, 0 module holders, 15 live files opened exclusively and released, region log last written 16:22:17 ending `[SHUTDOWN]: Shutdown processing on main thread complete`, grid 16:22:01 `Hosting stopped`). **joltc gate:** staged root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`**, `runtimes\` the same; stock hash in **0** of 855 staged region, 693 staged grid, 2350 live region and 778 live grid files; guard exit 0 on the staged root and on the live root before and after. Servers-down gate re-run inside the backup script and again immediately before the copy. Copy set: region **855 published, 158 differed, 0 new, 152 copied** (6 preserved `config-include\*`), grid **693 / 92 / 0 / 90** (2 preserved `*.example`); **152 of 152 and 90 of 90 re-hashed in the live root against the staged manifest, mismatches 0**, one clean pass. Both roots stamp **1.1.344-alpha+654981f0c7**. **Preserved and byte-identical against the backup, 17 files, 0 changed - `config/OpenSim.ini` keeps `[NPC]` (sha256 `7795212f522d6967` before and after), `osslDefaultEnable.ini` and `osslEnable.ini` identical to the backup.** No new assemblies (step 7: 0). Backups `D:\legiongrid\_backup\regionserver-20260911-1626` (2350, of which 1260 `assetcache`) and `gridserver-20260911-1626` (778). **8a, predicted then measured - the stamp-width caveat again, the other way.** Prediction from `git diff 270ff6d30c..654981f0c7 -- Source/` (six files): `InWorldz.Phlox.dll`, `Phlox.ScriptEngine.dll` and **`OpenSim.Region.CoreModules.dll`** (DRAW-1's `VectorRenderModule`) on the region root; **the grid root ships none of the three** (`OpenSim.Region.CoreModules.dll` is not in the grid publish - 0 files), so its expectation was **no outlier**. Measured: `FileVersion` went **`1.1.341.9999` -> `1.1.344.25929`**, four digits back to five, so every stamped assembly shifted by one character and the band read 183-945 on both roots - the caveat written into 8a yesterday, in reverse. Tested the way the caveat says: **by size**, `InWorldz.Phlox.dll` **+13,312 / 73,375 runs** and `Phlox.ScriptEngine.dll` **+9,728 / 25,312 runs** are real, nothing else moved in size; and **against yesterday's shift-only run counts**, every assembly matches within ten (`OpenSimNGC.Appearance.Baking` 284,929 -> 284,919, `Gloebit` 30,341 -> 30,332, `OptionalModules` 9,205 -> 9,196, `OpenSim.Framework` 6,679 -> 6,669, the grid's eight the same) **except `OpenSim.Region.CoreModules.dll`, 4,164 -> 155,441 at size +0** - the predicted DRAW-1 change, a setting and an offset that padding absorbed. Prediction confirmed on both roots. Landed byte-identical to staged: `InWorldz.Phlox.dll` **`CFD8FB58C34280FCC997CB334A15BE9FF356535F27E3D6A91BFC2240B31DD216`**, `Phlox.ScriptEngine.dll` **`3444D49786B6B1E90FB2C61C99D12AA07F36B56A9097C9D6C62C85D96F4A84AB`**, `OpenSim.Region.CoreModules.dll` **`97C90A6FB4F81468C0D53DF282569FA490DDFEA98EB58C4C8FE0D65C4405787B`**, `OpenSim.Server.RegionServer.dll` `C41CA5D1...930F59`, `OpenSim.Server.GridServer.dll` `48F12481...2CE026`. **8b:** `stage23` passed as a parameter to every script; counts read back against the 16:25 changeset (158-6=152, 92-2=90); the post-copy script took the backup stamp `20260911-1626` from the backup output. | **The open item, answered from the code before the copy - does `osGetNotecardLine` block the scheduler thread on a cache miss, and what bounds it?** Yes, and the bound is the HTTP client's timeout. The shim is synchronous - `Shim_osGetNotecardLine` pops, calls, pushes on the scheduler thread with no `RunAsync` (`SyscallShim.cs:8552-8558`; the trio's other two are the same shape) - and `OsslNotecardLines` calls `World.AssetService.Get(assetId)` (`LSLSystemAPI.cs:8436`). On Legion that is `RegionAssetConnector` (`config-include/Grid.ini:11`): `RegionAssetConnectorModule.Get` (`:246-280`) answers from the Flotsam cache first - a hit or a negative-cached miss returns at once - and on a real miss fetches from the asset server at `127.0.0.1:8003` (`GridCommon.ini:36`) through `WebUtil`'s shared `HttpClient`, whose `Timeout` is **30,000 ms when the caller passes none** (`WebUtil.cs:248`). So: not \"nothing\", but a 30 s worst case per cold notecard, on the thread every Phlox script shares; a local asset server answers in milliseconds and a notecard is read once into the cache, so the cost is one fetch per notecard per cache lifetime. The right shape is the PHLOX-2g one `llDamage` uses - `RunAsync` in the three shims so the fetch never sits on the scheduler thread - one line each, **not done in this deploy**; the verify script counts `Slow timeslice` lines so it can be seen if it ever bites. **What the live `[OSSL]` resolves to:** `osGetNotecardLine`, `osGetNotecard`, `osGetNumberOfNotecardLines` (:210-212) = **`ESTATE_MANAGER,ESTATE_OWNER`** through the empty `osslParcelO` macro - the prim's owner must be the estate owner or on the manager list; **`osListenRegex` (:118) = `true` - anyone**; `osGetAvatarHomeURI` (:124, via `osslParcelOG`), `osDetectedCountry` (:130), `osGetAgentCountry` (:133), `osGetNumberOfAttachments` (:140), `osGetRegionMapTexture` (:163) are `ESTATE_MANAGER,ESTATE_OWNER`; `osGetGender`, `osGetHealRate`, `osGetRezzingObject` (:81-85) are commented out, so they fall to the `VeryLow` default and pass (all three are None). **Did it land - named before the copy:** `D:\legiongrid\_ops\procedures\verify-draw1-phlox19-restart.sh` after John's restart - version line 1.1.344, six `(global)` AIS/SSB lines, loader 20 + 1 with 0 `Cache schema version` lines, restored 19, terminated 0, the `loaded STOPPED` count, 0 `Image Encoding Failed`, 0 notecard exceptions, and the `Slow timeslice` count. **In-world:** a prim holding a notecard \"cfg\" with two lines says `2`, then the first line, then the second line, each separated by a bar - **no trailing `}` on the second line** (the header stripper is fixed; `llGetNotecardLine`'s last line loses its `}` too); the draw prim shows **red Hello on white, below y=20**; `osListenRegex(7, \"\", NULL_KEY, \"^hel+o$\", OS_LISTEN_REGEX_MESSAGE)` hears `/7 hello` and not `/7 goodbye`. |
| 2026-09-11 | PHLOX-18b + PHLOX-20 deploy | `24e8130108` (deployed) | **Both roots, servers down (clean region stop 17:00:29, grid 17:00:13) and not started.** `1.1.344-alpha+654981f0c7` -> **`1.1.350-alpha+24e8130108`**, copied 17:38. Fifteenth run of the written procedure. Region `-c Release -r win-x64 --self-contained false`, grid `-c Release --self-contained false`, staged as `stage24` at 17:36. Five-check probe clean (0 debuggers, 0 sim processes by name or path, 0 module holders, 15 live files opened exclusively and released, region log last written 17:00:29 ending `[SHUTDOWN]: Shutdown processing on main thread complete`, grid 17:00:13 `Hosting stopped`). **joltc gate:** staged root `joltc.dll` **`16AF76381387DADD7DFA5E10D6E3AD025AB624F22187D7442D1BDB88146743B5`**, `runtimes\` the same; stock hash in **0** of 855 staged region, 693 staged grid, 2363 live region and 778 live grid files; guard exit 0 on the staged root and on the live root before and after. Servers-down gate re-run inside the backup script. Copy set: region **855 published, 158 differed, 0 new, 152 copied** (6 preserved `config-include\*`), grid **693 / 92 / 0 / 90** (2 preserved `*.example`); **152 of 152 and 90 of 90 re-hashed in the live root against the staged manifest, mismatches 0**, one clean pass. Both roots stamp **1.1.350-alpha+24e8130108**. **Preserved and byte-identical against the backup, 17 files, 0 changed - `config/OpenSim.ini` keeps its one `[NPC]` section (sha256 `7795212F522D6967` before and after), both ossl ini files identical.** No new assemblies (step 7: 0). Backups `D:\legiongrid\_backup\regionserver-20260911-1737` (2363, of which 1272 `assetcache`) and `gridserver-20260911-1737` (778). **Bytecode-cache gate, asked for by the brief and answered before the copy:** `git diff 654981f0c7..24e8130108 -- Tests/InWorldz.Phlox.Tests/dispatch-baseline.txt` is **19 added lines and 0 removed** - append-only, 913 -> 932 names, **no existing TableIndex moved**, so every `.plx` in `ScriptEngines/Phlox/bytecode` still dispatches to the function it was compiled against and the cache stays valid across this restart. (PHLOX-20's eight type-discriminated overloads add entries 940-947 and nineteen more to 966; the only renames are compiler-internal SYMBOL names, which never reach a cached bytecode file.) **8a, predicted then measured - the stamp-width caveat for the third time.** Prediction from `git diff 654981f0c7..24e8130108 -- Source/`: two projects only, `InWorldz.Phlox` and `Phlox.ScriptEngine`, both region-side; **the grid root ships neither**, so its expectation was **no real change at all**. Measured: `FileVersion` went **`1.1.344.25929` -> `1.1.350.9448`**, five digits back to four, so every stamped assembly shifted one character again and the band read 185-948 (region) and 185-873 (grid) - blind, exactly as the caveat says. Tested the two ways the caveat prescribes. **By size:** `InWorldz.Phlox.dll` **+11,776 / 80,163 runs** and `Phlox.ScriptEngine.dll` **+5,632 / 25,440 runs** are the only two files in either root whose size moved. **Against the previous run's shift-only counts:** `OpenSimNGC.Appearance.Baking` 284,929 -> 284,923, `Gloebit` 30,341 -> 30,338, `OptionalModules` 9,205 -> 9,199, `OpenSim.Framework` 6,679 -> 6,675 - every one within ten - and `OpenSim.Region.CoreModules.dll` **155,441 -> 4,158**, back to shift-only now that DRAW-1 is behind it, which is itself the confirmation that nothing touched CoreModules this time. The grid's eight outliers are the same assemblies at the same counts (Framework 6,675, Region.Framework 6,165, SQLite 2,682, MySQL 2,612, Handlers 1,958, HttpServer 1,713, Console 1,428, SharedBase 1,056), all at size+0: no real change on the grid root, as predicted. Landed byte-identical to staged: `InWorldz.Phlox.dll` **`3225A8CCF9233DF09B3110C087C33EEB59C8D9316C1E3435AF7E18CE072AC6B6`** (unstamped 1.0.0.0, so every one of its runs is real IL), `Phlox.ScriptEngine.dll` **`6E987D6573E305B4E827C7B5C067C5BCBB00E8060525C6D87AD396431F1E7B01`**, `OpenSim.Server.RegionServer.dll` `35E2C2DF...D23DB85C`, `OpenSim.Server.GridServer.dll` `A4085DE1...EF521679`. **8b:** `stage24` passed as a parameter to every script; counts read back against the 17:36 changeset (158-6=152, 92-2=90); the post-copy script took the backup stamp `20260911-1737` from the backup output. | **What went on:** PHLOX-18b - a script killed by a runtime error now reaches the state row a region stop actually writes (`OnShutdown` calls `StateManager.Stop()`, which flushes the DIRTY set only, and the crash branch returned before `ScriptChanged`), so item `9262c036` and its kind stay stopped across a restart instead of running `state_entry` again; a restored stopped script also pushes the item's Running flag off and logs `restored STOPPED`, which the region DB cannot carry for it. PHLOX-20 - the compiler chooses a built-in's overload by ARGUMENT TYPE within the arity, which unblocked five OSSL forms that could not exist before (osSetProjectionParams by key, osSetDynamicTextureDataFace, osSetPenColor by vector, osApproxEquals for vectors and rotations, osSlerp for vectors), plus PHLOX-12's misc row and the list family (17 names, including the two in-place sorts that change the caller's own list), and the surface audit was fixed and run: **13 of 756 API names remain absent** - osGiveLinkInventory, osGiveLinkInventoryList, osMakeNotecard, osMessageAttachments, osNpcSayTo, osNpcLookAt (no look-at door exists in this tree's BotManager), the four environment functions (the EEP module HAS setters; the ViewerEnvironment day-cycle plumbing is what is missing), and three LSL names outside the lane. **Did it land - named before the copy:** `D:\legiongrid\_ops\procedures\verify-phlox18b-20-restart.sh` after John's restart - version line 1.1.350 on both roots, the loader count with 0 `Cache schema version`, **terminated 0 at load**, the `restored STOPPED` and `loaded STOPPED` counts with the item ids, 0 `is ambiguous between`, 0 `[PhloxCompile]` errors, 0 unknown opcode or syscall, and the `Slow timeslice` count. **In-world:** one script says `PenColor Red;` and `PenColor FFFF0000;` from the two osSetPenColor forms and gets a three-component vector from `osSlerp(<1,0,0>, <0,1,0>, 1.0)`; `list src = [3,1,2]; osListSortInPlace(src, 1, TRUE);` leaves `src` as `1,2,3`; and a prim crashed before a region stop comes back **Running=False and silent**. |
| 2026-09-11 | A20 - AIS-SEC-1: the real inventory backend was not owner-scoped | `373bdcbe1c` (red), `f8263e22b8` (fix), `<this commit>` (docs) | **A valid AIS cap plus another resident's UUID could read, rename, delete, purge, slam and create against that resident's inventory - and the suite was green throughout.** `IAisInventoryBackend` has documented since A0 that `GetFolder`/`GetItem` return null for an object "not the agent's". `AISv3Module.InventoryServiceBackend` never implemented it: it was a pure pass-through, and `XInventoryService` resolves by UUID alone and says so in its own comments - `GetItem` queries `inventoryID` (`XInventoryService.cs:633-641`), `GetFolder` queries `folderID` (`:653-663`), `GetFolderContent` carries *"This method doesn't receive a valud principal id from the connector. So we disregard the principal and look by ID"* (`:319-323`), `DeleteFolders` *"Ignore principal ID, it's bogus at connector level"* (`:482-492`) and `DeleteItems` *"Just use the ID... \*facepalms\*"* (`:602-631`). Eleven routes were reproduced as adversarial tests and **all eleven answered 200 and did the work**: GET `/item`, GET `/category/{id}/children`, GET `/category/{id}/links`, PATCH `/item`, PATCH `/category`, DELETE `/item`, DELETE `/category`, DELETE `/category/{id}/children`, PUT `/category/{id}/links`, POST `/category/{id}`, and POST to the caller's own folder with a body `parent_id` naming somebody else's. The slam is the worst of them: it would have **stripped another resident's Current Outfit**. **Why the suite could not see it.** Every HTTP fixture in the project runs on `FakeAisBackend`, which enforces owner scoping itself (`if (agentId != Owner) return null;` in each read) - so the fake was keeping a promise the shipped code did not. The new `PrincipalIgnoringInventoryService` double removes that crutch and reproduces the real service, with two deliberate over-approximations documented on the class (its `GetInventorySkeleton` and `GetFolderForType` ignore the principal where the **real** ones do scope it, so the backend has to filter rather than inherit a guarantee from the connector). **Not fixed in `XInventoryService`**: that behaviour is upstream and other connector paths depend on it - the Robust connector genuinely passes a principal the service cannot trust, which is what those comments are about. The cap is the right place, because it is registered per agent from `OnRegisterCaps` and so knows exactly whose it is. **The fix.** The constructor takes a `UUID ownerId` and throws `ArgumentException` on zero; both call sites in `RegisterCaps` bind `agentID` - the inventory cap, and the library cap's COPY **destination** backend, so a COPY cannot be steered into another resident's folder by its `Destination` header (the library *source* backend is untouched and read-only by construction). Reads answer only for rows whose `Owner` is the bound owner; `GetFolderContent` requires the folder itself to pass `GetFolder` and then filters both collections, returning a rebuilt `InventoryCollection`; `GetItems`/`GetInventorySkeleton` filter, `GetSubFolders` derives from the scoped content. Writes require the object **and** its parent to be the owner's (`UpdateFolder` checks the parent only when `ParentID` is non-zero, so the inventory root can still be renamed). `DeleteItems` refuses the whole batch unless every id comes back from **one** scoped `GetItems` - which wraps `GetMultipleItems`, so a slam removing ~20 links costs a single Robust round trip rather than twenty; the count test works because the service returns one slot per requested id (`:643-651`). `ApplyAssetTransaction` **fails closed** (`Refused`, not `NotResolvable`, which would still yield a 200). `AisHandler` gained one rule: POST `/category/{parent}` whose body names a different `parent_id` is **400** before anything is written - the URL is the authority (`llaisapi.cpp:115`) and the viewer's own body repeats the same id (`llinventory.cpp:1256-1276`). **The status for a foreign object is 404, uniformly, reads and mutations alike.** The scoped reads make a foreign object indistinguishable from an absent one, so the handler's pre-existing not-found paths fire and no status mapping had to be invented; a 403 would be a membership oracle over the inventory keyspace, and the viewer treats any non-2xx alike here (`llaisapi.cpp:851-951`). **The 500s were checked and deliberately left alone** on `UpdateItem`/`UpdateCategory`/`RemoveItem`/`RemoveCategory`/`PurgeDescendents`/`SlamFolder`/`CreateInventory`: they are now unreachable for an ownership refusal, so they still mean only what they always meant - the inventory service failed - and three pre-existing tests assert exactly that by injecting a service failure. **Tests 160 -> 172** (11 red-then-green cross-user cases plus one positive control that runs every route against the caller's own objects, so the fixture cannot be satisfied by a backend that refuses everything); solution **0 errors**. `new InventoryServiceBackend(` is 2 hits in `Source/`, both with `agentID`, plus 1 in the new test - the test constructs the real backend by design, which is the whole point of it. | **Deployed later the same evening at `1.1.354-alpha+0cbd371567` (the row below); the first attempt STOPPED at the servers-down gate, third time on this grid.** At 21:15 on 2026-09-11 `OpenSim.Server.GridServer` (pid 28652) and `OpenSim.Server.RegionServer` (pid 39968) were both alive, started 17:42:29 / 17:42:33, ports 9000/8002/8003 listening and the region log written that second. Procedure rule 1 (CC never starts **or stops** a server) and rule 2 (a failed gate stops the deploy) apply; nothing was published, nothing staged and nothing copied, exactly as at the PHLOX-12/13 and PHLOX-14 stops. Live stays `1.1.350-alpha+24e8130108`, which does **not** carry this fix - **the defect is live until the deploy runs**. Baseline taken from the running build while it was up: the standing check gives its six lines for the 17:42 start (Ebony, Transylvania, Elm, each `AIS v3 "ON" ("global")` and `server-side baking "ON" ("global")`), zero `[AIS]` ERROR lines all day, zero mentions of `InventoryServiceBackend`. **Checklist step is 19, not 17** - 17 and 18 were already taken by S9 and S10; the brief's numbering was stale. It is also this deploy's "did it land" check (procedure step 11), named before any copy: Truly fetches Aleric's COF UUID through her own cap and gets **404**, then the same UUID against PATCH/DELETE-children/PUT-links, each refused with Aleric's rows unchanged. It fails differently from the bug - 200-and-done becomes 404-and-nothing - which a hash cannot tell apart. |
| 2026-09-11 | AIS-SEC-1 deploy - **stopped twice, resumed twice** | `0cbd371567` (deployed) | **Both roots, servers down (clean region stop 21:20:59, grid 21:21:01) and not started.** `1.1.350-alpha+24e8130108` -> **`1.1.354-alpha+0cbd371567`**, copied 21:29. Sixteenth run of the written procedure, and the first stopped at **two** different gates. Region `-c Release -r win-x64 --self-contained false` (**855** published, 697 identical, **158** differed, **0** new, **152** copied after the preserve rule); grid `-c Release --self-contained false`, **no `-r`** (**693** published, 601 identical, **92** differed, **0** new, **90** copied). Staged as `stage25` under `D:\legion-deploy`. Full copy set re-hashed **in the live root** against the staged manifest: **152 and 90, no mismatch, one clean pass** (152 of 152 and 90 of 90 actually copied, none skipped as already-identical). **Stop 1: the servers-down gate.** The first attempt found the grid up at 21:15 - GridServer pid 28652 and RegionServer pid 39968, both started 17:42, ports 9000/8002/8003 listening, region log written that second. Rule 1 (CC never starts *or stops* a server) and rule 2 (a failed gate stops the deploy): nothing was published, staged or copied, as at the PHLOX-12/13 and PHLOX-14 stops. **Stop 2: step 8a found TWO region outliers where one was predicted - and the second was a false positive, proven rather than waved through.** Predicted from `git diff 24e8130108..0cbd371567 -- Source/`: exactly one assembly with changed IL, `OpenSim.Region.ClientStack.LindenCaps.dll`. Measured: that one (**16,181 runs, size +2048**, as expected for ~250 added lines) **and `InWorldz.Phlox.dll`**. The tell was that Phlox came in **below** the band rather than above - **6 runs, size +0** against a 82-93 band - because the band is calibrated for *stamped* assemblies and Phlox carries `1.0.0.0` with no version string to move. **The whole difference is 71 bytes of 784,896 (0.009%), and all 71 are accounted for**: the PE COFF **TimeDateStamp** (`0x88`, 4 B), the **MVID** in the `#GUID` heap (`0xAE9CC`, 16 B), the **debug-directory entry timestamp** (`0xB4258`, 4 B - the table spans `0xB4254`-`0xB42A7`), the CodeView **PDB GUID** (`0xB42AC`, 16 B, split 7+8 by one coincidentally-equal byte, the `RSDS` signature occupying `0xB42A8`-`0xB42AB`) and the **PdbChecksum** (`0xB4309`, 32 B - the entry spans `0xB4302`-`0xB4328` and opens with `"SHA256\0"`). Offsets were established by **parsing the PE headers** for the debug directory and CodeView entry, not by matching on run lengths. `4+16+4+7+8+32 = 71` is the entire delta, the length is unchanged, and the diff touches six files, **none** under `Source/InWorldz.Phlox` - so the IL, metadata tables, string and blob heaps and resources are byte-identical and **the assembly did not change**. Most likely cause of it differing at all: the live copy was published from a different worktree path, and absolute paths feed the deterministic PDB id. John accepted the analysis and the deploy resumed. **The grid root had zero outliers**, correct by construction: `LindenCaps` is not in the grid publish at all, so the grid deploy is a version re-stamp and nothing more - done anyway to stop the two roots drifting. Stamp width was 4 digits on both sides (`1.1.354.3261` vs `1.1.350.9448`), so the run band was valid this run rather than blind. **The procedure was corrected before the copy.** Step 8a used to say *"`InWorldz.Phlox` carries `1.0.0.0` and no stamp, so any run there is real"* - generalised from the single PHLOX-2d pair where it happened to come out byte-identical. An unstamped assembly still carries five per-build identity values and they all move on any rebuild. The accurate rule is now written there: a below-band outlier is a rebuild-identity false positive when its runs land exactly on those five fields, the bytes they account for are the whole difference, the length is unchanged and the source diff touches nothing in that project; **a run anywhere else in the file is real.** **The joltc gate, sixth run, passed.** Two `joltc*` in the staged region publish, both **`16AF7638...43B5`**; **every** file of all four sets hashed against the stock hash - 855 staged region, 693 staged grid, 2375 live region, 778 live grid - **zero matches**. Guard exit 0 against staged and live before the copy and against live after it, 2 checked and no notes each time. Live root `joltc.dll` `16AF7638...43B5` throughout, never in the copy set because it is preserved. **Pre-copy probe clean on all five parts**, with the servers-down gate re-run immediately before the backups: no debugger or dump reader (15 names), no sim process by name, nothing imaged under `D:\legiongrid`, no process holding a module there, all four ports free, and the **exclusive-open probe opened and closed 16 of 16** live assemblies across both roots. **Step 7 had nothing to do: 0 new files in either root.** **Preserved** and proven byte-identical against the pre-deploy backup: the 6 region `config-include` files (`Grid.ini`, `GridHypergrid.ini`, `Standalone.ini`, `StandaloneHypergrid.ini`, `GridCommon.ini.example`, `StandaloneCommon.ini.example`), the 2 grid `*.example` (`Robust.ini.example`, `Robust.HG.ini.example`), and whole-tree checks over `regionserver\config` (4 files), `config-include` (12), `Regions` (2) and `gridserver\config` (4) - **0 differences in any of them**, with `config/OpenSim.ini` and `config/Robust.ini` named explicitly. Live-only `WebRtcVoiceServiceModule.dll` still **`1.1.160-alpha+d95754509d`**, confirmed absent from the staged publish. File counts unchanged: region **2375**, grid **778**. Backups `D:\legiongrid\_backup\regionserver-20260911-2129` (2375) and `gridserver-20260911-2129` (778), each verified file-for-file against its source before the copy. **Verified in the deployed binary, not only in the publish**: a byte-level search at both alignments (UTF-16 for `#US` literals, UTF-8 for metadata names - decoding the whole file at one alignment gives false answers in both directions, which it did on the first pass) finds `OwnerId`, `IsCaller`, `IsOwned`, `m_ownerId`, `an AIS inventory backend must be bound to a non-zero owner`, `the URL is the authority` and `but the request addressed category` in the live `LindenCaps.dll` and **none of them in the pre-deploy backup of the same file**. | **VERIFIED LIVE 2026-09-12 - step 19 passed.** The grid came up at 05:44:54 on `1.1.354-alpha+0cbd371567` with the standing check giving its six lines (Ebony, Transylvania, Elm, each `AIS v3 "ON" ("global")` and `server-side baking "ON" ("global")`). Truly on **Ebony**, cap `/3a09209b-f377-43ea-8201-38e5d5df5255`, agent `a7d2ff2e`: her **own** COF children fetched **200**; **Aleric's** COF children fetched **404**; `DELETE` of Aleric's COF children **404** - the one the log carries verbatim, at 06:19:09,613/618, `PurgeDescendents -> 404 "no category `509aa3ff-15be-4cc6-be8b-5c9aa8398a42`"`. Before 1.1.354 every one of those answered 200 and did the work, so this is the behavioural proof a hash cannot give. And the two things that had to keep working did: an **outfit change held** and a **tint save held**. Note the two GET 404s are the operator's observation and not log lines - `WriteError` logs only mutations (`AisHandler.LogMutationResponse` / the `IsMutation` guard in `WriteError`), so a refused *fetch* leaves no entry. Worth knowing before anyone looks for them. **"Did it land" was checklist step 19** (not 17; 17 and 18 were taken by S9 and S10), named before the copy and unchanged by it: Truly fetches Aleric's COF UUID through her own `InventoryAPIv3` cap and gets **404**, then the same UUID against `PATCH /category`, `DELETE /category/{id}/children` and `PUT /category/{id}/links`, each refused with Aleric's folder name, version and link rows unchanged in the database. It fails differently from the bug - 200-and-done becomes 404-and-nothing - which no hash can tell apart. **On the next start, also:** the standing `AIS v3 "ON"` / `server-side baking "ON"` six lines (two each for Ebony, Transylvania and Elm, every one `("global")`), no `[AIS]` ERROR lines, and no exception naming `InventoryServiceBackend`. Baseline from the 17:42 start of 1.1.350, taken while it was still up: those six lines present, zero `[AIS]` ERROR, zero mentions of `InventoryServiceBackend`. |
| 2026-09-12 | A21 - AIS-SEC-2: a malformed body was read as "remove every link" | `bd5cfe05d0` (red), `97c4ff0800` (harness), `6c37b5e9d5` (fix), `<this commit>` (docs) | **A truncated `PUT /category/{COF}/links` emptied the wearer's Current Outfit, and a dropped connection was enough to cause it.** A slam has REPLACEMENT semantics, so "I could not understand this body" and "this body asks for no links" are one keystroke apart in effect and a universe apart in meaning - and the code could not tell them apart. `AisHandler.ReadBodyOsd` ended in `catch { return new OSDMap(); }` and returned that **same** empty map for an absent body; `AisSlam.ParseBody` then read an empty map as an empty slam (`map.Count == 0 -> new OSDArray()`). `ParseBody` also skipped non-map entries silently, accepted a zero `linked_id` and accepted any asset type. Spec A-Q3 verifies the viewer sends a bare LLSD **array** of link maps and never `{}` (`llappearancemgr.cpp:2209-2245`, `:1795-1833`), so every one of those was a body no viewer produces. **THE PARSER FINDING, and it is the part that is easy to get wrong.** Probed against the shipped parser on 2026-09-12: **`OSDParser.DeserializeLLSDXml` neither throws nor returns null** for truncated XML or for well-formed XML that is not LLSD - it returns a **bare `OSD` whose `Type` is `OSDType.Unknown`**. So detection keys on the *type*, not on an exception and not on null. A `try`/`catch` plus a null check would have caught neither, and a malformed `PATCH` body would still have become an empty map and still answered **200**. That is exactly what the two red `PATCH` cases showed. The doc comment on `ReadBody` says so at the method so nobody simplifies it back. **FOLLOW-UP AUDIT ITEM, NOT THIS SESSION:** other body-reading caps in this tree that rely on a **throw** from that parser to detect a bad body have the **same blind spot** - they will treat `OSDType.Unknown` as a parsed body. Nothing outside AIS was examined here; the sweep is its own session. **THE FIX.** `ReadBodyOsd` is replaced by `ReadBody`, returning `NoBody` / `Parsed` / `ParseFailure` / `TooLarge`. A **1 MiB** ceiling (`AisHandler.MaxBodyBytes`, public so the tests assert the same number the handler enforces) is applied **while** the stream is copied and reading stops the moment it is passed - a body too large to trust is also a body too large to hold. `Dispatch` gates every mutating route before the switch: `ParseFailure` **400** *"malformed LLSD body"*, `TooLarge` **413**, and `NoBody` **400** *"missing body"* for `SlamFolder` alone; the other mutating routes keep today's empty-map behaviour for `NoBody`, their semantics being out of scope. Fetch routes are untouched and still ignore the body. Parse failures log at **Warning** with route, agent and byte count - never the body, which is attacker-controlled. `ParseBody` loses the `map.Count == 0` branch (`{}` is now 400), keeps `[]` as the intentional empty slam, keeps `{"links":[...]}` and `{"contents":[...]}`, and is **all-or-nothing**: one bad entry rejects the whole body, because skipping an entry the viewer meant to keep is the same outfit loss by a quieter route. A link needs a non-zero `linked_id` and a type of `AT_LINK` or `AT_LINK_FOLDER`. **THE LIBRARY-CAP ORDERING, found by a test going red.** The body gate runs **after** the read-only mode check: on `LibraryAPIv3` every mutation but COPY answers **405** because the cap is read-only, and that is true whatever the body says. The first cut ran the gate first and turned *"this cap does not do mutations"* into *"your body was malformed"* - less informative and wrong about the reason - which broke `the_library_cap_serves_the_same_reads_and_refuses_every_mutation_with_405`. **COPY keeps the size bound**, being a genuine library operation. That failure was a defect in the fix, and it was corrected in the fix rather than in the test. **A PRE-EXISTING TEST WAS WRONG, and the stop condition caught it.** `the_previously_unimplemented_mutations_now_answer_properly` built an `OSDArray` for its PUT case and then wrote `body as OSDMap` to satisfy a harness narrowed to `OSDMap` - the cast silently became **null**, so the case sent **no body at all** and passed only because the handler conflated no-body with `[]`. It was green for the reason this session removes. Harness widened to `OSD` in its own commit (`97c4ff0800`); all six request doubles audited and `as OSDMap` now appears nowhere in the test tree. **Recorded because the brief predicted that commit would be red against the unfixed handler and it measurably is not**: with the harness fixed the PUT sends a real `[]`, and `[]` answers 200 both before and after - it is the intentional empty slam. What was red was the *uncorrected* test against the *fixed* handler. The commit is a coverage correction, not a red-to-green one, and the no-body case is covered adversarially by its own test instead. **Tests 172 -> 184**, 0 failures; solution 0 errors; `new OSDMap()` inside a `catch` in the AIS folder: **0 hits** (the only match is the doc comment describing what was removed). The ten SLAM cases: truncated XML **400**, valid-XML-not-LLSD **400**, no body **400**, `{}` **400**, non-map entry **400**, zero `linked_id` **400**, type 0 **400**, over 1 MiB **413**, `[]` **200** (links emptied), valid two-link array **200** (links replaced); `PATCH /item` and `PATCH /category` with malformed XML **400**. Every negative case asserts **zero backend writes** and byte-for-byte unchanged COF link rows - `FakeAisBackend` gained a `Writes` view over the `Calls` list it already kept. | **Deployed `1.1.354-alpha+0cbd371567` -> `1.1.359-alpha+6c37b5e9d5`, both roots, servers down (clean region stop 06:20:56, grid 06:20:58) and not started.** Seventeenth run of the written procedure, copied 07:00. Region `-c Release -r win-x64 --self-contained false` (**855** published, 697 identical, **158** differed, **0** new, **152** copied); grid `-c Release --self-contained false`, no `-r` (**693**, 601, **92**, **0**, **90**). Staged `stage26`. Re-hash of the full copy set in the live roots against the staged manifest: **152 and 90, no mismatch, one clean pass**. **Step 8a ran under the stamp-width caveat and the run band was BLIND**: staged `1.1.359.27703` is five digits against live `1.1.354.3261`'s four, so every stamped assembly shifted a character and run counts spread 5-284,923 (region) and 184-6,671 (grid). Tested by **size** instead, as the caveat requires: **exactly one** region assembly moved length - `OpenSim.Region.ClientStack.LindenCaps.dll`, **+3584 bytes** - and the **grid had zero**, correct by construction since `LindenCaps` is not in the grid publish at all. `InWorldz.Phlox.dll` again showed the identity-only difference now documented in 8a: **5 runs, size +0, 72 bytes total = 4+16+4+16+32**, landing exactly on the PE TimeDateStamp (`0x88`), the MVID (`0xAE9CC`), the debug-directory entry timestamp (`0xB4258`), the CodeView PDB GUID (`0xB42AC`) and the PdbChecksum (`0xB4309`), with the debug directory and CodeView offsets read from the PE headers. **Five runs this time, not six**, because the PDB GUID came out as one clean run rather than being split by a coincidentally-equal byte - the byte total is the stable signature, not the run count. Not a stop, as the brief anticipated. **joltc gate, seventh run, passed**: two `joltc*` staged, both `16AF7638...43B5`; every file of all four sets hashed against the stock hash (855 + 693 staged, 2391 + 779 live) - **zero matches**; guard exit 0 staged and live pre-copy and live post-copy. **Pre-copy probe clean on all five parts**, gate re-run immediately before the backups, **exclusive-open probe 16 of 16**. **0 new files**, so step 7 had nothing to gate. **Preserved and byte-identical against the backup**: the 6 region `config-include` files, the 2 grid `*.example`, and whole-tree checks over `regionserver\config` (4), `config-include` (12), `Regions` (2) and `gridserver\config` (4) - **0 differences anywhere**; live-only `WebRtcVoiceServiceModule.dll` still `1.1.160-alpha+d95754509d` and absent from the publish. Counts unchanged: region **2391**, grid **779**. Backups `regionserver-20260912-0700` (2391) and `gridserver-20260912-0700` (779). **Byte-verified in the deployed binary**: `malformed LLSD body`, `missing body`, `the request body exceeds the`, `MaxBodyBytes` and `AisBodyStatus` are all present in the live `LindenCaps.dll` and **absent from the pre-deploy backup** of the same file. (`ReadBody` appears in both, the old `ReadBodyOsd` containing it as a substring - noted so the hit is not read as evidence.) **VERIFIED LIVE 2026-09-12 - step 20 passed, and the log carries every half of it.** The grid ran `1.1.359-alpha+6c37b5e9d5` from 07:15:53 to 07:47:47. Truly on Ebony, agent `a7d2ff2e`, COF `71c3c184-410b-4dae-b20a-855741cf1faf`, which held **14** links going in. **(a) Malformed XML -> 400.** 07:19:58,987 `WARN [AIS]: SlamFolder on "/category/71c3c184.../links" from agent "a7d2ff2e..." sent a body that is not LLSD (18 bytes); refused without writing`, then 07:19:58,990 `SlamFolder -> 400 "malformed LLSD body"`. The **18 bytes** is `<llsd><array><map>` to the character, and the WARN line is this session's own new logging doing its job. **(b) `{}` -> 400, by the other route, which is the right one.** 07:20:04,040 `SlamFolder -> 400 "a slam body must be an LLSD array of link maps (name, desc, linked_id, type)"` - **not** "malformed LLSD body", because `{}` **is** valid LLSD. It fails *validation* in `AisSlam.ParseBody`, not *parsing* in `ReadBody`, and the two messages distinguishing themselves in the live log is the design working as intended. **(c) The outfit was untouched by both**, then `[]` emptied it on purpose: 07:20:21,271 `SlamFolder -> 200` with `_removed_items` listing exactly **14** ids and `{71c3c184:796}` - so the 14 links the two refusals did not touch are the same 14 the intentional empty slam removed. That is the whole guarantee in three log lines: zero writes on a bad body, and the legitimate empty slam still works. **(d) The viewer round-trip, which is what proves it is not an outage.** Replace Outfit between "Truly Base" and a newly saved dressed outfit, repeatedly: 12 created / 12 removed at 07:27:28 (v832), 07:27:34 (v856), 07:35:00 (v885), 07:35:35 (v909), 07:45:45 (v941), 07:45:51 (v966), plus 14 created into the **new** outfit folder `f86ab3fd-a5e8-4894-bddf-710e43b5b078` at 07:45:36 (v15). **SSB followed it**: `Upper=Baked, reused 4/5` on the dressed one at 07:31:53, 07:34:36, 07:35:07, 07:44:28 and 07:44:44, and `reused 5/5` back on the base - the bake tracking the outfit rather than recomputing blindly. **One thing seen and explained, not a fault:** a run of `RemoveItem -> 404` on link ids (`7f729ba1`, `c8d29a8e`, `fd992ec8`) between 07:31 and 07:45. Those are links a slam had **already** removed, which the viewer then asked to delete individually from its stale cache - 404 is the correct answer and no write follows. Recorded so the 404s in that window are not read as an AIS-SEC-2 regression. |

## 5. Cross-references

- `Docs/feature/ssb-appearance/S0a-VERIFICATION.md` V6 (folder-version increment sites) and V9 (Robust handler pattern for Phase 2) — reused, not re-derived.
- `Docs/feature/ssb-appearance/ADR-SET-ssb-appearance.md` ADR-006 (SSB reads COF `Version` from the inventory service; no AIS dependency).
