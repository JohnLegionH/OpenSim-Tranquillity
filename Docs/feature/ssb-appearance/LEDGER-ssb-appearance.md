# Ledger — Server-Side Baking (L-2)

Living document. Update at every session close. **Date opened:** 2026-09-03

## 0. Principles

| ID | Principle |
|---|---|
| P-1 | The authority for bake output is the LL viewer compositor (`lltexlayer.cpp` / `llavatarappearance.cpp`) and SL's bake-service behaviour. Firestorm is a capture tool for reference bakes only; no code, config, threshold, or test may be keyed on Firestorm. |

## 1. Decisions

| ID | Decision | Status | Ruled | Notes |
|---|---|---|---|---|
| D-1 | Build SSB **before** AIS v3 (reverses BP-v2 order AIS→SSB) | **OPEN — needs John** | — | Justified by V7: LL viewer gets "log in as yourself"; web viewer needs it now. ADR-006 removes the AIS dependency. |
| D-2 | Appearance service + reaper on Robust; compute in the region | Accepted | 2026-09-02 | ADR-002 |
| D-3 | Sim-side fidelity policy: best-effort + report (recommended) vs strict refuse | **OPEN — needs John** | — | ADR-005. Affects whether Legion Grid can flip the flag while any unsupported wearable type remains. |
| D-4 | Library placement `Source/OpenSimNGC.Appearance.Baking`, NuGet-published | **OPEN — needs John (and Mike)** | — | ADR-003. Interim: gateway `ProjectReference`. |
| D-5 | Test region for flag-on = Transylvania | Proposed | — | Build Plan §4 |
| D-6 | Bakes expire (TTL reaper, default 30 days, off on standalone) | Accepted | 2026-09-02 | ADR-004 |
| D-7 | Bake size default, parameterised | **Superseded by measurement 2026-09-03** | 2026-09-02, revised 2026-09-03 | The 512 default ruled here was replaced by **1024** on the S1b evidence: 1024 is the knee (aleric lower mean abs RGB 2.29 at 512 vs 0.89 at 1024) and 2048 buys nothing at ~1.8× the bytes. See `S1b-FIDELITY.md` §6 and ADR-008. No ini change: S1 already shipped 1024 and the live sim runs it. 512 and 2048 stay configurable. |
| D-8 | Add-only: no legacy appearance handler removed | Accepted | 2026-08-31 | ADR-001 |
| D-9 | Gateway is a pure consumer on bit-0 regions | Proposed | — | ADR-009; keeps web-viewer G6 intact |

## 2. Open questions

| ID | Question | Owner | Blocks | Status |
|---|---|---|---|---|
| Q-1 | Does the inventory service bump COF folder `Version` on the UDP link create/delete path? | S0a | S3, S5 | **Resolved S0a V6:** yes — data layer `IncrementFolderVersion` on item store/delete/move and folder store; cap `CreateInventoryCategory` bumps the parent too. Sim must read `Version` fresh via `GetFolder`. |
| Q-2 | `avatar_lad.xml` provenance — vendor as embedded resource with LGPL notice (ADR-007)? | John | S0a | **Resolved S0a (`7dbc092d2e`):** vendored; provenance recorded in `Source/OpenSimNGC.Appearance.Baking/THIRD-PARTY-NOTICES.md` (viewer 26.1.1, wearable_definition_version 22, SHA-256). Viewer commit id not confirmable from `F:\viewer-develop` (no `.git`). |
| Q-3 | Is the Legion `Client_OnAvatarNowWearing` empty-appearance wipe-loop fix present on Tranquillity? | S0a | flipping any production region | **Resolved:** bug present (S0a V5, `AvatarFactoryModule.cs:1205` at `7dbc092d2e`), fixed in S0c `a5e88d72f1` — merge into existing wearables via `AvatarFactoryModule.MergeNowWearing`; unlisted slots retained, `UUID.Zero` still clears, save only on change. |
| Q-4 | Which J2K encoder does the tree expose (OpenJPEG via OpenMetaverse vs CoreJ2K/CSJ2K), and does it produce viewer-decodable J2C with the right layer count for 512 bakes? | S0a | S0b | **Resolved S0a V7:** CoreJ2K.Skia 2.3.3.91 (plain NuGet, no OpenJPEG code); single-tile config required (`WithTiles(t => t.SetSize(w, h))`, upstream #201 found multi-tile output renders blank). Layer count for 512 bakes still to be confirmed in S0b. |
| Q-5 | Does LibreMetaverse expose `RegionProtocols` from `RegionHandshake` to the gateway, or does the gateway need to read it off the raw packet? | S6 | S6 | open |
| Q-6 | Does Firestorm on a bit-0 OpenSim region still send `AvatarNowWearing` on outfit change, or only the cap POST? | S5 | S5 | open |
| Q-7 | Golden fixtures: which 6 (later 11) reference bake asset UUIDs (LL compositor output, captured via the Firestorm client-bake path) for Truly's stock outfit? | **John** | S0b's diff step | **Resolved S0b:** the five classic ones are in `Golden/manifest.json` (skirt and the five BoM aux slots are not set for this outfit); fetched by `fetch-fixtures.sh`, never committed. |
| Q-8 | Server bakes were 4-component; a viewer bake carries a 5th component — the **morph mask** (`gatherMorphMaskAlpha`), not a bump channel (bump layers are never rendered) — parity gap; add in the library before S3 | S0b → library | S3 | **Resolved S0d `30c82d472b`:** the premise was wrong — `lltexlayer.cpp:395` renders only `RP_COLOR` layers, so bump layers never reach a bake; the 5th component is the morph mask (`gatherMorphMaskAlpha`, `lltexlayer.cpp:460-472`), now produced and encoded (5-component single-tile J2C). Spec: `Source/OpenSimNGC.Appearance.Baking/Docs/MORPH-MASK-PASS.md` (renamed S0e). Reference check: head 0 vs 1, others 255 vs 254. |
| Q-9 | `appearance-utility-bin` as a viewer-independent golden generator; Linux/GL one-off; later | — | — | **CLOSED S1 Part 0 — not possible.** `F:\viewer-develop` carries no `appearance-utility-bin` sources: `indra/cmake/LLAppearanceUtility.cmake` only does `use_prebuilt_binary(llappearance_utility)`, gated on `INSTALL_PROPRIETARY` + LINUX — a proprietary prebuilt we do not have and may not redistribute. There is no viewer-independent generator to build from the permitted tree. The R,G,B,A,M packing order therefore stays **empirical**, per `Source/OpenSimNGC.Appearance.Baking/Docs/MORPH-MASK-PASS.md` §2; it is validated by the golden diff against reference bakes, not by a citation. |
| Q-10 | Where does the bake second go? Cold bake on the live sim was **2788 ms for 5 channels at BakeSize 1024** (Truly) and **3078 ms** (Aleric), with no instrumentation to say which phase owned it. | S2 | S2 scope, S3 login trigger | **CLOSED S2b by live measurement on Ebony.** `BakeTimings` (S2 Part 2) splits every bake five ways on the INFO line and in the console table. Live cold bake, five channels at 1024: **2823 ms** — fetch **76**, decode **282**, composite **970**, encode **1236**, store **60**, other **198**; 15 assets fetched (377 KiB), 303 KiB encoded. Live warm bake: **70 ms, reused 5/5**. So **composite + encode = 2206 ms, 78% of a cold bake**, and the input-hash skip is worth about **40x** (2823 -> 70). The answer to the question as posed: compositing and encoding dominate, asset fetching does not, so the ADR-004 skip is the whole win and per-channel parallelism is the remaining lever on the cold path — it would cap a cold bake at the slowest single channel. **Correction to S2's estimate.** From the in-memory harness S2 inferred "~390 ms for asset I/O if the machines are comparable". Live says asset I/O is **136 ms** (76 fetch + 60 store), a third of that; the rest of the gap is encode (1236 live against 1019 in the harness) and an "other" remainder of **198 ms** against the harness's 4 ms — wearable parsing, hashing, the bake-index read and write, and face writes, which are noise locally and are not noise on the live sim. The apportionment was wrong even though the conclusion held; the instrumented figures supersede the harness ones for any planning. |
| Q-11 | The unsupported-layer surface is **untested against real content.** S1's fidelity log for Truly's stock outfit shows every skip as "nothing worn" or "every mask parameter skipped" — no unsupported layer was hit at all. Multi-wearables per slot, tattoo, universal, alpha, gloves, socks, jacket and skirt channels have never met live content. | **John** | flipping `ServerSideBaking = true` on any region | **Narrowed S1b, not closed** (`296051b301`, `S1b-FIDELITY.md`). Second reference set `aleric-max` (Aleric Fenwood, Ebony, 2026-09-03) covers **jacket, socks and tattoo**; **multi-wearables, universal, alpha, gloves and skirt remain untested**, and with universal go the five BoM aux channels. No layer returned UNSUPPORTED on either outfit, and RGB/alpha passed on every channel of both — the richer outfit is closer in RGB (0.78-0.89) than the stock one (1.36-1.41). It did surface one real defect: see Q-12. R-1 is not retired. |
| Q-12 | **Upper-channel morph mask is empty when a jacket is worn without a shirt.** Aleric's reference upper bake carries a real morph mask (31.4% of pixels near 0); ours is uniform 255, because `avatar_lad.xml`'s `<morph_masks>` names only `upper_clothes` for upper_body and that layer's `local_texture` is the Shirt slot, which he wears empty (null asset id). mean |dM| **82.24**, **33.21%** of pixels over 8, identical at 512/1024/2048. | S2 or a dedicated slice | S3 flag flip on any region whose residents wear jackets | **Resolved S1c `ade0e01a44`** (candidate (b), with (a) refuted by the authority): a morph-mask template layer takes the mask of the **top worn wearable of its type, and a wearable counts as worn whether or not it carries a texture asset** — `updateWearableCache` counts `LLWearable` objects, not textures (`lltexlayer.cpp:1615-1638`), so Aleric's occupied-but-assetless Shirt slot still drives `upper_clothes`'s four param alphas, while `upper_jacket` — absent from `<morph_masks>`, so `hasMorph()` is false (`:1297-1302`, `llavatarappearance.cpp:1237-1243`) — contributes nothing. Spec: `MORPH-MASK-PASS.md` §2.4. upper meanAbsM **82.24 → 0.99**, pctM>8 **33.21% → 1.59%**, histogram 0:31.5%/224:66.4% against the reference's 0:31.4%/224:67.0%; RGB, alpha and every truly-stock number unchanged. |
| Q-13 | **A bake channel in which nothing was drawn overwrote its texture face with a solid image.** An all-skipped channel is not a blank bake: `LLTexLayerSet::render` clears the canvas to opaque black and only a mask layer would carve it, so the encoded result is solid — 96.5% opaque near-black, measured. The orchestrator stored it and wrote the face. The live trigger that predates S1d is **a real skirt wearable carrying no skirt texture**; S1d widened it to worn-but-assetless slots, which are common (default system items). | S1e | any server bake on an avatar with such a slot | **Resolved S1e** (`e338ecc598` library, `9b428e0994` orchestrator). Found by S1d while carrying S1c's rule into the region path, and fixed here rather than deployed: S1d stopped before its deploy step for this reason. `BakeResult.NothingDrawn` is set from the compositor's per-layer decisions when no layer has status `drawn`, and `StoreAndApply` neither stores nor applies such a channel — outcome `Skipped`, reason "nothing drawn for this channel". Drawn-but-transparent is explicitly **not** undrawn: Truly's bald hair draws a 4x4 transparent texture and its all-transparent bake is still stored, guarded by its own test so the fix cannot decay into "discard transparent bakes". |
| Q-14 | **Any appearance save destroys the ADR-004 bake index.** `AvatarService.SetAvatar` deletes every row for the principal (`Source/OpenSim.Services.AvatarService/AvatarService.cs:93`) before rewriting only the keys `AvatarData(AvatarAppearance)` derives — serial, height, wearables, visual params, attachments (`Source/OpenSim.Services.Interfaces/IAvatarService.cs:142-189`). `SetAppearance` goes through it, and `AvatarFactoryModule.QueueAppearanceSave` fires it ~5 s later (`AvatarFactoryModule.cs:51,334,828`). So an outfit change, an attachment change or any other queued save wipes `Bake:*`, `BakeHash:*`, `BakeCOFVersion`, `BakeSize` and `BakeUpdated`. | S2 → S3 | reuse rate in production; the reaper | **Open, mitigated and bounded in S2.** The failure is one-sided: the index is either wholly present or wholly absent, never stale-but-wrong, and absent means re-bake, which is always safe. Two costs remain — a needless full re-bake after an unrelated save, and a previous bake asset that supersede can no longer find (it is still findable by its `bake:<agent>:<channel>` name, which is why ADR-004 has that marker). S2 removed the one wipe the bake path caused itself: the bake no longer calls `QueueAppearanceSave`, because a bake changes only the baked faces and the avatar service does not persist those at all. Pinned by `BakeReuseTests.AnAppearanceSaveWipesTheIndex_AndTheNextBakeSimplyRebakes`. Fixing it properly means either teaching `SetAvatar` to preserve non-appearance keys (a service change, out of S2's scope) or re-writing the index after each save; S3, which owns the login and COF triggers, is where that decision belongs. |
| Q-15 | **The branch SSB now lives on is called `feature/ais-v3`, and that name is why this went wrong.** Two lanes were deploying to the same live region from different branches; the last deploy from `feature/ais-v3` silently removed the SSB console command, and a naive deploy from `feature/ssb-appearance` would have removed AIS. `feature/ais-v3` is in fact the integration branch: AIS v3 + `fix/appearance-save-flush` + `integration/legiongrid-trusted-hg` + SSB, and both live servers are built from it. | John | nothing technical; a naming and process decision | **Open, deliberately not acted on.** Candidates: rename to `integration`, or fold into `develop`. Not renamed this session by explicit instruction — a rename touches every worktree, both deploy scripts and the nbgv height. Until then: **deploy only from `feature/ais-v3`, and merge feature branches into it rather than deploying them.** Mirrored as A-Q18 in the AIS ledger. |
| Q-16 | **Which store does S5's outfit-change trigger read, and when?** AIS v3 is live and the COF is the authoritative record of what is worn, but the bake reads `sp.Appearance.Wearables` (`ServerSideBakingModule.cs:125`, an S1 Part 1a choice made before AIS existed). The two disagree for a window on every outfit change. | **John** | S5 | **Open; analysed and recommended in Design Brief §4.6.** The trace: AIS never touches the ScenePresence, so the region learns of an outfit change only from the viewer's UDP `AgentIsNowWearing` (`AvatarFactoryModule.cs:1289` is the sole assignment to `Appearance.Wearables` in the tree), and `MergeNowWearing` gives a newly worn item `AssetID = UUID.Zero` until `SetAppearanceAssets` resolves it inside the save that fires `DelayBeforeAppearanceSave` (5 s) later. A bake in that window reads the new item as **worn but assetless** and bakes its morph mask with none of its pixels. Options: (a) read the COF directly — authoritative, but a Robust round trip, a folder-version dependency, a second wearable resolver in the tree, and it still leaves `Wearables` disagreeing with the bake just stored; (b) keep reading the ScenePresence and trigger after the region has applied the change. **Recommended: (b), hooked to the completion of `SaveAppearance`** — the unused `EventManager.TriggerAvatarAppearanceChanged`, whose only call site is commented out at `AvatarFactoryModule.cs:891`, immediately after `AvatarService.SetAppearance`. That single ordering also fixes Q-14 for this path: the save that would wipe the index has already run. The argument is asymmetric failure — **(b) fails late** (a bake that does not happen; the previous valid bake stands; one console command recovers it) while **(a) fails wrong** (a bad bake stored and applied, with supersede having already deleted the good one — the Q-13 class of defect). Q-6 is the one measurement that could overturn it. |

## 3. Risks

| ID | Risk | Mitigation |
|---|---|---|
| R-1 | Compositor output worse than Firestorm's on some outfit → Firestorm users on a bit-0 region degrade | S0b diff gate before S3; per-region flag; ADR-005 never overwrites a good bake with a refused one |
| R-2 | COF version livelock (viewer re-requests forever) | Anti-livelock rule §4.3: after N mismatches in T seconds, bake with server version, log |
| R-3 | Bake assets accumulate on operators' grids | Supersede-delete + TTL reaper (ADR-004) |
| R-4 | Wipe-loop bug (Q-3) present → server-initiated bake reads a half-wiped wearable set and persists a wrong bake for every viewer | S0a check is a hard gate before any production flag flip; module never *writes* wearables, only bakes and TE |
| R-5 | Two compositor copies drift (gateway vs sim) | ADR-003: gateway dir deleted at S0b |
| R-6 | NGC package publication delayed (Mike) | `ProjectReference` interim; no code difference |
| R-7 | `AppearanceData` block on `AvatarAppearance` breaks an older third-party viewer | Emitted only for sim-baked avatars; Firestorm parses it on SL daily; flag-off regions unchanged |

## 4. Session log

| Date | Session | Commit | Result | Decisions/questions raised |
|---|---|---|---|---|
| 2026-09-02 | CC recon | — | RECON delivered to `D:\_TO_REVIEW\ssb-appearance\`; D-2/D-6/D-7 ruled; brief paused | — |
| 2026-09-02 | wire spike | (none) | Sim delivers others' bake UUIDs + VisualParams; AppearanceData omitted | — |
| 2026-09-02 | web-viewer S11/S12 | 6 local | Gateway compositor exists, LibreMetaverse Baker disqualified | ADR-003 source |
| 2026-09-03 | doc set | — | Addendum, Design Brief, ADR set, Build Plan, this Ledger | D-1, D-3, D-4 open |
| 2026-09-03 | S0a | `29105ccc44`, `7dbc092d2e` | Verification pass V1–V10 (`S0a-VERIFICATION.md`); `OpenSimNGC.Appearance.Baking` skeleton + test project in `Tranquillity.sln`, `avatar_lad.xml` embedded; solution builds, 1/1 test green | Q-1 yes; Q-2, Q-4 resolved; Q-3 = wipe bug present → hard gate; libomv drift 1.1.6 vs upstream 1.1.7 |
| 2026-09-03 | S0b | `303d2b39c1`, `8a245aa286`, `cbf3284e06` (tranq-ssb); `be67e2d` (web-viewer) | Compositor moved into `OpenSimNGC.Appearance.Baking` (no LibreMetaverse; own enums, TGA reader, 56 character masks embedded); `SkiaBakeBackend`, single-tile J2C, `BakeHash`; gateway is an adapter over the library, local compositor deleted; 24/24 library tests, 13/13 gateway tests. Golden harness vs reference bakes (LL compositor, captured via the Firestorm client-bake path) at 512, mean abs RGB / % pixels >8: head 1.62 / 1.47%, upper 1.41 / 0.64%, lower 1.36 / 0.63%, eyes 0.36 / 0%, hair n/a (both fully transparent). Decoder fix: 5-component viewer bakes now carry alpha. | P-1 recorded; Q-7 resolved; Q-8 (5th channel), Q-9 (appearance-utility-bin) opened; ADR-007 wording needs the masks; bake size 512 vs the reference's 2048 — threshold to be set on these numbers |
| 2026-09-03 | S0e | `916dc35d00`, `22b3695389` (tranq-ssb); `0a6acff` (web-viewer) | S0d decision 4 premise was wrong: `isUserSettable()` is `mLocalTexture != -1` (`lltexlayer.cpp:64`), so only textured layers are per-instance templates (`:290-297`, `:1659-1689`); a no-texture layer is a plain `LLTexLayer` rendered once with the last-worn wearable's parameters — the colour pass already did this, the S0d morph gather was fixed to match (test: two hairs == last hair, facialhair once). Doc renamed `MORPH-MASK-PASS.md`; viewer 26.1.1 has no `doUpload`/upload path, so the R,G,B,A,M order stays empirical. Gateway builds and passes 13/13 against the 5-component library unchanged. 27/27 library tests; golden table unchanged. | Q-8 wording corrected; packing-order citation impossible from the permitted viewer (no upload path in 26.1.1) |
| 2026-09-03 | S0d | `30c82d472b` | 5th component per LL `lltexlayer.cpp`: not a bump pass (RP_BUMP never rendered, `:395`) but the morph mask (`gatherMorphMaskAlpha`); compositor computes it from the `<morph_masks>` layers (facialhair / upper_clothes / lower_pants), encoder writes 5-component single-tile J2C via CoreJ2K planar streams, decoder exposes it. Masks re-verified SHA-256-identical to viewer 26.1.1 (0 replaced; `head_wrinkles_highlights_alpha.tga` absent from the viewer too). Golden 5th-component columns: head 0.00 / 0% (ref uniform 1), upper/lower/eyes/hair 1.00 / 0% (ref uniform 254); RGB now head 1.37, upper 1.41, lower 1.36, eyes 0.36, hair 0.00 (planar encode keeps RGB under zero alpha). 26/26 tests. | Q-8 resolved; ADR/Q-8 wording should drop "bump"; RP_BUMP layers stay unrendered by design |
| 2026-09-03 | S0c | `a5e88d72f1` | `Client_OnAvatarNowWearing` merges into existing wearables; 4 new xunit tests green. `OpenSim.Region.CoreModules.Tests` has 35 pre-existing failures at clean `7dbc092d2e` (identical set before/after; NRE/null-asset in Flotsam, IAR, PrimCount, Moap, Serialiser, two legacy AvatarFactory tests) — environmental, not caused by S0c | Q-3 resolved; R-4 gate closed; pre-existing CoreModules test failures need a separate owner |
| 2026-09-03 | S1 | `bbc065bc5f`, `99118ea1ab` | **Part 0:** golden thresholds set on the S0d numbers — mean |dRGB| ≤ 4.0, pixels |dRGB| > 8 ≤ 5.0%, mean |dA| ≤ 2.0; the two RGB assertions are skipped and said so when the reference alpha is entirely zero (hair). ADR-007 extended to cover the 56 `Data/character/*.tga` (status stays Proposed). **Parts 1–2:** `Source/OpenSim.Region.OptionalModules/Avatar/ServerSideBaking/` — `IServerSideBaker`, scene-free `BakeOrchestrator` (resolve → bake → store → write faces), `ServerSideBakingModule` (`ISharedRegionModule`, registered via `PluginRegistration`, config `[Appearance] ServerSideBaking` / `BakeSize` / `BakeQuality`, console `appearance serverbake <first> <last>`). Faces written from `AvatarAppearance.BAKE_INDICES` (`AvatarAppearance.cs:63`); send via `SendAppearanceToAllOtherAgents` (`ScenePresence.cs:4315`) + `SendAppearanceToAgent` (`:4338`), then `QueueAppearanceSave` (`IAvatarFactoryModule.cs:93`). Wearable set comes from `sp.Appearance.Wearables`, **not** COF. New project `Tests/OpenSim.Region.OptionalModules.ServerSideBaking.Tests` (the existing `OpenSim.Region.OptionalModules.Tests` folder has no csproj): 3/3 seam tests green, 27/27 library tests green, solution 0 errors. | Q-9 closed (no utility sources in the permitted viewer tree); `Reused` status reserved for S2; `ServerSideBaking` parsed and logged only until S3 |
| 2026-09-03 19:14 | S1 deploy | `99118ea1ab` (deployed) | Region-side only, both servers down. Built Release (0 errors), published `Source/OpenSim.Server.RegionServer` `-c Release -r win-x64 --self-contained false` — the one project whose publish transitively carries Framework, CoreModules, OptionalModules and the new library, and the only way to regenerate `deps.json`, which must gain the `OpenSimNGC.Appearance.Baking` entry. Backup: `D:\legiongrid\_backup\regionserver-20260903-1918` (4333 files, 1.15 GB, exact). 739 files copied, 0 failed, nothing deleted. `OpenSimNGC.Appearance.Baking.dll` now in the region root, `1.1.181-alpha+99118ea1ab`, absent from the backup (first time on the live sim). Preserved: `config-include\` wholesale (5 live-customised `.ini` plus live-only `GridCommon.ini` / `osslEnable.ini` a naive copy would have clobbered), `Regions\`, `config\`, `ScriptEngines\`, `assetcache\`, `maptiles\`, `MeshCache\`, `j2kDecodeCache\`, `appsettings.json`, 35 logs, all `*.example`. `OpenSimDefaults.ini` was the one `.ini` replaced — verified first that its diff against the new publish was exactly the three new keys, i.e. no local edits existed to lose. Robust untouched: nothing on this branch changes the grid server. | Deployed branch `fix/maptile-legacy-renderer` (`11a2456833`) also carries the S0c wipe fix and the T-1 fixture repair — both reached the live sim with this deploy |
| 2026-09-03 19:46 | S1 verify loop | — | **First server-composited avatar rendered on the live grid.** `appearance serverbake Truly Bazar` on Ebony: Head / Upper / Lower / Eyes / Hair **Baked**, 6 channels skipped (nothing worn), **2788 ms** at BakeSize 1024, five new bake asset UUIDs stored and written into her TextureEntry. A second viewer (Firestorm) observing saw her re-texture from the sim's bakes and render correctly — no visible defect. | Q-10 opened (cost baseline: where the 2788 ms goes); Q-11 opened (unsupported-layer surface untested — the stock outfit hit none of it) |
| 2026-09-03 | S1b | `296051b301`, `<docs>` | Golden harness generalised to multiple reference sets (`truly-stock/`, `aleric-max/`, each with its own manifest and gitignored fixtures); `fetch-fixtures.sh <set>`; `GoldenTests` a Theory over sets plus a `bake_size_sweep`. New set: Aleric Fenwood, 10 worn slots, fetched clean (9 wearables + 11 textures from Robust, 5 bakes from the region cache). **Q-11 narrowed:** jacket, socks and tattoo covered; multi-wearables, universal, alpha, gloves, skirt still untested. **Q-12 opened:** upper morph mask empty when a jacket is worn without a shirt (mean |dM| 82.24 vs threshold 4.0, size-independent); the test is left red. **Bake size:** references are 2048 except eyes at 512; 1024 is the knee — every channel improves 512→1024 (aleric lower 2.29→0.89), nothing improves 1024→2048 while bytes rise ~1.8x. Fixtures for both sets backed up to `D:\_TO_REVIEW\ssb-goldens-2026-09-03\`. | Q-11 narrowed; Q-12 opened; ADR-008 says default 512 while the ini and the live sim run 1024 — the measurements support 1024, so the ADR should follow reality |
| 2026-09-03 | S1c | `ade0e01a44`, `<docs>` | Q-12 diagnosed from the authority before any code changed, and the S1b candidate (a) refuted: `<morph_masks>` **is** the only selector for the morph gather — `gatherMorphMaskAlpha` walks every layer (`lltexlayer.cpp:460-472`) but only a `hasMorph()` layer can produce alpha data (`:1513-1531`, `:1183-1200`, `:1297-1302`, `:1389`), and `setHasMorph` is called exactly for the layers named in `<morph_masks>` (`llavatarappearance.cpp:1237-1243`). The real rule: the contributing wearable is **counted, not its texture** (`updateWearableCache` `:1615-1638`, `getLayer` `:1639-1656`), and only the **top** one contributes — `getLayer(num_wearables - 1)`, *"For rendering morph masks, we only want to use the top wearable"* (`:1710-1719`), **correcting S0d/S0e**, which had recorded the template morph rule as once per instance; `MORPH-MASK-PASS.md` §2.2 was corrected to match. Fix confined to the morph gather and the wearable intake; the colour pass untouched. upper meanAbsM 82.24 → 0.99, pctM>8 33.21% → 1.59%; truly-stock unmoved; 32/32 library tests green, both golden sets green. | Q-12 resolved; `MORPH-MASK-PASS.md` §2.2 corrected and §2.4 added; **`BakeOrchestrator.Resolve` still drops zero-asset slots**, so the live sim does not yet benefit — a region-module change outside this library-only session |
| 2026-09-03 | S1d | `7c972b7dac`, `ab7e06c163` | Carried S1c's rule into the region path: `BakeOrchestrator.Resolve` no longer drops a worn slot whose asset id is zero — it emits an empty `WearableInput` of that type, adds the matching `ParsedWearable` so the texture pass and fidelity report see the same outfit, and records a note naming the slot. ADR-008 revised from 512 to **1024** on the S1b measurements, with D-7 marked superseded rather than deleted; no ini change, it was already 1024. **Also found and fixed a regression S1b introduced:** moving `Golden/fixtures` to `Golden/truly-stock/fixtures` left the ServerSideBaking test project pointing at the old path, so two of its three tests had been taking the "fixtures absent" early return and passing vacuously since `296051b301`; repointed, and they pass for real. **Deploy deliberately not performed** — the session's own probe found Q-13. | Q-13 opened; ADR-008 and D-7 updated; the red test was left red rather than asserted into correctness |
| 2026-09-03 | S1e | `e338ecc598`, `9b428e0994`, `<docs>` | Q-13 fixed and the result deployed. Library: `CompositeResult.NothingDrawn` / `BakeResult.NothingDrawn`, set when no layer of the channel has status `drawn` — computed from the compositor's decisions, never from pixels, and per channel rather than per outfit. Placed on `BakeResult` and not `FidelityReport` because an undrawn channel is reproduced *faithfully* (nothing feeds it) and because `FidelityReport.Refusals` is outfit-wide. Drawn-but-transparent and the `IMG_INVISIBLE` whole-region case both count as drawn. Orchestrator: such a channel is neither stored nor applied; outcome `Skipped`, reason "nothing drawn for this channel". 34/34 library tests, 6/6 ServerSideBaking, solution 0 errors, and **no golden number moved on either set**. Deployed to the live region server 2026-09-03 21:35, backup `D:\legiongrid\_backup\regionserver-20260903-2135` (4566 files, 1.167 GB, exact); all five region DLLs stamped `1.1.190-alpha+9b428e0994`. Servers left down for John to start. | Q-13 resolved; the textureless-skirt case that predates S1d is retired with it |
| 2026-09-04 | S2 Part 0 | `3455deff98` | **S1's DoD closed literally.** The golden gate had been running at 512 while every bake the live sim stored was made at 1024, so the shipped size had never been diffed. `truly-stock`'s manifest `bakeSize` 512 -> 1024 (`aleric-max` was already there); both sets pass every S1 threshold at 1024, worst margin 3.2x, and every RGB mean is better than at 512. | none |
| 2026-09-04 | S2 Part 1 | `86880b7316` | **ADR-004 persistence and the input-hash skip.** Index in the avatar service's key/value `Avatars` table through the existing `GetAvatar` / `SetItems` / `RemoveItems` — **no service change, no schema change**; longest key `BakeHash:LeftArm` (16) against `Name varchar(32)`. The skip is per channel and runs before any texture is fetched, so a reused channel costs no fetch, decode, composite, encode or store; its face is still written and the appearance still sent. Reuse requires hash match **and** the stored asset still resolving. `BakeSize` invalidates. Supersede deletes the old asset only after the new one is stored and never one a face still points at. `BakeRequest.Channels` lets the backend composite a subset and decode only that subset's textures. The bake path no longer calls `QueueAppearanceSave`: a bake changes only the baked faces, which the avatar service does not persist at all. | Q-14 opened (any appearance save wipes the index) |
| 2026-09-04 | S2 Part 2 | `162bfadcc3` | **Q-10 answered: the bake second is CPU, not I/O.** `BakeTimings` splits every bake into asset fetch / decode / composite / encode / store on the INFO line and in the console table. Cold at 1024, quality 0.85: truly-stock 2401 ms (composite 43.7%, encode 42.4%, decode 13.7%), aleric-max 2714 ms (49.9% / 39.2% / 10.8%) — composite + encode are 86-89%. Against the live 2788/3078 ms that leaves ~390 and ~360 ms (12-13%) for asset I/O. Warm runs: 4 ms and 0 ms, five channels reused, nothing composited. The compositor's first use costs ~3.2-3.9 s, once per region lifetime, not per bake. | Q-10 resolved |
| 2026-09-04 | S2 merge + deploy | `f4b18601d3`, `<docs>` | **SSB merged into `feature/ais-v3` and the region redeployed carrying all three lanes.** merge-base `11a2456833`; 111 files on the SSB side, 107 on the AIS side, two touched by both: `OpenSimDefaults.ini` (auto-merged; the `[Appearance]` block gained `ServerSideBaking = false`, `BakeSize = 1024`, `BakeQuality = 0.85`) and `Tranquillity.sln` (keep-both, three hunks, one extra `EndProject` needed; 101/101, no `dotnet sln list` duplicates). **No `.cs` conflicted.** Build 0 errors; tests SSB library 35, ServerSideBaking 17, AIS 140, appearance flush 6, TrustedHypergrid 25 (+3 skipped), HypergridService 25 — the two SSB suites re-run after copying the gitignored fixtures into that worktree, without which they pass vacuously. Deploy: region `1.1.202-alpha+bfb50070d8` -> `1.1.212-alpha+f4b18601d3`; 146 of 843 published files differed by content plus 1 new, 147 copied, all preserved paths intact and the live-only `WebRtcVoiceServiceModule.dll` untouched. **`OpenSimNGC.Appearance.Baking.dll` stopped being a live-only add-on**: it was `1.1.190-alpha+9b428e0994`, hand-placed in S1e, and the publish now carries it at `1.1.212-alpha+f4b18601d3`. `ServerSideBaking` stays `false`; `AIS_Enabled = true` in `config/OpenSim.ini` untouched. Backup `D:\legiongrid\_backup\regionserver-20260904-2059` (5008 files). Servers not started. | Q-15 opened (the integration branch's name) |
| 2026-09-04 | S2b | `<docs>` | **The AIS/COF seam named before S5 meets it, and Q-10 closed on live numbers.** Design Brief **§4.6** added: which store wins (an ordering rule, not a precedence rule — the COF decides *whether* to bake, the ScenePresence decides *what*), the full trace of what happens today with file:line, the two trigger options, what Q-14 means for each, and a recommendation. The trace's finding: **AIS never touches the ScenePresence**, so a COF change reaches the bake only via the viewer's UDP `AgentIsNowWearing`, and for the `DelayBeforeAppearanceSave` window (5 s, unbounded at the front) a newly worn item carries `AssetID = UUID.Zero` and would be baked as worn-but-assetless — its morph mask, none of its pixels. Recommended trigger: keep reading the ScenePresence and fire after `SaveAppearance` completes, via the unused, commented-out `TriggerAvatarAppearanceChanged` at `AvatarFactoryModule.cs:891`; the same ordering also resolves Q-14 for this path. Live S2 measurement recorded against Q-10: cold **2823 ms** (fetch 76 / decode 282 / composite 970 / encode 1236 / store 60 / other 198), warm **70 ms reused 5/5** — composite+encode **78%**, skip worth **~40x**. Docs only: no code, no build, no deploy. | Q-16 opened (S5's trigger — John to rule); Q-10 closed; Q-6 promoted to S5's first step |

## 5. Cross-references

- [[repo-audit]] — RECON-02 (AIS absent), RECON-03 (appearance surface, viewer contract), BUILD-PLAN-sl-parity-v2 Track L.
- [[web-viewer]] — Sessions 11/12 (compositor, fidelity gate), wire spike, hotfix e881646 (appearance-passive rule), S13 (BoM rendering).
- [[avatar-character-system]] — Ledger Q-3 system body provenance (untouched by this programme).
- [[tranquillity-fork]] — viewer-compatibility policy 2026-08-31 (add-only, per-region flags).
- [[mike-dickson]] — ADR-003 package publication.
